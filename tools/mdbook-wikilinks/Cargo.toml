[package]
name = "mdbook-wikilinks"
version = "0.1.0"
edition = "2024"
description = "mdBook preprocessor: rewrite [[wikilinks]] to mdBook links"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
