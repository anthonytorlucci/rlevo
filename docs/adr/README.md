# Architectural Decision Records

Immutable records of the architectural decisions behind `rlevo`. Once accepted,
an ADR is not edited — a later decision supersedes it, and the superseded record
is annotated rather than deleted. Read these for the *why* behind the crate
boundaries and trait design before proposing structural changes.

When you make an architectural decision, add a new numbered file here following
the existing format (`Status` / `Context` / `Decision` / `Consequences` /
`Alternatives considered` / `References`).

> Drafts and in-flight proposals live in the maintainer's working notes; an ADR
> lands here only once accepted. The repo copy is canonical.

| # | Decision |
|---|----------|
| [0001](0001-keep-environments-and-benchmarks-separate.md) | Keep `rlevo-environments` and `rlevo-benchmarks` as separate crates; use a feature-gated `bench` adapter inside `rlevo-environments` instead of merging. |
| [0002](0002-collapse-evolution-traits-into-rlevo-evolution.md) | Move `GenomeKind` into `rlevo-evolution`, delete the dead `Fitness`/`MultiFitness` traits, and drop the `rlevo-core` dep from `rlevo-evolution`. |
| [0003](0003-collapse-rl-modules-into-rlevo-reinforcement-learning.md) | Move `memory`/`experience`/`metrics` from `rlevo-core` to `rlevo-reinforcement-learning` (RL-only consumers); fold `rlevo-utils::math::combinations` into `rlevo-core::util`; delete the `rlevo-utils` crate. |
| [0004](0004-move-bench-traits-into-rlevo-core.md) | **(§6 partially superseded by 0033.)** Move `BenchEnv`/`BenchError`/`BenchStep`, `BenchableAgent`/`FitnessEvaluable`/`Landscape`, `Metric`/`MetricsProvider`, and `SeedStream` from `rlevo-benchmarks` into `rlevo-core`; drop `rlevo-benchmarks` as a prod dep of `rlevo-evolution`; switch `BenchError` to wrap `EnvironmentError` typedly. |
| [0005](0005-examples-and-cross-crate-tests-in-umbrella.md) | **Superseded by 0012.** Examples and cross-crate integration tests live in `crates/rlevo/`; crate-internal tests stay with their owning crate; `[[bench]]` entries stay put. |
| [0006](0006-leptos-first-visualisation-defer-bevy.md) | Build the visualisation layer as a Leptos web client (`rlevo-viz-web`) served by an embedded `axum` server; defer Bevy and native 3D; render `locomotion` envs as a 2D sagittal-plane projection until a successor spec re-opens 3D. |
| [0007](0007-visualisation-crates-isolated-from-production-crates.md) | `rlevo-viz-core` depends only on `rlevo-core`; per-family render adapters live in `rlevo-viz-web` behind feature flags; neither viz crate is a prod or dev dep of any production crate; `Visualize` is not a supertrait of `Environment`. |
| [0008](0008-three-tier-visualisation-ratatui-live-static-report.md) | **Superseded by 0013.** Three-tier visualisation: `AsciiRenderable` at the library level, `ratatui` TUI for live training, static-HTML Leptos viewer for post-run replay. |
| [0009](0009-move-styled-render-into-rlevo-core.md) | Hoist the styled-render surface (`StyledFrame`/`StyledLine`/`StyledSpan`/`SpanStyle`/`Color`/`Modifier`, `palette`, `AsciiRenderable`/`AsciiRenderer`) from `rlevo-environments::render` to `rlevo-core::render`; preserve import paths via a re-export shim. Forced by the `rlevo-environments ↔ rlevo-benchmarks` package cycle. |
| [0010](0010-unify-on-parking-lot-across-viz-stack.md) | Redefine `rlevo_evolution::SharedPopulationObserver` over `parking_lot::Mutex` so the EA observer alias and the record-sink producers share one lock type; add `parking_lot` as an explicit dep. |
| [0011](0011-lift-construction-off-environment-trait.md) | Remove `fn new(render: bool)` from `Environment`; add a standalone `ConstructableEnv` factory trait (not a supertrait). Kill the degenerate `new` stubs in the recording/TUI taps. |
| [0012](0012-split-heavy-examples-into-rlevo-examples.md) | Heavy viz/record/report examples move to a new `crates/rlevo-examples` leaf crate; `rlevo` retains lightweight examples, cross-crate `tests/`, and `benches/`. Canonicalizes the three-tier test placement rule. Supersedes ADR 0005. |
| [0013](0013-metrics-only-live-tui.md) | Collapse visualisation to **two products**: (1) live metrics-only `ratatui` TUI (no env panel), (2) post-run `EpisodeRecord` + static-HTML report. Demotes `AsciiRenderable` to an optional debug helper. Supersedes ADR 0008. |
| [0014](0014-record-schema-v6-single-agent-richness-and-provenance.md) | Bump record `FORMAT_VERSION` 5→6: expand `CANONICAL_METRICS`, add typed run-provenance fields to `RunManifest`, add `EpisodeKind`, an episode wall-clock metric, and a deep-RL `checkpoints` seam. Extends ADR 0013. |
| [0015](0015-shared-typed-metric-registry-crate.md) | Extract a `#![no_std]`, zero-dependency leaf crate `rlevo-metrics-registry` holding the single typed canonical-metric table; consumers re-export it. |
| [0016](0016-memetic-wrapper-and-local-search-seam.md) | Phase 3a memetic algorithms: host-side `LocalSearch<B>` trait with four gradient-free searchers; `MemeticWrapper<B, S, L, F>` implements `Strategy<B>` and owns refinement inside `tell`; one-draw two-stream RNG scheme; `WritebackPolicy` defaults to `Partial(0.5)`. Purely additive. |
| [0017](0017-probability-model-trait-and-eda-strategy.md) | Phase 3b EDAs: `ProbabilityModel<B>` trait (`fit`/`sample`); generic `EdaStrategy<B, M>`; four models (UMDA, PBIL, cGA, MIMIC). Host-RNG sampling convention. Purely additive. |
| [0018](0018-boa-bayesian-network-and-concatenated-trap.md) | Phase 3b follow-up: `BayesianNetwork` (BOA) fifth `ProbabilityModel` with BIC-scored greedy structure learning; new `ConcatenatedTrap` deceptive landscape. Extends ADR 0017; purely additive. |
| [0019](0019-observable-projection-trait.md) | New standalone `Observable<OR>` trait in `rlevo-core::state` — the typed home for **modality-changing** POMDPs where observation tensor order differs from state order. `Environment` already permits `R != SR`; zero contract change. Purely additive. |
| [0020](0020-synthetic-pixel-over-grid-env.md) | First **production** `Observable<OR>` consumer — synthetic pixel-over-grid env `PixelGridEnv: Environment<3,1,1>` projecting a rank-1 grid latent to a rank-3 RGB image. Extends ADR 0019; purely additive. |
| [0021](0021-cma-es-placement-and-self-contained-strategy.md) | CMA-ES + CMSA-ES ship as flat, self-contained `Strategy<B>` in `algorithms/{cma_es,cmsa_es}.rs`; `ProbabilityModel<B>` is available but deliberately unused (CSA + evolution paths don't fit fit→sample). Refines ADR 0017 §9; hand-rolled host Jacobi (no new dep); `SeedPurpose::CmaSampling = 11`. Purely additive. |
| [0022](0022-tensorgenome-gat-population-storage.md) | New `TensorGenome: GenomeKind` trait with a GAT `type Tensor<B>` ties `Population<B, K>` storage to the genome kind: one typed field replaces two `Option`s, accessors become total (no `.expect()`), and non-rectangular kinds (`Tree`) are excluded at the type level. Refactor; also removes the unused `GENOME_LEN` const and a dead rank assertion. |
| [0023](0023-objective-sense-and-maximize-convention.md) | Adopt a single **maximise-native** internal convention across the whole library and add a `rlevo_core::objective::ObjectiveSense { Minimize, Maximize }` primitive. Flips the entire `rlevo-evolution` engine to canonical maximise (sense-unaware); cost objectives are reconciled at one chokepoint (the `EvolutionaryHarness` + `FromLandscape`/`FromFitnessEvaluable` adapters); `BatchFitnessFn::sense()` required (only `Landscape` defaults to `Minimize`). Deletes the NEAT exception and three hand-negations; bumps record schema 6→7. K=1 atom of the deferred MOO sense vector. |
| [0024](0024-rlevo-test-support-dev-crate.md) | Extract a **dev-only** (`publish = false`) `rlevo-test-support` crate — consumed via `[dev-dependencies]`, its deps tracking the fixtures it holds (today `rlevo-core` + `rlevo-environments`) — as the workspace's general home for shared cross-crate test scaffolding. **RL is the first consumer** (evolution/hybrid to follow additively): the `LinearEnv`/`cartpole_seeded` fixtures, the `Flex` determinism preamble, a **measured** random-policy baseline (`baseline::random_return`, replacing hard-coded magic numbers), standard assertions, and the `rl_learning_test!` / `rl_reproducibility_test!` suite macros. Fixes the `<algo>_<env>_<category>` test-naming convention; per-algorithm networks and behavioural tests stay test-local. Consume-don't-constrain posture mirrors the benchmark/viz isolation rule; no production-code or schema change. |
| [0025](0025-stateful-policy-rollout-contract.md) | New `StatefulPolicy<B,E>` trait (with a `ReactivePolicy` `Hidden = ()` convenience + blanket impl) in `rlevo-hybrid` makes **recurrent / memory policies first-class** on the RL-coupled fitness seam, so `RolloutFitness`/`PolicyNeuroevolution` can host the POMDP policies the library's POMDP envs require (Santa Fe ant). `RolloutFitness` is refactored to delegate scoring to an inner `ModuleEvalFn` (deleting the duplicated unflatten-score loop, #92); the rollout threads `&mut Hidden` via `reset`/`act`. **Not purely additive** — removes the stateless policy closure and changes `RolloutFitness::new` (drops the closure, gains a captured `device`); reactive call sites migrate to `ReactivePolicy`. Scorer is `Box<dyn Fn>` (not the spec's `Arc`, which does not impl `Fn`). Preserves ADR 0023 maximise-native sense; contained to `rlevo-hybrid`. Implements #91 + #92. |
| [0026](0026-shared-config-validation-convention.md) | Introduce a standalone `Validate` trait (`fn validate(&self) -> Result<(), ConfigError>`) + structured allocation-free `ConfigError`/`ConstraintKind` in a new `rlevo_core::config` module — one shared config-validation convention for the workspace's 87 `*Config` types. Resolves the standing `rules.md §4` panic-vs-`Result` contradiction: `Deserialize`-able configs are user data, so an **assembled** config validates to `Result` while a single documented **builder setter** (`with_capacity`/`with_alpha`) may still panic. `Default`s must pass their own `validate()`. Purely additive to `rlevo-core`; adoption of the ~85 remaining configs is deferred to the per-crate issues (#102/#103, #137–139, #164–166) this unblocks. Follows the ADR 0011/0023 pattern of a small typed primitive in its own core module. |
| [0027](0027-bounds-newtype-for-closed-ranges.md) | Add a `Bounds` inclusive-range newtype (`rlevo_core::bounds`) that is **valid by construction** — the whole invariant is `lo <= hi`, rejecting `lo > hi` and `NaN` while allowing a single-point `lo == hi` and a one-sided infinite `[0.7, ∞)` healthy interval — making the `f32::clamp` panic and the `x.max(lo).min(hi)` silent-collapse hazard unrepresentable wherever a `Bounds` is held, not just at the config boundary. Complements ADR 0026: a `Bounds` field is self-validating, so it **removes** the paired `config::ordered("bounds", …)` line (~22 sites) rather than replacing `Validate`. Validated `Deserialize` via `#[serde(try_from)]`; dedicated `Copy` `BoundsError` (constructor has no field name). Migrates every `(f32,f32)` range field in `rlevo-evolution` (all `bounds`) and `rlevo-environments` (`action_clip`, `HealthyCheck` ranges, `clip_contact_cost`) and folds mountain-car's `min/max` scalar pairs into `Bounds` (breaking serde shape, alpha). Closes #99/#140/#196 and fixes three previously-unguarded sites (four local-search params, `HealthyCheck::state_range`, `clip_contact_cost`). |
| [0028](0028-tensor-batch-conversion-seam.md) | Make the host-only flat-`f32` **row-writer the required primitive** of `rlevo_core::TensorConvertible` (`row_shape()` + `write_host_row()`); `to_tensor` becomes a **derived** default (do not override) so the single-item and batch layouts share one definition; new free fn `stack_to_tensor<R, BR>` stages a whole batch as one host buffer and issues a **single** `Tensor::from_data` upload (the sole library-side `BR = R + 1` assert — stable Rust cannot express `R + 1` in types). Burn 0.21 audit: no built-in single-upload batcher exists (Burn's own `Batcher`/MNIST idiom is N transfers + `cat`), so the helper is additive, not redundant. First consumer: `sample_batch` in RL `memory.rs` — obs/actions/next-obs via `stack_to_tensor` (one upload each, was N); rewards stay rank-1 `[batch]` via the row-writer + one `from_floats` (never the helper, which would yield `[batch, 1]`). **Breaking** trait change migrating all ~27 impls in one PR (alpha); `from_tensor` untouched; inverse batch decode and `Kind` (`Int`/`Bool`) generalisation explicitly deferred. Implements #195 P2; P1 (the `unwrap_or_default` footgun) lands as a `docs/rules.md` convention + CI grep guard, not a sweep. |
| [0029](0029-host-rng-seeding-convention.md) | Codify the **host-RNG seeding convention** (already followed in `rlevo-evolution`) as a `docs/rules.md` §8 rule and bring the two lagging crates into line. Environments **own a persistent RNG seeded once at construction**; `reset()` advances the stream (successive episodes differ) instead of re-seeding from `config.seed` each call — fixing #104 across all genuinely-stochastic envs (locomotion, classic-control, `pixel_grid`, `dynamic_obstacles`). Deterministic replay moves to an inherent `reset_with_seed(seed)`; run-level reproducibility is still guaranteed by the construction seed. The **bandit family** is brought into compliance too: the problem instance (arm-means / phases / context) is sampled **once at construction** and preserved across resets (`NonStationaryBandit` restores its drift baseline), so reward noise varies per episode; `AdversarialBandit` is genuinely deterministic (pure-function reward) and drops its now-unused RNG field. The deterministic `_rng` grids are no-ops and untouched. Host-sampling stays `seed_stream`/`SeedStream`-only (`B::seed` + `Tensor::random` banned — process-wide RNG mutex races parallel tests). Unifies PPO/PPG RNG trait bounds onto `R: Rng + ?Sized` (the `&mut dyn Rng` was never object-safety-load-bearing). **Behavioural / semver-relevant** env `reset()` change (alpha); otherwise additive. Extends ADR 0016/0017. Implements #197; fixes #104. |
| [0030](0030-permutation-tensorgenome-and-population-nonempty-invariant.md) | Close the three #158 core type-safety gaps in `rlevo-evolution`. **`Permutation` implements `TensorGenome`** (`Tensor<B, 2, Int>`) now — a permutation is genuinely rectangular, unlike `Tree` — so `Population<B, Permutation>` type-checks and gains a `new_permutation` constructor (its per-row bijection invariant stays **unchecked**, mirroring `Binary`/`Integer` unchecked values; the ACO operators remain a `todo!()` stub). This advances the opt-in ADR 0022 anticipated, landing the *storage seam* ahead of the *operators*. **`Population` constructors** (`new_real`/`new_binary`/`new_integer`/`new_permutation`) change `-> Self` → `-> Result<Self, ConfigError>`, rejecting a zero-row/zero-column tensor via `config::nonzero("Population", …)` so the failure names `Population` instead of surfacing as an opaque downstream operator panic (ADR 0026 convention; breaking but no external consumers yet, alpha). The **`TensorGenome::Tensor` GAT gains `+ Send + Sync`** (was `Clone + Debug` only) so `Population<B, K>: Send + Sync` is provable under the `Strategy: Send + Sync` contract — a straight bug fix under ADR 0022. Extends ADR 0022; reuses ADR 0026. Fixes #158. |
| [0031](0031-probability-rate-newtypes.md) | Add two validated `f32` newtypes in `rlevo-core` — `probability::Probability` (invariant `0.0 <= p <= 1.0`) and `rate::NonNegativeRate` (invariant `is_finite() && r >= 0.0`) — mirroring the `Bounds` shape (ADR 0027): `new` (panicking literal) / `try_new` (`Result`), dedicated `Copy` error, validated `#[serde(try_from)]`. Threads them into the `rlevo-evolution` operator signatures (`blx_alpha` α, `uniform_crossover`/`bit_flip_mutation`/`uniform_reset` p, `gaussian_mutation` σ) and the `GaConfig`/`BinaryGaConfig`/`CgpConfig`/`GaCrossover`/`WritebackPolicy::Partial` fields, so a `NaN`/`Inf`/out-of-range rate is **unrepresentable** at the operator boundary rather than silently degenerating the Bernoulli mask (all-false → no-op / one-parent clone), inverting the CGP host comparison (mutate-everything), or poisoning the BLX-α offspring tensor. Self-validating fields **remove** their paired `config::in_range` line (ADR 0027 §3) and delete the memetic hot-path `debug_assert!`. Resolves #144; the 🔴 non-finite *parent-gene* propagation guard (crossover §1.3) and the out-of-scope `arch_mutation_rate`/`gep` rates are deferred to follow-ups. |
| [0032](0032-neat-opaque-id-newtypes.md) | Convert the three NEAT id aliases (`type NodeId = u64` / `InnovationId` / `SpeciesId`) into **opaque newtypes** over `u64`, defined in place (`topology.rs` / `species.rs`), so id-confusion — passing a `NodeId` where an `InnovationId` is expected, the 🔴 gap all three file reviews flagged — becomes a compile error. Reuses the opaque half of the ADR 0027/0031 shape (private field, `const new`/`get`, `pub(crate) succ` for the registry/speciation counters) **minus validation**: an id has no invariant (infallible `new`, no `try_new`/error) and no NEAT type is serialized (no serde, no schema bump). Derives `Eq + Hash` (HashMap/HashSet keys) and `Ord` (the innovation-sorted `connections` invariant, `BTreeMap<NodeId,_>`, ascending-id `sort_*`). Contained: production `neat.rs` works via the derives (only `next_species_id` init wrapped); no downstream-crate, example, or NAS/weight-only impact. Resolves #142. |
| [0033](0033-share-splitmix64-mixer-across-core-and-evolution.md) | Dedupe the byte-identical `splitmix64` mixer — promote it to a single `pub const fn rlevo_core::util::seed::splitmix64` (with a **frozen-reference stability contract** and a golden-value pin test), delete the copy in `rlevo-evolution/src/rng.rs`, and import the core one. **Partially supersedes ADR 0004 §6** (which chose to keep the local copy): for a frozen algorithm whose output must never change, one source of truth guarded by a pin test is strictly safer than two copies that can silently drift. Pure refactor — byte-identical body means **no runtime-output change**; the two seed-derivation *schemes* (`SeedStream`'s `(env, trial)` fan-out vs `seed_stream`'s `(base, generation, purpose)`) stay independent, sharing only the mixer. Same PR lands §7.1 (exhaustive all-pairs `SeedPurpose` distinctness + constant-distinctness tests) and §8.1 (documents that `SeedPurpose::Other` is a shared catch-all whose cross-strategy isolation relies on a fresh `base`/`generation`, and that its constant coincides with φ64). Resolves #161. |
| [0034](0034-fitness-hygiene-chokepoint-convention.md) | One fitness-hygiene primitive in `rlevo-evolution` (`sanitize_fitness` scalar + `sanitize_fitness_tensor` device sibling) applied at the four *driver chokepoints* — `EvolutionaryHarness::step` (on the **canonical** post-`sense` tensor, so one insert covers every `Strategy<B>`), `NeatStrategy::tell`, `ArchNasStrategy::tell`, and the coevolution coupled-fitness path — backed by the per-site "sanitize-then-`total_cmp`" `rules.md` §3 convention as the correctness floor. Rule: `NaN → −∞` (worst under maximise, preserved from ADR 0023; Rust's positive `NaN` else `total_cmp`-ranks as an immortal champion), `+∞ → f32::MAX` (ranks top but **finite**, so it can't blow a mean/variance/reward), `−∞`/finite pass through. `StrategyMetrics` means over finite members and reports `broken_count`. Closes only the **fitness-hygiene** class of #131–#134 (the class-2 ordering-direction and class-3 parameter-numerics/config bugs split to follow-ups); generalizes the per-model #129 (EDA)/#130 (GEP) precedents. Extends ADR 0023 (not superseded); class-3 guards belong at the ADR 0026 `Validate` boundary. `CanonicalFitness<B>` newtype named as the fast-follow if a bypass regression recurs. |
| [0035](0035-coupled-fitness-sense-and-coevolution-canonicalization.md) | Extend **ADR 0023 to the co-evolution coupled path**: add a **required, no-default `CoupledFitness::sense()`** (mirroring `BatchFitnessFn::sense`) so `evaluate_coupled` returns **natural** fitness and the co-evolutionary algorithm `step` becomes the sole **canonicalise-then-sanitize** chokepoint (negate iff `Minimize`, then `sanitize_fitness_tensor` — ordering load-bearing, per ADR 0034). `CoEAMetrics` display fields (`best/mean_fitness_{a,b}`) now report in the objective's **natural** sense (parity with `StrategyMetrics`), and a new **`binding_fitness`** field carries the canonical `min(best_a,best_b)` harness reward. `HallOfFameFitness` canonicalises champion selection **unconditionally** (even at blend `0`) before the sense-blind `HallOfFame::update` argmax, and its mutex is narrowed to an archive snapshot + final update. `CooperativeState` caches its `dims_b` complement (state-caching, not a `DimSplit` newtype — the `Validate` chokepoint already enforces the split). Enforces a **uniform-common-direction** constraint on `sense()`; asymmetric zero-sum objectives are out of scope for v1 (pre-negate + `Maximize`). Extends ADR 0023, reuses ADR 0034/0026. Resolves #160. |
| [0036](0036-adopt-proptest-for-property-tests.md) | Adopt **`proptest`** as a workspace-declared, **`rlevo-evolution`-only dev-dependency** for property/invariant tests, complementing (not replacing) the seeded-`StdRng`/`seed_stream` example convention. Declared `{ version = "1", default-features = false, features = ["std", "bit-set"] }` — defaults stripped to drop `fork`/`timeout` (`rusty-fork`/`tempfile`/`wait-timeout`), since per-case forking would re-init the Burn backend in a subprocess. **HARD RULE (ADR 0029):** proptest generates **host config only** (`λ`, `D`, structural sizes, `seed: u64`); the test body drives all algorithm RNG through `seed_stream(seed, generation, SeedPurpose::_)` — `B::seed` + `Tensor::random` stays banned, proptest's PRNG never touches Burn, and `fork`-off keeps cases sequential (no new race against the process-wide RNG mutex). proptest for **input-space** invariants (roundtrip, shape/length, monotonicity, no-panic/no-NaN across the valid domain, `Validate` boundaries); seeded examples for **specific-scenario/known-answer** cases (don't rewrite passing tests). Strategies bounded to the valid domain (`Bounds`/`Probability`/`NonNegativeRate`, `Validate` ranges); assertions thread-count-invariant (GEP rayon) or pin the pool. CI-cost policy: 16 cases + capped `max_shrink_iters` for backend-heavy props, 64–128 for cheap structural ones (no config file — per-`proptest!` `ProptestConfig` / `PROPTEST_*`). **Commit `proptest-regressions/`** as a durable replay guard (reversible). Adversarial findings are filed issues, not silenced properties. Builds on ADR 0029; reuses ADR 0026/0027/0031/0034. Resolves #239. |
| [0037](0037-external-force-lifetime-and-substep-actuation.md) | Fix the #98 force-accumulation defect: rapier3d 0.32 `add_force`/`add_torque` are additive and the pipeline never clears the accumulator (the "auto-cleared each step" claim is false). Enforce a **one-integration-step force lifetime** inside each backend's step primitive — `Rapier3DWorld::step_once` now calls a private `reset_external_forces` (`reset_forces`/`reset_torques`, `wake_up = false`) **after** `pipeline.step`; the box2d half (`RapierWorld::step`) does the same under the shared contract (parallel engineer). Add `Rapier3DWorld::step_actuated(apply)` that re-invokes a control closure before **each** of the `frame_skip` substeps (MuJoCo `ctrl`-held-across-substeps): the four rapier3d locomotion envs (InvertedPendulum, InvertedDoublePendulum, Reacher, Swimmer) precompute control, capture `Copy` handles + control into a world-only closure (no `self`/`RefCell`), and re-apply it every substep; Swimmer's world now uses `config.frame_skip` (was a hand-rolled loop) and its `apply_drag` becomes a free `apply_drag_to` recomputing drag per substep from live velocity. Motor-driven joints (bipedal walker) and snapshot/restore are unaffected. Swimmer `gear [5,5]`/drag frozen (canonical `[150,150]` re-tune deferred). Regression tests assert stationary per-step Δv under a constant action. Resolves the force-accumulation scope of #98; RNG-reseed / finiteness / terminal-guard / config-validation scopes split to follow-ups. |
| [0038](0038-continuous-action-components-const.md) | Fix #100 (`ContinuousAction::random()` panics for multi-component rank-1 actions) by adding a **required, no-default** `const COMPONENTS: usize` to `ContinuousAction` — defined as the length of `as_slice()`/`from_slice()`, explicitly not `Action::RANK` (tensor rank) and not `shape().iter().product()` (fails the trait's own Convention-B test type, `ContinuousActionTest`, whose `shape() == [1,1,1]` products to `1` for 3 real components). The default `random()` now loops `0..Self::COMPONENTS`, correcting `BipedalWalkerAction`/`LunarLanderContinuousAction`'s symmetric `[-1,1)` sampling for free; `CarRacingAction` keeps an asymmetric-bound override (Gymnasium gas/brake `∈ [0,1]`). A `COMPONENTS == as_slice().len()` contract test, added across all 11 impls, caught and fixed a pre-existing `CarRacingAction::as_slice()` truncation bug. Breaking change to all 11 impls (alpha, atomic). Explicitly defers `BoundedAction<R>::low()/high()` (`[f32; R]`, same rank-vs-component gap, blocked on unstable `generic_const_exprs`) to a follow-up ADR, naming the concrete DDPG/TD3/SAC `act`/`act_with` call sites and SAC's `-(A::RANK as f32)` target-entropy default that already rest on the coincidental `RANK == COMPONENTS == 1` of every existing `BoundedAction` impl. |
| [0039](0039-box2d-states-own-markov-dofs.md) | Resolve #117 (box2d `State`s alias external Rapier handles instead of owning Markov state). **Step 1 ships now:** encapsulate the three box2d states (`pub(crate)` fields + `#[must_use]` accessors — deliberately **no `try_new`**, since construction is in-module incremental with `::invalid()` handle placeholders and there is no public/serde path, so `pub(crate)` alone closes the rules §3 threat model), give each a **real `is_valid()`** (handle-validity + finiteness — including lunar's silently-skipped `prev_shaping`, + CarRacing's `tiles_visited <= total_tiles`), **wire it via `debug_assert!`** at the end of each `reset`/`step` (it had zero callers — cross-arch-safe NaN/handle check, not a magnitude bound), turn CarRacing `current_tile: usize` → `Option<usize>` (kills the `0` dual-sentinel), and delete the redundant `numel()` overrides. **Target (deferred):** box2d states own their Markov DOFs as values with handles moved onto the env core; **CarRacing re-models as `Environment<3,1,1>` = compact `State<1>` + `Observable<3>` pixel projection** (ADR 0019/0020) — the load-bearing constraint being that `observe`/`project` are `&self`-only, so BipedalWalker lidar and CarRacing pixels stay env-computed **cached sensor snapshots**, not pure functions of the owned DOFs. Refactor deferred to #255 (CarRacing) / #256 (LunarLander + BipedalWalker). Grounded in Gymnasium canonical state defs + Sutton & Barto §3.5. |
| [0040](0040-environment-config-error-and-terrain-output-contract.md) | Resolve #120 (BipedalWalker terrain generates invalid geometry). Add a **generic** `EnvironmentError::Config(#[from] ConfigError)` variant — kept environment-agnostic because `reset()` re-runs procedural-world construction, so a config-domain invariant can re-surface at reset, and `#[from]` gives `?` ergonomics without a stringly re-wrap. Define and enforce the **`TerrainGenerator` output contract** (≥2 points, x non-decreasing, first x < 0, world-space) — the trait stays infallible + object-safe (`Box<dyn TerrainGenerator>`), and `BipedalWalker::rebuild_world` is the **single chokepoint** that re-validates the polyline (rejecting the backwards/zero-length collider with `ConstraintKind::Custom`) before building; the silent `pts.len() < 2` early-return in `build_ground` is removed and `build`/`reset` propagate with `?`. Fix the three #120 High findings: (1) `HardcoreTerrain::generate` rewritten in **world space** with named constants, appending the terminal `[190, 0]` cap **only if** the last obstacle didn't overshoot (a wide boundary pit previously pushed x past 190, so the unconditional cap landed left of it → backwards cuboid); (2) both Rough/Hardcore hold a flat `SPAWN_PAD = 20` pad **spanning the spawn point** (walker at the pad middle, Gymnasium `TERRAIN_STARTPAD`) so obstacles begin ahead of the hull; (3) **Model B** — `RoughTerrain`/`HardcoreTerrain` fields go private with validating `new(...) -> Result<Self, ConfigError>` + `impl Validate` (roughness non-negative **and finite** — `0` legal, not strictly positive; step positive+finite; `pit + stump <= 10`), killing the `random_range(-roughness..=roughness)` empty-range panic on deserialized/struct-literal config. Reuses ADR 0026 (`Validate`/`ConfigError`); follows ADR 0039's make-invalid-states-unrepresentable posture. Regression: 512-seed monotonicity sweep. |
| [0041](0041-rapier3d-joint-actuation-and-contact-wrench-semantics.md) | Resolve #123 (rapier3d backend: unimplemented joint torque, under-specified contact force). **Implement `apply_joint_torque`** — signature changes to `-> Result<(), BackendError>` (new `thiserror` enum: `InvalidJointHandle`, `UnsupportedJoint(&'static str)`; zero prior callers) — **dispatched by joint kind**: `Impulse` (maximal coords) applies **equal-and-opposite** `±τ·â` to both bodies (solver enforces the hinge, zero net external torque); `Multibody` (reduced coords) torques the **child only** (`+τ·â`, solver supplies the parent reaction — the swimmer double-torquing convention), under the parent→child insertion invariant. `τ` is the generalized force on the single free **revolute** axis (MuJoCo `motor` analogue; gear stays the caller's job), one-substep lifetime (ADR 0037, re-applied via `step_actuated`); sign: `+τ` drives `body2` positively about `+â`. **Domain is revolute-only** (`Result`-rejects prismatic/spherical/fixed via `locked_axes`); prismatic/linear cart-force actuation deferred (envs keep using `bodies_mut`). Two rapier-0.32-under-glam discoveries: `GenericJoint::local_axis1()` is **anchor-contaminated** (`Pose3 * Vec3 = transform_point`) so `â` is extracted from `local_frame1.rotation · X` (unit) instead; and `contact_force` reported an **inverted vertical sign** (a resting ball yielded negative `wrench[2]`) — **discovered by the new pinning tests and fixed under this same issue** (#123): the returned wrench is the external contact force-torque acting **ON the queried body**, so a resting ball now reports `wrench[2] ≈ +m·g`. **`contact_force` math unchanged**, docs rewritten + tests added: instantaneous **last-substep** `impulse/substep_dt` (frame-skip-invariant, Gymnasium read-after-last-substep parity), **force-first** `[f(3), τ(3)]` layout (deliberate deviation from `cfrc_ext`'s `[τ(3), f(3)]` — false "matches cfrc_ext" claim removed), **body-CoM** torque reference (vs subtree-CoM — Ant/Humanoid `contact_cost` caveat), contact-manifold-only content, same-parent pairs cleared (`narrow_phase.rs:841`). Skeleton builders set `contacts_enabled(false)` on jointed neighbours (MuJoCo parent–child filter parity; applied to the four env builders by a concurrent change). Extends ADR 0037. |
| [0042](0042-snapshotbase-carries-optional-metadata.md) | Resolve #128 (`LunarLanderSnapshot` duplicates `LocomotionSnapshot` and mis-documents its metadata). `SnapshotBase<R, ObservationType, RewardType>` gains a `pub metadata: Option<SnapshotMetadata>` field and a fluent `#[must_use] with_metadata(self, SnapshotMetadata) -> Self` builder, and overrides `Snapshot::metadata()` to return it — closing the gap where core declared `SnapshotMetadata` + a `metadata()` seam but its own default `Snapshot` carrier could not express either. Both bespoke `impl Snapshot<1>` types collapse to local type aliases (`pub type LocomotionSnapshot<O> = SnapshotBase<1, O, ScalarReward>;`, `pub type LunarLanderSnapshot = SnapshotBase<1, LunarLanderObservation, ScalarReward>;`), dropping in-tree direct `Snapshot` impls from 2 to **0**. **The decider is `TimeLimit`**: `wrappers/time_limit.rs` binds its inner env's `SnapshotType = SnapshotBase<D, Obs, Rew>` and mutates `.status` in place, so a bespoke `SnapshotType` forfeits `TimeLimit` composition — a wart already admitted in `lunar_lander/snapshot.rs`'s doc comment. This unblocks `TimeLimit` for all six previously-locked-out environments (4 locomotion + 2 lunar lander) and every future `SnapshotBase`-bound wrapper. Rejects the naive shared alias `LunarLanderSnapshot = LocomotionSnapshot<LunarLanderObservation>` on verified evidence: `box2d` and `locomotion` are orthogonal Cargo features, so it fails E0433 under `--no-default-features --features box2d` — a class of bug no existing CI job could catch, so a two-step `cargo check` feature-orthogonality guard is added to `crate-tests.yml` (a full `cargo hack --feature-powerset` job is deliberately deferred to its own issue). Also rejects promoting a separate `MetadataSnapshot<R,O,Rw>` into core (institutionalizes the exception, does not fix `TimeLimit`) and an ungated shared type inside `rlevo-environments` (leaves core's contract unfulfilled, does not fix `TimeLimit`, inverts the dependency story). **Breaking at alpha** (5 struct-literal sites gain `metadata: None`; ~20 constructor call sites move metadata to a `.with_metadata()` tail; no `#[deprecated]` shim — the type *names* survive as aliases, but a constructor cannot be deprecated-and-retained on a now-foreign type, E0116); precedented by ADRs 0028/0030/0038. `SnapshotMetadata` still has no production consumer (`BenchStep` drops it, `EpisodeRecord` has no metadata field) and no serde derives are added, so there is no record `FORMAT_VERSION` bump. Narrows ADR 0019's "`SnapshotBase` byte-unchanged" statement, which scoped only that ADR's own change. Amends `docs/rules.md §10`. |
| [0043](0043-grid-observation-contract.md) | Resolve #109 (`MemoryEnv` is reactively solvable; `GoToDoorEnv`'s mission never reaches the policy). Records **Invariant M** — the egocentric view must never contain the cue cell, for any decision-region cell/facing — and makes `MemoryEnv` size-configurable on the canonical layout with `Validate` rejecting `size < 11`; the bound is *derived* (agent view reach 6, cue at `x=1`, fork column `size-2` ⇒ `size >= 11`) from rlevo having **no occlusion** anywhere in the grid family (`egocentric_view` reads every cell unmasked), so rlevo ships a strict subset of canonical Minigrid configs (S11/S13 only) and adding occlusion later would relax the bound to 7 — a documented two-way door. `GoToDoorEnv` gets a bespoke `GoToDoorObservation` (`[7,7,4]`, channel 3 broadcasts the mission color as the **same ordinal encoding** `Entity::color_u8` already uses in channel 1, not a one-hot, so a network can learn mission/door equality); rank stays 3 so `Environment<3,3,1>` and the `SnapshotBase`-alias convention (ADR 0042) are unchanged. Rejects widening the shared `GridObservation` (breaks all 12 envs' shape), routing the mission through `SnapshotMetadata` (ADR 0042 — never reaches a policy), and an out-of-band `env.mission()` accessor (no rollout loop threads it). Records, but explicitly defers, the crate-wide occlusion gap as a stated non-decision (Invariant M is phrased to survive it). Both envs were also violating ADR 0029 (re-seeding `self._rng` from `config.seed` inside `reset()`, previously harmless only because the RNG was never read); both lines are deleted and both envs gain `reset_with_seed`, and `MemoryConfig::swap_fork` / `GoToDoorConfig::target_color` are removed as config fields that pinned a quantity the environment is supposed to sample. |
