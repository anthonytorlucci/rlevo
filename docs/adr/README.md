# Architectural Decision Records

Immutable records of the architectural decisions behind `rlevo`. Once accepted,
an ADR is not edited — a later decision supersedes it, and the superseded record
is annotated rather than deleted. Read these for the *why* behind the crate
boundaries and trait design before proposing structural changes.

When you make an architectural decision, add a new numbered file here following
the existing format (`Status` / `Context` / `Decision` / `Consequences` /
`Alternatives considered` / `References`).

> Drafts and in-flight proposals live in the maintainer's working notes; an ADR
> lands here only once accepted. The repo copy is canonical.

| # | Decision |
|---|----------|
| [0001](0001-keep-environments-and-benchmarks-separate.md) | Keep `rlevo-environments` and `rlevo-benchmarks` as separate crates; use a feature-gated `bench` adapter inside `rlevo-environments` instead of merging. Decides to keep `rlevo-environments` and `rlevo-benchmarks` as separate crates rather than merging them, preserving each crate's disjoint dependency cone (physics for environments, `rayon`/`tracing` for benchmarks). To make the boundary practical, it ships a feature-gated `bench` module in `rlevo-environments` providing a `BenchAdapter` and preset `Suite` factories, plus a `Result`-based `BenchEnv` API for truthful error semantics. |
| [0002](0002-collapse-evolution-traits-into-rlevo-evolution.md) | Collapses `rlevo-core::evolution`'s EA traits into `rlevo-evolution` and removes that crate dependency. Only `GenomeKind` had a single real consumer; `Fitness` and `MultiFitness` were dead, aspirational placeholders with no usage. The refactor moves `GenomeKind` into `rlevo-evolution`, deletes the two dead traits, and removes the `rlevo-core` dep edge, leaving core as RL-only and evolution self-contained. RL/EA composition now happens at the `rlevo-hybrid` layer. |
| [0003](0003-collapse-rl-modules-into-rlevo-reinforcement-learning.md) | Moves three RL-specific modules (`memory`, `experience`, `metrics`) out of `rlevo-core` into `rlevo-reinforcement-learning`, since all are consumed only there — premature centralization by the ADR 0002 test. It also folds the empty `rlevo-utils` crate (a single orphan `combinations()` function) into `rlevo-core::util` and deletes the crate, shrinking the workspace from eight crates to seven. The move uses `git mv` for history, rewrites import paths, drops the orphan `ringbuffer` dep, and relocates the integration test. Two non-decisions are recorded: `rlevo-reinforcement-learning` and `rlevo-environments` stay separate (zero runtime coupling), and `rlevo-hybrid` remains the EA↔RL bridge. Speculative zero-consumer trait shells stay in `rlevo-core` as roadmap markers rather than being deleted. |
| [0004](0004-move-bench-traits-into-rlevo-core.md) | Relocates the ~184-line benchmark trait surface (the `env`, `agent`, and `seed` modules) from `rlevo-benchmarks` into `rlevo-core`, alongside the `Metric`/`MetricsProvider` types. It also converts `BenchError` from stringly-typed errors to typed wrappers around `EnvironmentError`, and rewrites import sites in `rlevo-evolution` and `rlevo-environments` accordingly. The goal is a strict-DAG dependency graph with no dep-cone bloat in evolution, while backward-compat shim modules keep existing callers compiling. Alternatives like creating a separate `rlevo-bench-traits` crate were rejected. A later note supersedes the local splitmix64-mixer decision in favor of ADR 0033's shared mixer. |
| [0005](0005-examples-and-cross-crate-tests-in-umbrella.md) | Moved all examples and cross-crate integration tests into the umbrella `rlevo` crate, keeping only crate-local unit and single-crate integration tests in their owning subcrates. This eliminated dependency cycles caused by dev-dependency edges that mirrored prod-dependency edges, notably `rlevo-benchmarks` dev-depending on `rlevo-environments[bench]`. Combined with ADR 0004, it yields a strictly acyclic workspace and a single discovery point via `cargo run -p rlevo --example <name>`. Accepted costs include subcrates losing standalone runnability and a larger umbrella `Cargo.toml`. The ADR is now superseded by ADR 0012, which refines the examples split and canonicalizes the test-placement rule. |
| [0006](0006-leptos-first-visualisation-defer-bevy.md) | (superseded) Decided to build rlevo's visualisation layer as a **Leptos web client** served by an embedded `axum` server, and to **defer adopting Bevy** for 3D this milestone. Only the `locomotion` family genuinely needs 3D, so it gets a temporary 2D sagittal-plane skeleton projection instead. The decision favoured shareable URLs, a richer plot ecosystem, and lower author activation energy over Bevy's native 3D strength. It retained a transport-agnostic `rlevo-viz-core` so a future 3D/native viewer could reuse the core unchanged. Its conclusions were later replaced by ADR 0008 and 0013, though the "no Bevy" and production-crate-isolation principles carried forward. |
| [0007](0007-visualisation-crates-isolated-from-production-crates.md) | Keeps visualisation crates (`rlevo-viz-core` and `rlevo-viz-web`) out of the dependency graph of all production crates. `rlevo-viz-core` may depend only on `rlevo-core`, while `rlevo-viz-web` may additionally depend on `rlevo-environments` via feature-gated adapters; neither may be a prod or dev dependency of any production crate. The `Visualize` trait lives in `rlevo-viz-core`, environments opt in with adapters located in `rlevo-viz-web`, and algorithm crates stay visualisation-agnostic, emitting metrics through existing callback seams. This avoids bloated dependency cones, recompilation, and WASM contamination across the workspace. The ADR is now superseded, though its isolation principle persists in later ADRs. |
| [0008](0008-three-tier-visualisation-ratatui-live-static-report.md) | Adopts a three-tier visualisation architecture for `rlevo`, splitting visualisation into the library tier (`AsciiRenderable`), a live `ratatui` TUI, and a static-HTML Leptos report — superseding the single Leptos-server model of ADRs 0006/0007. The live tier wraps benchmark runs in a terminal dashboard (feature-gated in `rlevo-benchmarks`), while the report tier renders post-run playback from a recorded `EpisodeRecord` file into one self-contained HTML. A core principle is that no production crate depends on viz crates; all viz lives behind features in the leaf `rlevo-benchmarks` crate. The ADR is **superseded by 0013**, which collapses the tiers to a live metrics TUI plus post-run records/report, removes the live env panel, and demotes `AsciiRenderable` to an optional debug helper. The `EpisodeRecord` seam and production-crate isolation rules remain in force. |
| [0009](0009-move-styled-render-into-rlevo-core.md) | Moves the styled-output render surface (`StyledFrame`, `StyledLine`, `StyledSpan`, `SpanStyle`, `Color`, `Modifier`, the `palette` module, and `AsciiRenderable`/`AsciiRenderer`) from `rlevo-environments::render` into `rlevo-core::render`. This was driven by a Cargo cyclic-dependency failure: `rlevo-benchmarks` (for the live ratatui TUI) needed the `AsciiRenderable` bound and `StyledFrame`, but `rlevo-environments` already optionally depends on `rlevo-benchmarks`. Since the trait and types are structural and env-agnostic, they pass the ADR 0004 "shared vocabulary" test for living in core. `rlevo-environments::render` is preserved as a pure re-export shim so all per-env impls compile unchanged. Consequence: the cycle is eliminated, the workspace stays a strict DAG with viz deps confined to `rlevo-benchmarks`, and the `StyledFrame → ratatui` conversion ships as free functions instead of a `From` impl due to orphan rules. |
| [0010](0010-unify-on-parking-lot-across-viz-stack.md) | Unifies the visualisation stack on `parking_lot::Mutex` after a repair pass found ~10 of 17 `rlevo/examples/viz/` examples silently failing to compile (they were `required-features`-gated and never built/tested). The root cause was a lock-type split: record-sink producers use `Arc<parking_lot::Mutex>`, while the `SharedPopulationObserver` alias uses `std::sync::Mutex`, forcing one example to import both types. The ADR redefines the observer alias over `parking_lot::Mutex`, removes the dual-import smell, converges the remaining sphere examples onto the same type, and drops `.lock().unwrap()` calls. It accepts the cost of a public API change and loss of lock poisoning, since observer state is non-critical telemetry. An example build+clippy CI job is tracked separately as the higher-leverage companion fix. |
| [0011](0011-lift-construction-off-environment-trait.md) | Removes the `new(render: bool)` constructor from the behaviour-focused `Environment` trait, which previously forced viz decorators like `RecordingTap` and `TuiEnvTap` to implement degenerate, silent-failure stubs. Construction is lifted into a standalone `ConstructableEnv` trait in `rlevo-core` (not a supertrait of `Environment`), keeping behaviour and construction concerns decoupled. All ~31 concrete env `new` bodies in `rlevo-environments` moved into `impl ConstructableEnv` blocks, and the `NullSink`/dead-channel stubs in `rlevo-benchmarks` were deleted. This aligns `Environment` with the clean `BenchEnv` shape and reinforces ADR 0007 (rendering stays off the production env surface). |
| [0012](0012-split-heavy-examples-into-rlevo-examples.md) | Creates a new workspace crate, `rlevo-examples`, to host application-tier examples — any that import `rlevo-benchmarks` or the viz/record/report feature stack (which pull in server and dependency-heavy deps like ratatui and axum). The umbrella `crates/rlevo/` keeps only lightweight examples that import exclusively from the five library sub-crates, and its `[dev-dependencies]` block is pruned accordingly. It also codifies the canonical three-tier test placement rule: in-source unit tests, per-crate `tests/` for single-crate integration tests, and flat `crates/rlevo/tests/` for cross-crate tests. `rlevo-examples` is added to workspace members but not default-members, so builds opt in via `cargo run -p rlevo-examples`. The main breaking change is that viz example commands change from `-p rlevo` to `-p rlevo-examples`. |
| [0013](0013-metrics-only-live-tui.md) | Reframes rlevo's visualisation from three tiers into two products, superseding ADR 0008: a live metrics-only `ratatui` TUI and a post-run `EpisodeRecord`-driven HTML report. The live TUI drops its env panel entirely, answering only "is it learning?" via learning curves, while the report answers "what did it learn?" with publication-quality per-family SVG/canvas playback from structured state. `AsciiRenderable` is demoted from a mandatory library-wide invariant to an optional debug-helper trait, since neither product consumes it. ADR 0008's `EpisodeRecord` seam and the rule that production crates never depend on viz (confined to `rlevo-benchmarks`) are preserved. Reversal criteria keep a cheap opt-in live env panel available if a mid-run debugging need arises. |
| [0014](0014-record-schema-v6-single-agent-richness-and-provenance.md) | Bumps the on-disk record format to `FORMAT_VERSION = 6`, extending (not superseding) ADR 0013. It fixes three gaps found by surveying RLlib, CleanRL, SB3, Tianshou, Gymnasium, and reproducibility literature: thin metric coverage, untyped/incomplete provenance, and no eval-vs-training distinction. The decision lands single-agent RL metric richness (e.g. `explained_variance`, per-iteration returns, DQN/SAC losses), typed provenance fields on `RunManifest`, an `EpisodeKind` train/eval tag, an `EpisodeRecordHeader`/wall-clock seam, and a learner-checkpoint reference seam. Multi-agent, variable-topology neuroevolution, and Bayesian-network records are deliberately deferred to future additive bumps. All changes follow binding invariants: additive-only `Option`/non-exhaustive enum fields, `BTreeMap`, typed fields over the string map, and mirrored consts/wire types enforced at compile time. Accepted cost: v5 files no longer decode under v6, and producer wiring for the new metrics is deferred. |
| [0015](0015-shared-typed-metric-registry-crate.md) | Introduces a new leaf crate `rlevo-metrics-registry` to solve two problems: (1) the canonical metric list was duplicated between `rlevo-benchmarks` and the WASM report client with no drift guard, causing silent demotion of new metrics; (2) the flat string list couldn't carry semantics, forcing the client to maintain three hardcoded shadow taxonomies for grouping, cadence, and titles. The solution extracts a `#![no_std]`, zero-dependency crate containing a typed `MetricDescriptor` table with `MetricKind` (Rl/Eo/Shared), `Cadence`, title, and unit. Both crates now depend on this single source of truth—the benchmarks crate becomes a thin re-export, and the report client deletes its fork, deriving all UI logic from registry lookups. No `FORMAT_VERSION` bump is needed; production crates remain isolated (they still emit only `tracing` field names). |
| [0016](0016-memetic-wrapper-and-local-search-seam.md) | Introduces a **memetic wrapper** and **host-side local-search seam** that enables hybridizing population-level evolutionary search with per-individual local refinement. The design adopts a `MemeticWrapper<B, S, L, F>` that implements `Strategy<B>` by composing an inner strategy `S` with a `LocalSearch<B>` trait and a fitness function `F` held behind a `parking_lot::Mutex<F>` (since `Strategy::tell` is `&self` but `F` requires `&mut self`). The wrapper drives refinement inside its `tell`, using a two-stream RNG scheme (`seed_stream` with `SeedPurpose::LocalSearch` and `SeedPurpose::Replacement`) so writeback policy (`Lamarckian`, `Baldwinian`, `Partial(p)`) is bit-identically overlay-compatible. Four searchers ship v1 (`HillClimbing`, `NelderMead`, `SimulatedAnnealing`, `RandomRestart`), all monotone-non-worsening over host `Vec<f32>` genomes. The `EvolutionaryHarness` stays memetic-unaware and holds a second `F` instance; this additive design preserves zero blast radius on the frozen public surface. Reversal criteria address the two-fitness-instance edge, per-refine re-eval cost, and calibration margins. |
| [0017](0017-probability-model-trait-and-eda-strategy.md) | Introduces a `ProbabilityModel<B>` trait and generic `EdaStrategy<B, M>` to enable Estimation-of-Distribution Algorithms (EDAs) as first-class `Strategy<B>` implementations in the evolutionary computation crate. The trait defines `fit` (incremental model fitting with optional prior state for algorithms like PBIL/cGA and future CMA-ES) and `sample` (host-side RNG sampling via `rand_distr`, not Burn's seedless PRNG kernels) with a dedicated `SeedPurpose::EdaSampling` stream for reproducibility. Four concrete univariate/chain models ship (UnivariateGaussian/UMDA, UnivariateBernoulli/PBIL, CompactGenetic/cGA, DependencyChain/MIMIC), while BOA is deferred. The design is purely additive—zero changes to the frozen `Strategy` surface, harness, or manifests—and reuses ADR 0016's NaN chokepoint and deterministic truncation selection. The `prev: Option<&State>` seam in `fit` deliberately supports incremental models and CMA-ES evolution-path state without a third `init_state` method, and `EdaStrategy` remains pure/lock-free because it consumes pre-evaluated populations in `tell` without interior mutability. |
| [0018](0018-boa-bayesian-network-and-concatenated-trap.md) | Introduces the **BOA (Bayesian Optimization Algorithm)** as a new Estimation-of-Distribution Algorithm (EDA) model in `rlevo-evolution`, extending the frozen `ProbabilityModel<B>` trait from ADR 0017 without any trait changes. BOA learns a **Bayesian network (DAG)** over genes each generation to capture higher-order dependencies that univariate (UMDA, PBIL, cGA) and first-order chain (MIMIC) models miss — critical for deceptive landscapes where per-gene statistics drive the population toward the deceptive basin. The decision ships `ConcatenatedTrap`, the canonical Deb & Goldberg deceptive trap function (summed over contiguous `block_size`-bit blocks), as the workspace's first discrete landscape and the discriminating benchmark. Structure learning uses **BIC scoring on raw MLE counts** (no smoothing), with a greedy lexicographic edge-addition search bounded by `max_parents = 3`; the BIC penalty ($\frac{1}{2} \cdot \ln N \cdot 2^{|Pa|}$) replaces the ad-hoc significance filter used in MIMIC with a principled complexity guard. Smoothing (`s = 1` default) applies only to CPT estimation for sampling, never to structure selection. `fit()` is non-incremental (relearns from scratch), matching canonical BOA. The convergence gate is empirically pinned at `pop = 2000`, selection ratio `0.3`, 60 generations — at this budget BOA solves trap-5 10/10 while UMDA and MIMIC stall (median cost $\ge 2$). The trait held without modification, validating the ADR 0017 surface for learned-structure models; accepted costs include host-side $O(D^2 \cdot N \cdot \kappa)$ fit time and the order-of-magnitude larger population requirement, both consistent with literature. |
| [0019](0019-observable-projection-trait.md) | Introduces the `Observable<OR>` trait to `rlevo-core::state` as a standalone, additive projection trait for modality-changing POMDPs where observation tensor order differs from state tensor order (e.g., compact RAM state → pixel observations). The trait decouples observation order `OR` from state order `SR` by providing an infallible `project(&self) -> Self::Observation` method, distinct from `State::observe()` which preserves order. `OR` is a const generic (not an associated const) to drive the `Observation<OR>` bound, and the trait deliberately avoids a blanket impl over `State` to prevent coherence lock-in and method ambiguity. The `Environment` contract already supports `R != SR`; this trait simply gives the projection a typed, tested home. A cross-crate test validates `Environment<2, 1, 1>` end-to-end, and real modality-changing environments will consume the seam. |
| [0020](0020-synthetic-pixel-over-grid-env.md) | Introduces the first real consumer of the `Observable<OR>` trait (from ADR 0019): a synthetic **pixel-over-grid** navigation environment implemented as `Environment<3, 1, 1>` in `crates/rlevo-environments/src/pixel_grid.rs`. The environment uses a rank-3 RGB observation (`[20, 20, 3]`) projected from a rank-1 latent state (`[2]`), exercising the exact `R != SR` modality-changing path that a future Atari/ALE backend would use (which is deferred behind a feature gate due to C++ FFI and ROM toolchain concerns). The `PixelGridState` implements both `State<1>` (trivial `observe()`) and `Observable<3>` (rendering `project()` → `PixelObservation`), with snapshots built exclusively via `project()`. The observation round-trips through `TensorConvertible<3, B>` so Burn policies can consume the projected image directly. Dynamics are 4-way Cartesian with wall clamping, terminated on goal with a decaying success reward, truncated at max steps. Fixed dimensions (`GRID_SIDE=5`, `CELL_PX=4`, `CHANNELS=3`) are used for v1; configurable parameters are deferred. The module lives as a standalone concept module (`pixel_grid.rs`), deliberately decoupled from the `grids/` family (which is egocentric, `R == SR`, and shares no core). |
| [0021](0021-cma-es-placement-and-self-contained-strategy.md) | Finalizes the placement and design of CMA-ES and CMSA-ES as two flat, self-contained `Strategy<B>` implementations in `crates/rlevo-evolution/src/algorithms/{cma_es,cmsa_es}.rs` rather than an `es_advanced/` submodule, following the project's convention that single-file algorithm families remain flat siblings. Both algorithms implement `Strategy<B>` directly and deliberately do **not** instantiate `ProbabilityModel<B>`, refining ADR 0017's anticipation that CMA-ES would reuse the trait; the authors argue that CMA-ES's identity lies in evolution-path machinery (CSA, rank-1/$\mu$ covariance updates) and CMSA-ES's in per-individual log-normal $\sigma$ self-adaptation—neither of which fits the `fit`/`sample` density-estimation seam, and forcing them through it would leak strategy state into the model. A hand-rolled cyclic Jacobi eigensolver handles host-side decomposition and sampling (no new dependency), `SeedPurpose::CmaSampling = 11` isolates the multivariate draw, and CMSA-ES reuses the classical ES $\sigma$ rule ($\tau = 1/\sqrt{2D}$). The change is purely additive with zero impact on the frozen API surface. |
| [0022](0022-tensorgenome-gat-population-storage.md) | Replaces `Population<B, K>`'s two `Option` tensor fields (one float, one int) guarded only by a runtime convention with a `TensorGenome` trait carrying a generic associated type `type Tensor<B: Backend>`, making `Population` store a single field `tensor: K::Tensor<B>`. This moves the kind→storage mapping into the type system: `Real` maps to `Tensor<B, 2>`, `Binary`/`Integer` to `Tensor<B, 2, Int>`, and non-rectangular kinds like `Tree` simply don't implement `TensorGenome`, so `Population<B, Tree>` fails to compile. The GAT is necessary because the storage type depends on both the kind and the backend; a `Clone + Debug` supertrait bound preserves `#[derive(Clone, Debug)]` on `Population`. Per-kind constructors (`new_real`, etc.) remain for ergonomics, while accessors become total (no `Option`, no panic paths). The change is a zero-blast-radius refactor of an internally unconsumed public type. |
| [0023](0023-objective-sense-and-maximize-convention.md) | Establishes a library-wide **maximise-native convention** (higher = better) across `rlevo`, replacing the previous split where RL maximised but the EA engine minimised. It introduces a zero-cost `ObjectiveSense { Minimize, Maximize }` enum in `rlevo-core::objective` with involutive `to_canonical`/`from_canonical` conversions, confining all cost↔canonical translation to a single chokepoint—the `EvolutionaryHarness`—which reads `BatchFitnessFn::sense()` (required, no default) to canonicalise on ingest and restore natural values on report. The ~30 strategy files, operators, shaping rules, and metrics in `rlevo-evolution` are flipped to operate purely in canonical maximise space (NEAT loses its exception, harness reward negation is deleted, `rlevo-hybrid` hand-negation removed). `Landscape` defaults to `Minimize`; higher-level problems must declare sense explicitly. The schema bumps to v7 with an `objective_sense` field on `RunManifest`. This scalar contract is deliberately the *K = 1 atom* of a future per-objective sense vector for MOO (resurrecting the `MultiFitness` seam ADR 0002 deleted), ensuring NSGA-II/SPEA2 can be added additively without rework. |
| [0024](0024-rlevo-test-support-dev-crate.md) | Introduces `rlevo-test-support`, a `publish = false` dev-only crate consumed via `[dev-dependencies]` by the `rlevo` test binary to eliminate duplication across the eight RL integration test files. Previously, each test independently copied a ~140-line `LinearEnv` fixture, the `Flex`/rayon determinism preamble (pinning threads, locking the backend, seeding weight init), hand-written acceptance assertions, and hard-coded random-policy baselines that drifted from reality. The new crate centralizes these into domain-agnostic modules: `env` (shared fixtures like `LinearEnv` and `cartpole_seeded`), `flex` (backend alias, per-binary `flex_guard` RAII, seeded device), `baseline` (seeded random-policy rollouts that *measure* rather than hard-code thresholds), `assert` (finite checks, improves-over-random with measured baseline, absolute floors, reproducibility), and `TrainOutcome` (the uniform return type for test `run()` functions). Two declarative macros (`rl_learning_test!`, `rl_reproducibility_test!`) generate the universal test scaffolding with a conventional `<algo>_<env>_<category>` naming scheme. The crate depends only on production crates it serves (today `rlevo-core` + `rlevo-environments`; `rlevo-evolution`/`rlevo-hybrid` as those fixtures land), maintains a strict one-way dev→prod boundary, and leaves algorithm-specific networks and behavioral tests in each test file. |
| [0025](0025-stateful-policy-rollout-contract.md) | Introduces the `StatefulPolicy` trait in `rlevo-hybrid` to generalize the rollout fitness seam from reactive (Markov) policies to recurrent policies with per-episode memory for POMDP environments. The trait defines `reset(&self, device) -> Hidden` and `act(&mut Hidden, obs, device) -> Action`, with a `ReactivePolicy` convenience trait (blanket-impl'd for `Hidden = ()`) preserving ergonomics for classic control. `RolloutFitness` is refactored to delegate scoring to an inner `ModuleEvalFn`, eliminating the duplicated unflatten/score loop (resolving #92) and replacing the stateless policy closure with a `device` parameter at construction (a pre-1.0 API break). This enables POMDP policies like the Santa Fe ant to be solved through the RL-coupled neuroevolution path rather than example-local workarounds, while keeping the maximise-native fitness convention from ADR 0023 and requiring no `rlevo-core` changes. |
| [0026](0026-shared-config-validation-convention.md) | Establishes a shared configuration-validation convention by introducing a `Validate` trait and `ConfigError` type in a new `rlevo_core::config` module, resolving a contradiction in `docs/rules.md` where config validation was ambiguously classified as both a programming error (permitting panics) and user-supplied runtime data (requiring `Result`). The `Validate` trait provides a `validate(&self) -> Result<(), ConfigError>` method that all public `*Config` and hyperparameter-bearing builder types must implement, with `ConfigError` capturing the config name, offending field, and a closed set of constraint kinds (`OutOfRange`, `NotPositive`, `NotOrdered`, `DegenerateInterval`, `Zero`, `Custom`). The decision draws a precise boundary: single-field builder setters (e.g., `with_capacity(0)`) may panic as localized fail-fast guards, but assembled configs—especially those arriving via `Deserialize` or struct literals without guarded setters—must return `ConfigError` through `validate()`, never panic. Adoption is incremental: this ADR lands only the trait, error type, one reference adopter (folding the existing panicking `CooperativeCoevolutionParams::validate()`), and the `rules.md` reconciliation; wiring the remaining ~85 configs is deferred to per-crate issues. |
| [0027](0027-bounds-newtype-for-closed-ranges.md) | Introduces a `Bounds` newtype in `rlevo_core::bounds` — a validated, inclusive `[lo, hi]` range over `f32` that is **valid by construction** (`lo <= hi`, rejecting `NaN` and inverted pairs while allowing degenerate points and one-sided infinities). It replaces ~22 scattered `(f32, f32)` tuples and `config::ordered` checks across `rlevo-evolution` and `rlevo-environments`, eliminating the `clamp` panic hazard (`min > max`/`NaN`) and the silent `lo > hi` collapse in `clamp_vec`. The type serializes via `TryFrom<(f32,f32)>`, so deserialized ranges are validated on load, and its `clamp`/`clamp_slice` methods absorb the unsafe helpers. `Bounds` complements (not replaces) the ADR 0026 `Validate` convention: a `Bounds` field is self-validating, so the corresponding `ordered` line is removed from `validate()`, but cross-field checks remain. The migration is breaking for `MountainCarConfig`/`MountainCarContinuousConfig` (field shape and serialized form change), acceptable in alpha. |
| [0028](0028-tensor-batch-conversion-seam.md) | Introduces a host-row primitive on the `TensorConvertible` trait to enable single-upload batching in `rlevo-core`. Instead of calling `to_tensor` per element (which incurs N host→device round-trips before stacking), the decision makes `row_shape()` and `write_host_row(&mut Vec<f32>)` the required primitive, derives `to_tensor` from it so single-item and batch layouts share one definition, and adds a free function `stack_to_tensor` that flattens an entire batch on the host and issues a single `Tensor::from_data` upload. The inverse path (`from_host_row`/`from_tensor_batch`) is deferred since no consumer decodes batches. This breaking change migrates ~27 existing impls in one PR (each already produces a flat `f32` row), replaces the per-element `to_tensor` loop in `memory.rs::sample_batch` with one upload, keeps rewards rank-1 via direct concatenation, and declines generalising over tensor `Kind` or adding an additive second trait (Option A) because those would either re-derive Burn's existing N-transfer idiom or introduce a silent layout-drift hazard. |
| [0029](0029-host-rng-seeding-convention.md) | Establishes a workspace-wide **host-RNG seeding convention** to fix a bug where environments re-seeded their RNG on every `reset()`, causing bit-identical episode initialization. The rule: environments seed their RNG **once at construction** via `seed_stream`, then `reset()` draws from the persistent stream so successive episodes see independent noise—run-level reproducibility is preserved because a fixed construction seed yields a fixed reset sequence. Deterministic replay is an explicit opt-in via inherent `reset_with_seed(seed)` methods on concrete env types (not a trait method). Problem instances (e.g., bandit arm means, grid layouts) are sampled once at construction and preserved across resets; non-stationary envs restore their construction baseline on reset rather than re-sampling. All randomness is drawn on the **host** via `SeedStream` → `Vec` → `Tensor::from_data`; `B::seed` + `Tensor::random` is forbidden in production paths due to Burn's process-wide RNG mutex. RNG trait bounds unify to `rng: &mut R` with `R: rand::Rng + ?Sized` across `rlevo-reinforcement-learning`. The convention is codified as a rule in `docs/rules.md`; `rlevo-evolution` (the reference implementation) is unchanged. |
| [0030](0030-permutation-tensorgenome-and-population-nonempty-invariant.md) | Resolves three foundational type-safety gaps in `rlevo-evolution`'s `TensorGenome` and `Population` types. First, `Permutation` now implements `TensorGenome` with `Tensor<B, 2, Int>`, making `Population<B, Permutation>` constructible and aligning code with its documentation (previously the doc promised tensor storage that didn't exist). Second, all `Population` constructors (`new_real`, `new_binary`, `new_integer`, `new_permutation`) now return `Result<Self, ConfigError>` and validate non-empty tensors at construction using the ADR 0026 `config::nonzero` helper, rejecting zero rows or columns with a structured error instead of silent downstream panics. Third, the `TensorGenome::Tensor` GAT gains a `Send + Sync` bound (purely additive, already satisfied by all concrete impls), enabling `Population<B, K>: Send + Sync` in generic code to satisfy the crate-wide `Strategy: Send + Sync` contract. The `Permutation` constructor validates shape only, deferring the per-row bijection check to future ACO operators; this mirrors the unchecked element values in `Binary`/`Integer` genomes. |
| [0031](0031-probability-rate-newtypes.md) | Introduces two validated `Copy` newtypes in `rlevo-core`—`Probability` (invariant `0.0 ≤ p ≤ 1.0`) and `NonNegativeRate` (invariant `is_finite() && r ≥ 0.0`)—to eliminate silent `NaN`/`Inf` misbehavior in mutation and crossover operators. Previously, bare `f32` rate parameters (`p`, `alpha`, `mutation_rate`, `crossover_p`, `WritebackPolicy::Partial`) could silently degenerate operators: comparisons with `NaN` always yield `false` (all-false Bernoulli masks or inverted CGP logic), while `NaN`/`Inf` `alpha` poisoned BLX-α arithmetic. Validation existed only at config construction via `config::in_range` and release-stripped `debug_assert!` calls, leaving raw `ops/` functions unchecked. The newtypes thread validity directly into operator signatures (`blx_alpha`, `uniform_crossover`, `gaussian_mutation`, etc.), remove redundant `config::in_range` checks from configs (`BinaryGaConfig`, `CgpConfig`, `GaConfig`, `WritebackPolicy`), and deserialize safely via `#[serde(try_from)]`. Both mirror the `Bounds` pattern from ADR 0027 (const `new`, `try_new`, dedicated `Copy` errors, `TryFrom<f32>`). Deferred: per-element parent-gene finiteness and migration of `arch_nas`/`gep` configs. |
| [0032](0032-neat-opaque-id-newtypes.md) | Replaces bare `type` aliases (`NodeId`, `InnovationId`, `SpeciesId` = `u64`) with opaque `Copy` newtypes in their existing modules (`topology.rs` and `species.rs`), each exposing only an infallible `new(u64)`, a `get() -> u64` accessor, and a crate-internal `succ()` for counter allocation—no validation, no serde, no public field. The derives (`Clone`, `Copy`, `Debug`, `PartialEq`, `Eq`, `Hash`, `PartialOrd`, `Ord`) are driven by existing `HashMap`/`BTreeMap` key usage and sorted-iteration invariants. This makes id-space confusion a compile-time error without changing any public import paths or consumer code, resolving the type-safety gap where passing a `NodeId` for an `InnovationId` would silently misclassify genes during NEAT mutation/crossover. |
| [0033](0033-share-splitmix64-mixer-across-core-and-evolution.md) | Resolves a silent-drift risk by promoting the private `splitmix64` mixer in `rlevo-core/src/util/seed.rs` to a single `pub const fn` pinned by a golden-value test, then deleting the byte-identical copy in `rlevo-evolution/src/rng.rs` so both crates now import the same frozen reference implementation. The two seed-derivation schemes (`SeedStream`'s `(env, trial)` fan-out and `seed_stream`'s `(base, generation, purpose)` derivation) remain independent—only the inner mixer is shared—ensuring zero behavioral change while eliminating the possibility of the two copies diverging under future edits. |
| [0034](0034-fitness-hygiene-chokepoint-convention.md) | Establishes a single fitness-hygiene primitive in `rlevo-evolution` (`sanitize_fitness` / `sanitize_fitness_tensor`) that maps `NaN → −∞`, `+∞ → f32::MAX`, and passes `−∞`/finite values through, then applies it at the four driver chokepoints—`EvolutionaryHarness::step`, `NeatStrategy::tell`, `ArchNasStrategy::tell`, and the coevolution coupled-fitness path—so every `Strategy<B>` and neuroevolution run inherits the guarantee by default. The per-site "sanitize-then-`total_cmp`" convention remains the correctness floor for callers that bypass the harness, while `StrategyMetrics` now averages only finite members and reports a `broken_count` to surface poisoned populations without blanking the mean. |
| [0035](0035-coupled-fitness-sense-and-coevolution-canonicalization.md) | Extends the maximise-native convention (ADR 0023) to the co-evolution path by requiring `CoupledFitness::sense()` (no default) so `evaluate_coupled` returns **natural** fitness, then canonicalises once per generation inside the co-evolutionary `step` using the canonicalise-then-sanitize chokepoint (ADR 0034) — negating iff `Minimize` before `sanitize_fitness_tensor` to preserve "NaN = worst" semantics in maximise space. `CoEAMetrics` now reports natural `best_fitness`/`mean_fitness` for parity with single-population `StrategyMetrics`, while a new `binding_fitness` field holds the canonical `min(best_a, best_b)` used as the harness reward. The hall of fame canonicalises champion selection unconditionally before `HallOfFame::update`, whose argmax is sense-blind. A uniform-direction constraint applies (all populations share one `sense()`), with asymmetric zero-sum objectives deferred to v1. The change also narrows the `HallOfFameFitness` mutex to a cheap archive snapshot plus final `update`, and caches `dims_b` in `CooperativeState`. |
| [0036](0036-adopt-proptest-for-property-tests.md) | Adopts `proptest` as a workspace-declared, `rlevo-evolution`-only dev-dependency (version 1, default features disabled to avoid process forking that would re-initialize Burn backends) for property-based testing of input-space invariants—roundtrips, shape/length/monotonicity, finiteness/NaN-freedom, and `Validate` boundaries—while strictly preserving the ADR 0029 host-RNG boundary: `proptest` generates only host config (population size, dimensionality, a `seed: u64`), and all algorithm randomness flows through `seed_stream(seed, generation, SeedPurpose)` so `proptest`'s PRNG never touches Burn. Backend-heavy properties run 16 cases with capped shrink iterations; cheap structural properties run 64–128 cases. Shrunken counterexamples are committed to `proptest-regressions/` for durable replay. Seeded `StdRng` example tests remain for specific-scenario/known-answer assertions and are not rewritten as properties. |
| [0037](0037-external-force-lifetime-and-substep-actuation.md) | Fixes a critical physics-integration bug where external forces in both Rapier3D and Box2D backends were accumulating monotonically across environment steps instead of living for exactly one integration step. The root cause was that `RigidBody::add_force`/`add_torque` in Rapier 0.32 are additive and the pipeline never auto-clears the accumulator—contrary to widespread folklore—so every actuated environment (InvertedPendulum, InvertedDoublePendulum, Reacher, Swimmer) was integrating a growing sum of all historical forces. The decision enforces a "one-integration-step lifetime" contract inside each backend's step primitive: Rapier's `step_once` now calls `reset_forces`/`reset_torques` after `pipeline.step`, and a new `step_actuated` primitive advances `frame_skip` substeps while re-applying the same precomputed control before each substep (mirroring MuJoCo's `ctrl`-held-across-substeps semantics). Box2D receives an equivalent fix. This makes constant actions produce constant effective forces, eliminates Swimmer's divergence under monotone actions, and ensures the defect cannot recur in new environments since the primitive self-clears regardless of caller behavior. |
| [0038](0038-continuous-action-components-const.md) | Resolves issue #100 where `ContinuousAction::random()` panics for multi-component rank-1 actions by adding a required `const COMPONENTS: usize` associated constant to the `ContinuousAction` trait. The bug stemmed from conflating **rank** (number of tensor axes) with **component count** (total flattened scalars), which are equal only for single-component actions—every production `ContinuousAction` impl uses rank 1 (`Action<1>`) with `shape() = [C]` where `C` is the true component count (Convention A), while the in-trait test type uses rank = component count with `shape() = [1,1,1]` (Convention B). Neither `RANK` nor `shape().iter().product()` universally derives component count, so the decision mandates each implementor explicitly declare `COMPONENTS` (no default, to prevent silent re-inheritance of the bug), corrects the default `random()` to loop over `COMPONENTS`, and adds a contract test asserting `T::COMPONENTS == T::random().as_slice().len()`. This is a breaking change for all 11 existing impls (acceptable in alpha) and exposes the same rank-vs-component defect in `BoundedAction::low()/high()` (keyed on rank, not component count), which is deferred to a follow-up ADR. |
| [0039](0039-box2d-states-own-markov-dofs.md) | Addresses a structural Markov property violation in the three Box2D environments (`CarRacingState`, `LunarLanderState`, `BipedalWalkerState`) where state structs hold only Rapier arena handles into an environment-owned physics world rather than owning their Markov degrees of freedom (pose, twist, joint/wheel dynamics) as values. This makes cloned states shallow aliases that dangle after `reset()`, and the existing `is_valid()` methods are rubber stamps. The decision splits remediation into two phases: **Step 1** encapsulates fields as `pub(crate)` with read accessors, implements real `is_valid()` checks (handle validity, finiteness including the previously-unchecked `prev_shaping` in LunarLander, structural invariants like `tiles_visited ≤ total_tiles`), converts CarRacing's `current_tile` sentinel to `Option<usize>`, wires `debug_assert!(state.is_valid())` into `reset`/`step`, and removes redundant `numel()` overrides—all without moving handles or restructuring structs. **Steps 2–3** relocate handles onto the environment core so each state owns its canonical Markov DOFs as values (LunarLander: 8-dim pose+twist+contacts; BipedalWalker: hull+4 joints+contacts+lidar; CarRacing: hull+wheel dynamics+fuel+progress) and re-model CarRacing as `Environment<3,1,1>` with `Observable<3>` projecting the pixel frame, gaining a second physics-based `Observable` consumer after `PixelGridEnv`. World-projected sensors (lidar, pixels) remain env-cached and returned by `project()`/`observe()`, documented as a cache not a pure function. The `try_new` constructor deviation is explicitly recorded as intentional because incremental in-module construction with placeholder handles cannot satisfy a validating constructor, and no public/serde construction path exists to guard. |
| [0040](0040-environment-config-error-and-terrain-output-contract.md) | Resolves invalid geometry generation in the BipedalWalker terrain by introducing three coordinated changes: a generic `EnvironmentError::Config(ConfigError)` variant to surface configuration-domain failures during environment lifecycle operations (especially `reset`), a documented `TerrainGenerator` output contract (non-decreasing x, minimum two points, first point left of spawn, world-space coordinates) enforced at a single chokepoint in `rebuild_world`, and Model B validation (private fields with fallible `new()`, `Validate` impl) for `RoughTerrain`/`HardcoreTerrain` to make invalid config unrepresentable—eliminating panics from negative `roughness` in `random_range`, fixing the non-monotone x defect by generating in world space with a guarded terminal cap, and adding a flat `SPAWN_PAD=20` lead-in so the walker spawns on safe ground matching the Gymnasium reference. |
| [0041](0041-rapier3d-joint-actuation-and-contact-wrench-semantics.md) | Resolves three critical defects in the Rapier3D locomotion backend: it implements the previously-unimplemented `apply_joint_torque` with joint-kind-dispatched semantics (equal-and-opposite world-axis torques on both bodies for maximal-coordinate `Impulse` joints; child-only torque on the hinge axis for reduced-coordinate `Multibody` joints), replaces the broken `local_axis1()` axis extraction with a rotation-only computation to avoid glam-backend anchor contamination, and precisely specifies `contact_force` semantics—instantaneous last-substep force-on-body with force-first layout, CoM torque reference, and self-contact exclusion—backed by regression tests for sign (now corrected so a resting ball reports positive vertical force), frame-skip invariance, Newton's-third-law antisymmetry, and insertion-order robustness. A new `BackendError` rejects non-revolute joints rather than silently misactuating. The document also establishes a skeleton-builder convention of disabling jointed-neighbour contacts to prevent internal-contact pollution, and explicitly defers prismatic/cartesian actuation to a separate seam. |
| [0042](0042-snapshotbase-carries-optional-metadata.md) | Adds an optional `metadata: Option<SnapshotMetadata>` field and a fluent `.with_metadata()` builder to `SnapshotBase` in `rlevo-core`, then collapsing the two bespoke snapshot types—`LunarLanderSnapshot` and `LocomotionSnapshot<O>`—into local type aliases of `SnapshotBase`. This unblocks `TimeLimit` composition for six previously locked-out environments (four locomotion + two lunar lander) by ensuring all snapshots satisfy `TimeLimit`'s `SnapshotBase`-bound constraint, while also generalizing metadata support to any rank or reward type. The change is a breaking alpha alteration (struct literals and constructor call sites must add `metadata: None` or chain `.with_metadata()`), accepted per precedent (ADRs 0028/0030/0038) with no deprecation shim possible due to orphan rules; the steady-state size cost is stack-only (~48–56 bytes) and unmeasured, and `SnapshotMetadata` remains a debug-only affordance with no production consumer or serde changes. |
| [0043](0043-grid-observation-contract.md) | Fixes two grid environment bugs: `MemoryEnv` wasn't a true recall task (cue was constant, RNG unread, colors leaked answers) and `GoToDoorEnv`'s mission never reached the policy (`mission()` had zero callers). The decision establishes **Invariant M**—the egocentric view must geometrically hide the cue from the decision region—forcing `MemoryEnv` to be size-configurable with a `Validate` bound rejecting `size < 11` (a consequence of rlevo's no-occlusion view, relaxable to 7 when occlusion lands). `GoToDoorEnv` gets a bespoke `GoToDoorObservation` `[7,7,4]` where channel 3 broadcasts the mission color as an ordinal byte matching the existing entity encoding, enabling the network to learn equality between mission and perceived door colors. This sets the precedent: goal-conditioned envs surface goals via per-env observation types that fold the goal into the per-cell encoding, not side channels. The crate-wide occlusion gap (`see_through_walls` effectively always `true`) is explicitly recorded as a non-decision (issue #281), and both envs are corrected to follow ADR 0029's persistent-stream `reset_with_seed` pattern. |
| [0044](0044-post-terminal-step-is-an-error.md) | Establishes that calling `step()` after an episode has terminated is an error, not an absorbing transition: the call returns `Err(EnvironmentError::StepAfterEpisodeEnd)` carrying the terminal `EpisodeStatus` (`Terminated` or `Truncated`), making the mis-sequenced call explicit and reversible rather than silently appending zero-reward transitions to replay buffers. A crate-level, private-field `EpisodeGuard` (in `rlevo-environments`) provides `check()`/`record()`/`reset()` as a two-way-door state machine used as the first statement of every guarded `step()`, with wrappers that manufacture terminal status (e.g., `TimeLimit`) owning their own guard. `EpisodeStatus` is the single source of truth (no `done: bool`), the contract is normative in `Environment::step`'s rustdoc with an alpha migration note (tracked by #289), fixture environments are deliberately exempt, and conformance is checkable via a shared test helper—not enforced. The decision rejects absorbing-state re-emission as the default (it biases data silently), a template-method trait split (incompatible with wrapper pre/post patterns), stringly-typed errors, and payload-less error variants; it fixes the four `toy_text` defects and blackjack's `u8` overflow at the root, closes `TimeLimit`'s post-truncation hole, and surfaces latent test defects, while accepting that 44 environments remain non-conformant until a future `VecEnv` must own auto-reset itself. |
| [0045](0045-landscape-bounds-is-a-search-box.md) | Redefines `Landscape::bounds()` to return a **per-coordinate search box** (applied uniformly to every dimension) rather than the function's mathematical domain, because the EA configs in `rlevo-evolution` can only hold a single scalar `Bounds` pair applied per-coordinate via `clamp_vec`. The decision pins two testable obligations on every landscape: **O1 (reachability)** — the box must contain every certified global optimum on all coordinates (fixing Branin and Trefethen, which previously excluded optima), and **O2 (no spurious optimum)** — the box must contain no point with `f < f*`, discharged analytically in a code comment and regression-tested by a dense grid sweep. Two conforming box shapes are legitimate: a **square hull** of an asymmetric domain (Branin `(-5,15)`, Trefethen `(-6.5,6.5)`, Bukin6 `(-15,3)`) or a **cited reduced range** from an impractically large canonical domain (RosenbrockFlat `(-30,30)`, Six-Hump Camel `(-2,2)`). The true per-axis domain remains documentation-only; no `domain()` accessor or per-axis renderer is added, as both would introduce a second, colliding rectangle convention against `Landscape2DSnapshot`'s search-domain bounds. Consequences: affected landscapes are now solvable in principle, but results are **not directly comparable** to published baselines using literature rectangles; widening is guarded by O2; showcase mutation constants (absolute, not box-relative) were hand-retuned. No API changes; the missing chokepoint wiring landscape boxes to optimiser configs remains an open question for a future bounded-objective seam. |
| [0046](0046-slot-newtype-replaces-option-take-around-learn-step.md) | Introduces `Slot<M>` — a newtype wrapper around `Option<M>` — to eliminate the wide "module is temporarily nowhere" window that previously existed in all eight gradient-based RL agents (`dqn`, `c51`, `qrdqn`, `ddpg`, `td3`, `sac`, `ppo`, `ppg`). The old pattern used `Option::take()` at the start of a learn step, leaving the network field `None` for the entire forward/loss/backward/`from_grads` sequence; any panic in that window permanently bricked the agent. `Slot` closes this by providing only three operations: `new` (never empty at construction), `get(&self) -> &M` (the single read path, used for all forward/backward work), and `step_with` (the sole ownership transfer point, which moves the module into `Optimizer::step` and writes the result back). This shrinks the vulnerable window to exactly the irreducible call to `Optimizer::step` itself (which consumes the module by value, so a panic there still poisons the slot — a deliberate, documented trade-off). The change also fixes a latent multi-field bug in TD3/SAC where a panic stepping one critic destroyed the other, collapses 17 hand-copied `.expect()` calls into one in `Slot::get()`, and is behaviour-preserving (seeded A/B runs produced bit-identical output). |
| [0047](0047-sensor-relocates-emission-model-to-environment.md) | Relocates the POMDP emission model $O: S \times A \to \Pi(\Omega)$ from `State::observe()` to a new environment-side `Sensor<OR, AR, SR>` trait, resolving a category error where observation production was incorrectly attached to state points rather than the environment. The `Sensor` trait provides `observe(&self, action, next_state) -> Observation` and `observe_reset(&self, state) -> Observation`, with `&self` as the environment granting direct world/simulator access—eliminating the `&self`-only weakness that forced ADR 0039's cached-sensor hack (lidar raycasts, rendered frames stashed on state). This decouples observation rank `OR` from state rank `SR`, fixing rank inflation (e.g., CarRacing no longer must masquerade as `Environment<3,3,1>`), and removes the dual-impl redundancy of `State::observe` alongside `Observable::project`. `Observable<OR>` is demoted to an optional pure-projection helper that `Sensor` may delegate to (as `pixel_grid` does). The grid family is a documented exception: eleven environments share a state-pure projection via `Observable<3>` and a common `build_snapshot` chokepoint, while `GoToDoorEnv` implements `Sensor` directly for its goal-conditioned observation. `BeliefState` is repointed off `State::Observation` to its own associated `Observation` type with independent `OR`. The change is breaking (alpha) but atomic; env-holds-`Sensor` for swappable modalities is recorded as a two-way door. |
| [0048](0048-partial-episode-bootstrapping-in-gae.md) | Adopts **partial-episode bootstrapping (PEB)** per Pardo et al.'s Eq. 6 as the correct on-policy learning target for truncated episodes, deliberately diverging from CleanRL's default PPO. The core issue was that `compute_gae` incorrectly conflated `terminated` and `truncated` into a single `next_nonterminal` mask, zeroing the bootstrap on truncation the same way as termination — a simplification acknowledged upstream as pedagogical, not correct, and which Pardo et al. show causes qualitative failure on truncation-heavy tasks (especially at γ=0.99). The fix introduces **two explicit masks** inside `compute_gae`: a recursion-cut mask (`terminated || truncated`) and a separate delta bootstrap that uses `V(s_continuation)` on truncation. This requires changing `truncated: Vec<bool>` to `truncation_value: Vec<Option<f32>>` so a truncated step cannot exist without its bootstrap value, fixing an entangled off-by-one indexing bug in the same atomic change, deleting the redundant `last_done` parameter, and shifting the continuation-value computation into `record_step` at the agent. The change is breaking with no shim (three public APIs), moves all seeded baselines, and adds one value forward per truncation; residual GAE weighting bias at boundaries remains a known, deliberate non-decision per Doering et al. (2026). |
| [0049](0049-ppo-gaussian-log-std-is-bounded.md) | Bounds PPO's Gaussian policy head's state-independent `log_std` parameter to a clamped interval `[log_std_min, log_std_max]` (default `[-20, 2]`) on the policy-head config to prevent numerical underflow/overflow that corrupts training: the gradient of the log-probability with respect to `log_std` exerts sustained downward pressure (~ -1 per update) that, left unchecked, drives `log_std` below f32's representable range (σ → 0, NaN propagates) within normal training budgets, and a wide config interval can cause ratio overflow between old/new σ during policy evaluation. The decision includes validation that rejects an inverted interval, an absolute floor below -35 (guarding mode 1 underflow), and a span > 40 (guarding mode 2 ratio overflow); it adds telemetry (per-iteration min `log_std`, one-shot warning on first bind) because clamping alone turns a loud NaN crash into a silent degenerate policy pinned at the bound with no gradient path back — a critical difference from SAC where `log_std` is a per-step network output that retains gradient flow. The change is a deliberate deviation from reference PPO (which leaves `log_std` unbounded), justified by Andrychowicz et al. 2021's finding that the minimum σ "matters little if not set too large," and applies only to PPO (PPG is discrete-only in v1). |
| [0050](0050-replay-strategy-seam.md) | Introduces a new `replay` module in `rlevo-reinforcement-learning` that replaces the dead, unfinished `PrioritizedExperienceReplay` from `memory.rs` with a proper replay-strategy seam. The seam defines a single generic `Transition<O, P>` type (with `DiscreteTransition` and `ContinuousTransition` aliases) that erases the action payload to avoid `Clone + 'static` bounds, and a tensor-agnostic `ReplayStrategy<T>` trait with two separate implementations: `UniformReplay` (i.i.d. with-replacement draws, pinned to the exact draw order for reproducibility) and `PrioritizedReplay` (a fidelity-complete implementation of Schaul et al. 2016 proportional variant with stratified sampling, sum-tree, IS weights with max-normalization, `β` annealing, and a `Priority` newtype that makes `NaN` priorities unrepresentable). The design explicitly scopes prioritization to the value-based agents (DQN/C51/QR-DQN) per literature evidence that PER is harmful for DDPG/TD3/SAC, uses generation-stamped `TransitionId` handles to survive FIFO eviction, and retires `TrainingBatch` (zero consumers) in favor of agents staging their own tensors from `SampledBatch`. The breaking removal of the advertised-but-broken `PrioritizedExperienceReplay` is accepted at alpha with no deprecation shim. |
| [0051](0051-replay-kind-dispatch-and-validated-importance-exponent.md) | Corrects three implementation-mismatched decisions from ADR 0050: it replaces the trait-level `update_priorities` default with an exhaustive `match` dispatch via a closed `ReplayKind<T>` enum (preventing silent priority writeback drops), introduces a validated `ImportanceExponent` newtype for the $\beta$ parameter (enforcing finite `[0,1]` at construction with `new`/`try_new` and a `ONE` constant) instead of raw `f32`, and withdraws ADR 0050's accepted-cost mitigation of config-level endpoint validation after discovering that a zero-length anneal schedule produces `NaN` at evaluation time despite valid endpoints—fixing this with a three-layer guard (nonzero config validation, clamped schedule evaluation, and the newtype backstop). The `ReplayKind` enum keeps PER as a config-level opt-in (per ADR 0050) while accepting a closed strategy set and forwarding boilerplate; the `ImportanceExponent` change is a strict no-op on sampling arithmetic but converts silent release-build `NaN` poisoning into a loud construction-time panic. |
| [0052](0052-hostrow-supertrait-splits-layout-from-backend.md) | Splits the monolithic `TensorConvertible<R, B>` trait into a backend-independent `HostRow<R>` supertrait (host-side row layout: `row_shape`, `write_host_row`) and a slimmed device-facing `TensorConvertible<R, B>: HostRow<R>` (tensor conversion: `to_tensor`, `from_tensor`). The split moves the backend-independence contract from unenforced prose into the type system: `HostRow` has no `B` parameter, making a backend-specialised row writer **unrepresentable** and eliminating a silent divergent-bytes hazard where dual `TensorConvertible` bounds on `B` and `B::InnerBackend` forced six call sites to use arbitrary turbofish annotations. The ambiguity resolves via shared supertrait obligation (`O: HostRow<DO>` appears twice, identically) rather than relocation. Costs include a breaking change (35 impl blocks split across 6 crates), two impl blocks per type, and a documented one-rank-per-type invariant enforced by a loud compile error. `stack_to_tensor` relaxes to `T: HostRow<R>`, enabling staging without a decode impl, and `Observation<R>: HostRow<R>` becomes expressible (deferred). No performance change; purely a type-safety and ergonomics improvement. |
| [0053](0053-bounded-action-per-component-bounds.md) | Fixes a latent defect in the `BoundedAction` trait where `low()` and `high()` returned fixed-size arrays keyed on tensor rank `R` rather than component count `COMPONENTS`, making it impossible to express per-component bounds for multi-component rank-1 actions (which is every production continuous action in the workspace). The fix changes the return type to `&'static [f32]` slices, preserving the trait's generic arity so all existing `A: BoundedAction<DA>` bounds compile untouched, while migrating all three consuming agents (DDPG, TD3, SAC) from `A::RANK` to `A::COMPONENTS` loops and fixing DDPG/TD3's scalar target-action clip to use per-component `max_pair`/`min_pair` tensor clipping. The `COMPONENTS`-agreement invariant (`low().len() == high().len() == Self::COMPONENTS`) becomes a contract-tested rather than compile-time guarantee, and the five previously blocked multi-component environments (`BipedalWalker`, `CarRacing`, `LunarLanderContinuous`, `Reacher`, `Swimmer`) receive `BoundedAction` impls as the final step. Convention B (non-uniform rank shapes) is retained with `shape()` explicitly documented as never a source of component count. |
| [0054](0054-policy-head-construction-is-fallible.md) | Closes a validation bypass in policy-head construction by replacing the infallible `init()` with a single fallible `try_init()` that calls `validate()` as its first statement, ensuring no invalid config can reach head construction on any Burn backend. The change was driven by a backend-divergent failure mode where inverted `log_std_min > log_std_max` bounds silently collapse gradients on `Autodiff<Flex>` but panic on raw `Flex`, making the bug undetectable in tests depending on the backend used. The decision applies uniformly to all four head configs (two Gaussian, two categorical), replaces the Gaussian heads' raw `log_std_min`/`log_std_max` pair with a single `Bounds` field (making inverted ranges unrepresentable), and deliberately names the method `try_init` rather than changing `init`'s return type to avoid violating Burn's ecosystem convention that `init` is always infallible. The trade-off is a permanent ergonomic tax: every construction site now requires a `?` or `.expect()` against a failure mode with zero production occurrences in the repo, and 31 call sites (all in tests, benches, or examples) were migrated. |
| [0055](0055-config-invariant-enforcement-allocation.md) | Refutes the premise that 71 `pub` config structs constitute a validation bypass. The decision establishes a four-way allocation of invariant enforcement: (1) `*Config` types retain `pub` fields to preserve the `Config { ..Default::default() }` tuning idiom; (2) validation is the consumer's obligation at construction via `config.validate()?` at every chokepoint—already implemented across 34/34 environments, 8/8 RL agents, 4/4 policy heads, and a generic evolution bound; (3) `*State`/`*Params`/`*Genome` encapsulate because their structural invariants must survive in-place mutation; (4) any invariant that must survive struct-literal construction is encoded in a validated newtype (ADRs 0027/0031), making field privacy on the container unnecessary. `#[non_exhaustive]` is reserved for enums only. Accepted costs include a real but deliberate TOCTOU window (mitigated by newtypes), a dormant `Deserialize` obligation, and scoped conformance testing. |
| [0056](0056-non-finite-loss-skip-and-warn-guard.md) | Introduces a `FiniteLossGuard` in `algorithms/shared.rs` that performs a host-side `f32::is_finite()` check on each agent's loss scalar before `backward()`. Since every agent already reads the loss host-side for metrics, the guard adds no device→host sync and runs unconditionally in release. On a non-finite loss, it skips `backward()` and the optimizer step (re-firing on every occurrence) while emitting a one-shot `tracing::warn!` per loss site; skipped values are excluded from epoch-mean accumulators so a single NaN cannot re-poison reported metrics. This generalizes the SAC-$\alpha$ guard across all 8 agents (17 loss sites: PPO policy/value, PPG policy/value/aux-value/aux-total, DQN, C51, QR-DQN, SAC critic₁/critic₂/actor, DDPG critic/actor, TD3 critic_1/critic_2/actor). The guard sits before `Slot::step_with` to prevent NaN gradients from ever forming, strictly strengthening ADR 0046's invariant. It is a loss-level proxy that prevents poisoning when NaN originates in the loss (the dominant mode: PPO exp overflow, log(0), TD-target blowup) but catches gradient-origin NaN one step late; grad-finiteness and reward-finiteness remain separate issues. |
| [0057](0057-target-soft-update-path-is-fallible.md) | Makes the target soft-update path fallible by introducing `PolyakError` (with `MissingActive` and `MissingTarget` variants) and changing `polyak_update`, `soft_update`, and the six off-policy agents' `learn_step` to return `Result` instead of panicking on `ParamId` topology mismatches—treating mismatched target networks as a recoverable configuration error rather than a crash. The change preserves the existing clone-then-reassign pattern so targets retain prior weights on error, extends ADR 0056's skip-don't-crash posture, and upgrades the three topology tests from `should_panic` to exact `PolyakError` assertions. Known limitation: multi-target sequences (DDPG/TD3/SAC) are not atomic—partial updates can occur if a caller swallows the error—but this is unreachable for correctly-cloned in-tree targets. |
| [0058](0058-target-update-type-unifies-cadence-and-tau.md) | Introduces `TargetUpdate` — a single unified type (`struct TargetUpdate { tau: PolyakTau, every: NonZeroUsize }`) in `rlevo-reinforcement-learning/src/target.rs` — that replaces the divergent, per-agent `tau` + `target_update_frequency` (or `policy_frequency` alias) fields across all six off-policy agents (DQN, C51, QR-DQN, DDPG, TD3, SAC). The type encodes the complete "when/how far" contract: `every` gates *when* a target update fires, `tau` (constrained to the half-open interval `(0, 1]`) gates *how far* the target moves at that firing, and `tau = 1.0` yields a hard copy by degeneracy of the shared Polyak formula rather than a separate variant. This design corrects a two-way premise to a three-way divergence (soft-every-gradient-step in DQN family, soft-every-`target_update_frequency` in SAC, soft-every-`policy_frequency` tied to actor delay in DDPG/TD3), closes issue (`tau = 0.0` passing validation) by construction via `PolyakTau`'s exclusive lower bound, eliminates three cross-field validation blocks by making the frozen-target state unrepresentable, and provides `fires_at(updates) -> Option<f64>` as the sole call-site seam. The change is breaking but alpha with no external consumers; DDPG/TD3 retain `policy_frequency` for actor delay only, gaining independent `target_update` control. |
| [0059](0059-target-update-cadence-counts-gradient-updates.md) | Establishes that the target-network update cadence (`TargetUpdate::every`) counts **gradient/optimizer updates** uniformly across all six off-policy agents (DQN, C51, QR-DQN, DDPG, TD3, SAC), rejecting environment-step counting and configurable units. This aligns with the canonical literature (Mnih 2015, Haarnoja 2018, Fujimoto 2018), which define their cadences against parameter updates, and eliminates the "unit trap" where changing `train_frequency` silently altered the effective target-update frequency. The DQN family gains a gradient-update counter matching SAC/DDPG/TD3's existing `critic_updates`, which advances unconditionally—even on skipped non-finite losses—preventing cadence drift during divergence. The `sync_target()` method is removed from the DQN-family agents, moving target updates into `learn_step` alongside the counter and eliminating a train-loop omission failure mode. Default values are preserved at current settings (`polyak(0.005, 1)` for DQN/C51/QR-DQN/SAC; `polyak(0.005, 2)` for DDPG/TD3), while the old `target_update_frequency: 10_000` default is deliberately not carried over to avoid a 10,000× cadence collapse under the new semantics; default-value tuning is deferred separately. |
| [0060](0060-config-values-must-be-finite.md) | Establishes that configuration **values** (the `got` parameter) must be finite, while configuration **bounds** (`lo`/`hi`) may legitimately be ±∞ to express unbounded ranges. The shared predicates `config::positive` and `config::in_range` now reject non-finite values (`NaN`, `±∞`) upfront via a new `ConstraintKind::NotFinite` variant checked before other constraints, while still accepting infinite bounds—so `in_range(C, f, 0.0, f64::INFINITY, x)` remains the correct way to express "non-negative, unbounded above." `ConstraintKind` is marked `#[non_exhaustive]` since it already had a `Custom` escape hatch. This single predicate-layer fix closes 142 call sites (105 `positive` + 37 `in_range` with infinite bounds) with zero migration, prevents infinite learning rates or physics constants from propagating as `NaN` through optimizers, and ensures future deserializers inherit finiteness validation automatically. Two deliberate exceptions (TD3 `noise_clip = ∞` and CMSA-ES `tau_c = ∞`) are rejected because they are non-canonical, `f32::MAX` expresses the same intent finitely, and per-field exemptions would reintroduce the very "two roles, one domain" defect this ADR eliminates. |
| [0061](0061-optional-facing-and-tensorconvertible-no-fabrication.md) | Fixes a contract violation in `GridObservation` and `GoToDoorObservation` where `agent_direction` (a `u8`) was silently dropped during tensor serialization (`to_tensor`) and then fabricated as `Direction::North` during deserialization (`from_tensor`), breaking the `TensorConvertible` trait's lossless round-trip guarantee. The decision makes two changes: (1) `agent_direction` becomes `Option<Direction>` on both types — `from_entity_view` yields `Some(real_facing)` while `from_tensor` yields `None`, honestly representing the absent data; (2) the `TensorConvertible` contract splits into two required clauses — *tensor-image fidelity* (decode-then-re-encode preserves the tensor exactly) and *no fabrication* (any field not written to the tensor must decode to an absence sentinel like `Option::None`, never a plausible in-domain value). The fix is scoped to the rotated-observation branch only; a future unrotated/fully-observable variant would reopen the decision for that variant, since it would need facing encoded in the tensor (as Minigrid's `FullyObsWrapper` does). |
| [0062](0062-grid-layout-fidelity-and-no-dead-rng.md) | Resolves a critical inconsistency in how Minigrid-style grid environments handle randomness: seven of nine environments stored and re-seeded an RNG on every `reset()` but never actually drew from it, silently deviating from their upstream `_gen_grid` implementations which do sample layout parameters (wall positions, door placements, agent/key positions). The ADR establishes that grid environments must either faithfully reproduce their upstream algorithm's random draws (with documented deviations only) or be honestly deterministic with no RNG field at all — a dead RNG field is forbidden as a resting state because adding sampling without removing the re-seed creates a silent bug where every episode gets identical layouts despite appearing randomized. To enforce this, a new shared `placement.rs` sampler materializes free cells and draws uniformly (returning `Result` on exhaustion, never panicking), and a crate-wide source-text guard test mechanically prevents `seed_from_u64` from appearing inside `reset()`. Two environments (`MultiRoom` and `Unlock`) remain deliberately non-conformant with tracked issues and in-file documentation, while five environments (`LavaGap`, `Crossing`, `DoorKey`, `FourRooms`, `UnlockPickup`) now produce varying layouts per episode — a semver-relevant behavioral change requiring consumers to use `reset_with_seed` for reproducibility. |
| [0063](0063-grid-visibility-occlusion.md) | Establishes that grid-environment visibility/occlusion is an **environment-level emission policy** implemented via the `Sensor` trait (not the state-level `Observable` trait), with each of the twelve Minigrid-family environments declaring its policy as a compile-time `const VISIBILITY: Visibility` (eight occluded, four see-through, matching canonical Minigrid exactly). Unseen cells are encoded as `Option<Entity>::None` — never as an `Entity` variant — and the observation type-byte table is renumbered to match Minigrid's `OBJECT_TO_IDX` with `0` reserved for unseen, fixing a live correctness defect where occluded empty cells were indistinguishable from seen ones. Occlusion is applied as a post-pass (`grid::process_vis`, a flood-fill shadow cast) over the raw `egocentric_view` window, which stays policy-free and becomes crate-private; `Sensor::observe` forwards to a shared `observe_grid` that dispatches on the single `Visibility` constant. The `MemoryEnv` minimum size of 11 is retained because occlusion changes nothing about Invariant M satisfiability at any tested size — the flood fill routes light around the corridor walls, making occlusion ineffective at hiding the cue. The changes close ADR 0043's deferred occlusion gap, correct the byte encoding before any baselines are recorded, and unify the grid family with the workspace's `Sensor` convention, at the cost of a breaking observation encoding and loss of runtime A/B occlusion toggling. |
| [0064](0064-observation-carries-no-serde-supertrait.md) | Removes the `Serialize + for<'de> Deserialize<'de>` supertrait from `Observation<R>` because its stated justification (replay-buffer storage) was false — no code in the workspace generically required serde on observations, the replay path explicitly avoids serde to keep bounds minimal, and the sole persistence consumer (`RecordingTap`) persists actions not observations. The bound imposed real costs: two hand-written serde impls (for `ContextualBanditObservation` and `CarRacingObservation`) existed solely to satisfy it but were never exercised, and it made the true bound set invisible at use sites by silently importing serde through the trait. The decision relaxes `Observation` to just `Debug + Clone + Send + Sync` (load-bearing bounds for buffer storage and cross-thread movement), retains all existing serde derives and manual impls on concrete types, and requires future persistence seams to declare their own `where O: Serialize + Deserialize` bounds additively. `Action` and `Reward` deliberately do not gain the bound; symmetry was measured and rejected. |
| [0065](0065-non-finite-reward-is-dropped-at-replay-ingestion.md) | Introduces a `FiniteRewardGuard` that runs an `f32::is_finite()` check at the top of each of six off-policy agents' `remember` methods, dropping any transition with a non-finite reward before it enters the replay buffer. The guard is implemented as a shared struct in `algorithms/shared.rs` (mirroring ADR 0056's `FiniteLossGuard`), with one instance per agent, and exposes a public `dropped_transitions()` counter. Every non-finite occurrence is dropped, but warnings fire only on a decade schedule (1, 10, 100, 1000…) reporting the running total, bounding log volume while preserving magnitude observability. The drop does not affect the env-step counter or ε-decay, but correctly delays `can_learn` since dropped transitions don't count toward buffer fill; episode returns deliberately accumulate the NaN so the primary scientific metric truthfully reflects what happened. The guard runs unconditionally in release because the host-side `f32` check is a register compare with no device sync, and the decision explicitly rejects alternatives like `debug_assert`, clamping to zero, panics, or inline copies per agent — the last rejected because copy-paste omission is exactly how C51 and QR-DQN missed the guard initially. |
| [0066](0066-clamp-nan-behavior-is-backend-specific-pin-with-is-nan.md) | Establishes that `Tensor::clamp`'s NaN behavior is unspecified and diverges across Burn backends (e.g., Flex propagates NaN while Metal's WGSL clamp rescues it to the lower bound), which caused C51's projection to silently produce a valid-looking but corrupted distribution on GPU backends when given a NaN reward. The decision mandates two explicit primitives for handling NaN before clamping: `clamp_preserving_nan` (preserves NaN so downstream finiteness guards can detect it) and `sanitize_fitness_tensor` (replaces NaN with a sentinel before clamping), and flags any bare `Tensor::clamp` on potentially-NaN data without one of these primitives as a review-blocking defect. The fix couples NaN preservation through coordinate clamps with an explicit index clamp to `[0, num_atoms-1]` to prevent out-of-range scatter writes on GPU backends where `NaN.floor().int()` yields `i32::MIN`. This is a convention-only change with no public API modifications, but regression testing requires manual GPU runs since CI lacks GPU adapters. |
| [0067](0067-non-finite-observations-are-dropped-at-replay-ingestion.md) | Establishes that non-finite observations (NaN or ±Inf) are detected and dropped at replay ingestion (`remember`), counted, and reported at action selection (`act`) without substituting a fallback action. The decision is driven by a critical finding: on the CPU backend (flex), ReLU silently converts NaN to 0.0, causing fully non-finite observations to produce valid-looking but meaningless actions that evade all existing guards (loss, Q-value, action validity). A new `HostRow::row_is_finite(&self, scratch: &mut Vec<f32>) -> bool` provided method implements a branchless IEEE-754 exponent-field reduction for performance (~1× memcpy speed), while integer-backed image observations (e.g., `CarRacingObservation` with `u8` pixels) override it with compile-time witnesses proving structural impossibility of non-finite values, avoiding unnecessary cost on large observations. The guard mirrors ADR 0065's `FiniteRewardGuard` design—single ingestion choke point, decade-schedule warning, public counter—and deliberately avoids action substitution because masking the failure with a plausible action would reproduce the exact silent-corruption problem the ADR addresses. |
| [0068](0068-bounds-strictness-enforcement-is-crate-asymmetric.md) | Resolves a migration trap where folding an `ordered`-checked scalar pair into a single `Bounds` field silently drops the strictness requirement (`low < high`) because `Bounds` deliberately permits `lo == hi` (zero-width ranges). An audit found zero live defects but confirmed the strictness-sensitive population is exactly two fields—both `log_std` bounds on Gaussian policy heads in `rlevo-reinforcement-learning`—where a zero-width range collapses `σ` silently without NaNs or panics, while all 30 other `Bounds` fields across `rlevo-evolution` and `rlevo-environments` legitimately tolerate zero width. The decision therefore introduces a named helper `config::nondegenerate_bounds` (delegating to `config::distinct` to also reject infinite endpoints) and scopes a bidirectional-allowlist source-text guard exclusively to `rlevo-reinforcement-learning`, leaving the other crates to a prose convention in `docs/rules.md`. This asymmetric enforcement is justified by the perfectly crate-partitioned ratio; the guard's recurrence trigger covers any new strictness-needing `Bounds` field anywhere, a population rise above ~5, or that population crossing a crate boundary. |
| [0069](0069-sanitized-fitness-is-reduced-in-f64.md) | Corrects a fundamental misconception in ADR 0034: clamping `+∞` to `f32::MAX` does **not** protect reductions from overflow—any finite sentinel fails for unbounded population sizes because `f32::MAX + f32::MAX == f32::INFINITY`. The real safeguard is accumulator width, not the clamp. Four of five fitness-reduction sites in `rlevo-evolution` were silently overflowing in `f32` (affecting species apportionment, `z_score`, and strategy metrics), turning fitness-proportional selection into uniform selection with no crash or `NaN`. The decision mandates that **all host-side reductions over sanitized fitness accumulate in `f64` and narrow to `f32` at most once, after the reduction**, and introduces `sanitized_mean` / `sanitized_sum` primitives to make the correct pattern the shortest thing to write. For device-side reductions (Burn tensors), where accumulator width belongs to the backend, the fix bounds terms instead—`z_score` now rescales inputs to `[-1, 1]` before squaring. A rescale-invariance property test (not a source-text guard) provides the mechanical net, since the defect is transitive through stored fields and cannot be caught by grepping for `sanitize_fitness`. |
| [0070](0070-avg-score-transits-non-finite-scores-the-hardened-mean-is-additive.md) | `AgentStats::avg_score` stays unchanged (it still transits non-finite scores per ADR 0065's contract) but adds two new additive accessors: `finite_avg_score()` returning an `Option<f32>` mean over only finite window entries, and `non_finite_recent_len()` returning the count of excluded entries — these two form an indivisible pair because a hardened mean without its exclusion count constitutes silent sanitization. The predicate uses `is_finite()` (not just `!is_nan()`) to catch both `NaN` and `±∞`, the latter being reachable via `f32` saturation even with no non-finite rewards. An all-non-finite window yields `None` (not `0.0` or `NaN`) to avoid fabrication. The count is computed on demand with no new stored state, and the mean accumulates in `f64` narrowing once to `f32` — this mirrors ADR 0069's pattern but deliberately does not extend that ADR's scope to this crate. The original contract moves from a test-only comment into `avg_score`'s rustdoc for discoverability. |
| [0071](0071-best-score-latches-plus-infinity-the-finite-best-is-additive-and-counted.md) | `AgentStats::best_score` permanently latches at `+∞` when any episode returns infinite reward (a real and reachable defect, worse than the `avg_score` case because it never self-heals), and instead of filtering the raw maximum—which would fabricate a score the agent never earned—it keeps the latch but documents it explicitly in rustdoc, while adding two additive accessors: `finite_best_score()` returning the highest finite score observed, and `non_finite_episodes()` returning a monotone lifetime count of episodes excluded from that finite maximum. The pair mirrors the windowed `finite_avg_score`/`non_finite_recent_len` design from ADR 0070 but with a critical difference: because `best_score` is a lifetime statistic whose record-setting episode may have been evicted long ago, the count must be stored and append-only rather than derived from the sliding window—making this the first place `record` gains state, which ADR 0070's no-stored-state decision explicitly scoped out because its eviction-coupling objection does not apply to a lifetime counter. The two finite/raw pairs are now unified in prefix (`finite_`), predicate (`is_finite()`), empty/all-non-finite behavior (`None`), and return type (`Option<f32>`), discharging ADR 0070's third reopen trigger, while the count uses `usize` with saturating addition to preserve the invariant `non_finite_episodes() <= total_episodes()` by construction, and no `f64` accumulator is introduced because a maximum is a selection, not a reduction. |
| [0072](0072-loss-skips-are-counted-per-site-and-surfaced-as-a-metric.md) | Replaces the one-shot warning latch on the `FiniteLossGuard` with a decade-schedule warning (firing at skip counts 1, 10, 100, …) and adds a cumulative `skipped: u64` counter per loss site (17 total across 8 RL agents), exposed via per-site agent accessors (`skipped_[site_]updates()`) plus an aggregate `skipped_updates()` on the five multi-site agents. The counter enables the identity `applied = attempts − skipped`, making ADR 0059's unconditional counter-advance legible, and surfaces a single canonical metric row `skipped_updates` in `rlevo-metrics-registry` (repealing ADR 0056's "warn only, no metric" clause). The aggregate lives on the agent—not on episode-scoped `AgentStats`—because skips are per-update/per-site, and the cumulative (not per-interval) reporting avoids `log_every` dependence. Negative costs include stricter test assertions, ~7 log lines per site, and 22 new public methods. |
| [0073](0073-carried-item-is-stamped-into-the-agents-own-view-cell.md) | Fixes a POMDP-breaking defect where the agent's carried item (`AgentState::carrying`) never reached its own observation tensor, even though transition dynamics (`dynamics.rs` door unlocking) and episode success (`unlock_pickup.rs` target-carrying check) both gated on it—making policies unable to condition on hand state from observations alone. The fix stamps the carried item (or `Entity::Empty` when empty-handed) into the agent's own view cell **after** occlusion masking, inside the shared `mask_view` chokepoint used by all twelve grid environments, matching canonical Minigrid's `gen_obs_grid` ordering (slice → rotate → occlude → stamp). Writing `Some(Entity::Empty)` rather than `None` avoids falsely marking the always-visible agent cell as unseen (`[0,0,0]`). This changes terminal-frame bytes for eight environments (Goal/Lava underfoot becomes Empty) but introduces no shape changes, no new fields, and no fabrication—ADR 0061's tensor-contract clauses remain untouched. Four alternative stamp sites (out-of-tensor field, fourth encoder channel, inside `from_masked_view`, inside `process_vis`) were rejected for violating tensor-roundtrip invariants, encoder purity, or coverage of `SeeThrough` environments. ADR 0063 is not superseded; this ADR closes the single deferred item it named. |
| [0074](0074-panic-contract-table-is-mechanically-checked.md) | Introduces a workspace-wide source-text guard (`crates/rlevo/tests/panic_contract_table_guard.rs`) that mechanically validates the `rules.md` "Documented Panic Contracts" table's Site column against all production code, resolving a silent drift problem where deleted items (#1085) and missing rows (#1108) went unnoticed. The guard only enforces the "dead-row" direction (every table row still names a live item), leaving the "missing-row" direction (every panic site has a table row) as a review-enforced convention because distinguishing contract panics from internal invariants across six syntactic forms would require an unmaintainable allowlist. The resolver uses a two-tier strategy—module-tier (proves a name exists somewhere in a crate-anchored subtree) and type-tier (locates the type's unique declaration then searches its file or impl blocks)—plus glob rows requiring at least two production matches, all with a single verbatim-text exemption for the unresolvable "Batch rank assertion" const-generic relation. The guard is text-based (not semantic), catches accidents not adversaries, has known limits (comment false positives, same-file collisions, blind to `#[cfg(test)]`), and is deliberately independently deletable—its `cfg_test_regions` scanner is a third workspace copy rather than a shared helper so removing any guard stays a single-crate change. The table's surrounding prose remains unchecked, with a named present cost: `rules.md:116` cites `C51Config` but the actual type is `C51TrainingConfig`. |
| [0075](0075-bench-env-erases-rank-not-modality.md) | Supersedes the object-safety rationale for `BenchEnv` in ADRs 0001 and 0004 without changing any code. Three executed findings: (1) the trait *is* object-safe — `Box<dyn BenchEnv<Observation = O, Action = A>>` compiles once both associated types are named, so claims that associated types make it unusable are false; (2) the erasure is on the wrong axis — it strips the const-generic ranks `R`/`SR`/`AR` while `Observation` and `Action` survive, and those are exactly what differ between environments (`CartPoleObservation` vs. `PendulumObservation`, …), so a single `dyn BenchEnv` holds only envs already sharing an obs/action pair and the deferred "all of classic control" suite **cannot** be built on it; the real prerequisite is an obs/action normalization that does not exist; (3) zero `dyn BenchEnv` values exist in the workspace — `Suite<E>` is monomorphic and monomorphization already does the only erasure that happens. Separately, the implementor census (7 impls) refutes ADR 0004's "narrower `Environment`" framing: `BenchEnv` is a union bound over two disjoint families, and the two production non-`Environment` implementors (`EvolutionaryHarness`, `CoEvolutionaryHarness`) step generations rather than transitions. Corrects six doc sites including `rlevo-benchmarks/README.md`'s false "heterogeneous environments can be boxed and dispatched at runtime" claim. Deliberately decides nothing about the trait's fate — it removes a false premise so that decision can be made on accurate grounds. |
| [0076](0076-trial-seam-splits-machinery-from-trial-shape.md) | Acts on ADR 0075's two-disjoint-families finding without removing anything. Splits `Evaluator::run_suite` into a shape-agnostic `run_trials` (rayon fan-out, per-trial `catch_unwind`, checkpointing, fail-fast, reporter lifecycle) generic only over `T: Trial`, and an `EpisodicTrial` holding the rollout loop; `run_suite` keeps its signature as a thin wrapper. Adds `GenerationProbe` (`begin` / `advance -> Option<Metrics>`) to `rlevo-core::evaluation` beside `BenchEnv`, on the ADR 0004 precedent, so `rlevo-benchmarks` can implement `GenerationTrial<P>` without the `record`-gated `rlevo-evolution` dep. Justified by a survey showing the machinery is nearly shape-free — `Reporter` has 1 episode-shaped hook of 5, `TrialReport` 1 episode-shaped field of 9, `checkpoint.rs` zero — and by three existing workarounds for the same missing abstraction (`PopulationReporter` bypassing `Reporter` via `PopulationObserver`, `RecordingReporter::without_lifecycle`, and the negate/divide/`.last()` correction in `rastrigin_run_suite.rs`). A generation trial deliberately reports **no** episodes rather than fabricating one, since the episode axis was measured degenerate (bit-identical replays under `num_episodes: 2`). `StrategyMetrics`/`CoEAMetrics` become the first `MetricsProvider` implementors, prefixed `ea/` and `coea/`. Rejects an `evolution` feature gate as entrenching the `benchmarks -> evolution` edge that blocks the other fork, and corrects `rules.md`'s `rlevo-core` inventory, which had omitted the `evaluation` module since ADR 0004. |
| [0077](0077-remove-benchenv.md) | Deletes `BenchEnv`, `BenchStep`, `BenchError`, the `rlevo_benchmarks::env` shim, and the crate-root re-export, completing the arc from ADR 0075 (the rationale was false) and ADR 0076 (the replacement seams). By this point both implementor families had migrated — evolutionary harnesses to `GenerationProbe`/`GenerationTrial`, environments to `Evaluator::run_suite` binding directly on `Environment<D, SD, AD, RewardType = ScalarReward>` with `BenchAdapter` deleted — leaving all three types with zero implementors and zero users. Supersedes the remainder of ADR 0004 as it applies to the `env.rs` move and its typed `BenchError`, while 0004's `fitness.rs`, `util/seed.rs`, `Metric`/`MetricsProvider` relocation, `agent`/`seed` shims, and dep-edge rework stay active. Records two costs honestly: the rank-erasure wart relocated to `EpisodicTrial` rather than vanishing (E0207 forces it, exactly as it forced `BenchAdapter`'s phantom params), and const-generic inference now sits on the critical path — sound while each type has one `Environment` impl, E0284 at the call site if a second is ever added. Makes the deferred `Steppable`/`StepOutcome`/`StepError` rename moot. Breaking change; recorded in the CHANGELOG with a migration note. |
| [0078](0078-non-finite-reward-is-counted-and-reported-by-the-benchmarks-harness.md) | Deletes the `debug_assert!` on reward finiteness from `EpisodicTrial::run` and replaces it with a per-trial `u64` count, one summary `tracing::warn!` per affected trial, and a `Metric::Counter` keyed `return/non_finite_steps` absorbed into `TrialReport` **unconditionally, including as `0`**; accumulation stays raw, so a poisoned return still reaches the report rather than being sanitized away (ADR 0061's no-fabrication rule, matching ADR 0065/0070 on the RL side). Leads with a profile argument that needs no prior ADR: the assert made the harness's *artefact* a function of `cfg(debug_assertions)` — release completed with a `NaN`/`inf` `return/mean`, debug panicked into `run_trials`' `catch_unwind`, whose handler builds a **fresh** errored `TrialReport` and discards every already-completed episode, and under `fail_fast` killed the suite. Reconciles two ADRs that disagreed: 0065 rejects `debug_assert!` by name, while 0034:153-157 explicitly permits it — scoped here, not reversed, to sites where sanitize-to-a-sentinel is the primary mechanism and the tripwire is a redundant second net, which the evaluator's was not (it guarded nothing even in debug). Corrects the issue's `NaN`-only framing with the executed aggregate table: `+inf` is scored as a *success the agent never earned* and poisons `max`, while both signs shift the median silently (9.0 → 20.0) beside finite-looking order statistics — the argument for a counter over a bare warning. In `rlevo-core`, documentation only: a `# Finiteness` section on `Reward` naming what all layers do, plus pointers on `ScalarReward` and `Environment::step`'s `RewardType`; no runtime check, and none deferred. Deliberately takes **no** `rlevo-metrics-registry` row — that table classifies tracing field names emitted by algorithms, while this counter travels the `TrialReport` channel; ADR 0072 is precedent for counting-and-surfacing, not for the transport. Honest limits: the counter reaches the JSON reporter only (the TUI no-ops `TrialEnd`, the HTML report reads the tracing layer), it is the workspace's first `Metric::Counter`, it covers `ScalarReward` environments only, and PPO's `compute_gae` (#1042) stays untouched and unguarded. **Partially superseded by ADR 0079**: Decision 4's third bullet and the corresponding clause in Decision 6 ("a deliberate override is not the hazard being defended against" — an agent naming the exact key `return/non_finite_steps` used to win) no longer hold; `absorb_metrics` no longer lets an agent overwrite that counter. Everything else in 0078 — the counting, the unconditional-including-zero emission, the warn schedule, the no-registry-row call — is unaffected. |
| [0079](0079-harness-metrics-are-a-privileged-absorption-path.md) | `absorb_metrics` becomes the **checked** path (collisions re-homed to `agent/<name>`, recorded in `displaced_metrics`, warning emitted); `absorb_harness_metrics` is the new **privileged unchecked** path for harness's own measurements. The polarity — safe behaviour on the default method name — is the load-bearing decision because `Trial` is a public trait with unbounded future implementors. `agent/` prefix is reserved; prefix reservation is deliberately broad. `displaced_metrics` field added with `#[serde(default)]` for checkpoint compatibility. `Metric::Counter` doc fixed (no accumulation). Breaking on behavioural and API axes.
