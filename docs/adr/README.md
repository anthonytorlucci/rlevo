# Architectural Decision Records

Immutable records of the architectural decisions behind `rlevo`. Once accepted,
an ADR is not edited — a later decision supersedes it, and the superseded record
is annotated rather than deleted. Read these for the *why* behind the crate
boundaries and trait design before proposing structural changes.

When you make an architectural decision, add a new numbered file here following
the existing format (`Status` / `Context` / `Decision` / `Consequences` /
`Alternatives considered` / `References`).

> Drafts and in-flight proposals live in the maintainer's working notes; an ADR
> lands here only once accepted. The repo copy is canonical.

| # | Decision |
|---|----------|
| [0001](0001-keep-environments-and-benchmarks-separate.md) | Keep `rlevo-environments` and `rlevo-benchmarks` as separate crates; use a feature-gated `bench` adapter inside `rlevo-environments` instead of merging. |
| [0002](0002-collapse-evolution-traits-into-rlevo-evolution.md) | Move `GenomeKind` into `rlevo-evolution`, delete the dead `Fitness`/`MultiFitness` traits, and drop the `rlevo-core` dep from `rlevo-evolution`. |
| [0003](0003-collapse-rl-modules-into-rlevo-reinforcement-learning.md) | Move `memory`/`experience`/`metrics` from `rlevo-core` to `rlevo-reinforcement-learning` (RL-only consumers); fold `rlevo-utils::math::combinations` into `rlevo-core::util`; delete the `rlevo-utils` crate. |
| [0004](0004-move-bench-traits-into-rlevo-core.md) | **(§6 partially superseded by 0033.)** Move `BenchEnv`/`BenchError`/`BenchStep`, `BenchableAgent`/`FitnessEvaluable`/`Landscape`, `Metric`/`MetricsProvider`, and `SeedStream` from `rlevo-benchmarks` into `rlevo-core`; drop `rlevo-benchmarks` as a prod dep of `rlevo-evolution`; switch `BenchError` to wrap `EnvironmentError` typedly. |
| [0005](0005-examples-and-cross-crate-tests-in-umbrella.md) | **Superseded by 0012.** Examples and cross-crate integration tests live in `crates/rlevo/`; crate-internal tests stay with their owning crate; `[[bench]]` entries stay put. |
| [0006](0006-leptos-first-visualisation-defer-bevy.md) | Build the visualisation layer as a Leptos web client (`rlevo-viz-web`) served by an embedded `axum` server; defer Bevy and native 3D; render `locomotion` envs as a 2D sagittal-plane projection until a successor spec re-opens 3D. |
| [0007](0007-visualisation-crates-isolated-from-production-crates.md) | `rlevo-viz-core` depends only on `rlevo-core`; per-family render adapters live in `rlevo-viz-web` behind feature flags; neither viz crate is a prod or dev dep of any production crate; `Visualize` is not a supertrait of `Environment`. |
| [0008](0008-three-tier-visualisation-ratatui-live-static-report.md) | **Superseded by 0013.** Three-tier visualisation: `AsciiRenderable` at the library level, `ratatui` TUI for live training, static-HTML Leptos viewer for post-run replay. |
| [0009](0009-move-styled-render-into-rlevo-core.md) | Hoist the styled-render surface (`StyledFrame`/`StyledLine`/`StyledSpan`/`SpanStyle`/`Color`/`Modifier`, `palette`, `AsciiRenderable`/`AsciiRenderer`) from `rlevo-environments::render` to `rlevo-core::render`; preserve import paths via a re-export shim. Forced by the `rlevo-environments ↔ rlevo-benchmarks` package cycle. |
| [0010](0010-unify-on-parking-lot-across-viz-stack.md) | Redefine `rlevo_evolution::SharedPopulationObserver` over `parking_lot::Mutex` so the EA observer alias and the record-sink producers share one lock type; add `parking_lot` as an explicit dep. |
| [0011](0011-lift-construction-off-environment-trait.md) | Remove `fn new(render: bool)` from `Environment`; add a standalone `ConstructableEnv` factory trait (not a supertrait). Kill the degenerate `new` stubs in the recording/TUI taps. |
| [0012](0012-split-heavy-examples-into-rlevo-examples.md) | Heavy viz/record/report examples move to a new `crates/rlevo-examples` leaf crate; `rlevo` retains lightweight examples, cross-crate `tests/`, and `benches/`. Canonicalizes the three-tier test placement rule. Supersedes ADR 0005. |
| [0013](0013-metrics-only-live-tui.md) | Collapse visualisation to **two products**: (1) live metrics-only `ratatui` TUI (no env panel), (2) post-run `EpisodeRecord` + static-HTML report. Demotes `AsciiRenderable` to an optional debug helper. Supersedes ADR 0008. |
| [0014](0014-record-schema-v6-single-agent-richness-and-provenance.md) | Bump record `FORMAT_VERSION` 5→6: expand `CANONICAL_METRICS`, add typed run-provenance fields to `RunManifest`, add `EpisodeKind`, an episode wall-clock metric, and a deep-RL `checkpoints` seam. Extends ADR 0013. |
| [0015](0015-shared-typed-metric-registry-crate.md) | Extract a `#![no_std]`, zero-dependency leaf crate `rlevo-metrics-registry` holding the single typed canonical-metric table; consumers re-export it. |
| [0016](0016-memetic-wrapper-and-local-search-seam.md) | Phase 3a memetic algorithms: host-side `LocalSearch<B>` trait with four gradient-free searchers; `MemeticWrapper<B, S, L, F>` implements `Strategy<B>` and owns refinement inside `tell`; one-draw two-stream RNG scheme; `WritebackPolicy` defaults to `Partial(0.5)`. Purely additive. |
| [0017](0017-probability-model-trait-and-eda-strategy.md) | Phase 3b EDAs: `ProbabilityModel<B>` trait (`fit`/`sample`); generic `EdaStrategy<B, M>`; four models (UMDA, PBIL, cGA, MIMIC). Host-RNG sampling convention. Purely additive. |
| [0018](0018-boa-bayesian-network-and-concatenated-trap.md) | Phase 3b follow-up: `BayesianNetwork` (BOA) fifth `ProbabilityModel` with BIC-scored greedy structure learning; new `ConcatenatedTrap` deceptive landscape. Extends ADR 0017; purely additive. |
| [0019](0019-observable-projection-trait.md) | **Superseded by 0047.** New standalone `Observable<OR>` trait in `rlevo-core::state` — the typed home for **modality-changing** POMDPs where observation tensor order differs from state order. `Environment` already permits `R != SR`; zero contract change. Purely additive. |
| [0020](0020-synthetic-pixel-over-grid-env.md) | First **production** `Observable<OR>` consumer — synthetic pixel-over-grid env `PixelGridEnv: Environment<3,1,1>` projecting a rank-1 grid latent to a rank-3 RGB image. Extends ADR 0019; purely additive. |
| [0021](0021-cma-es-placement-and-self-contained-strategy.md) | CMA-ES + CMSA-ES ship as flat, self-contained `Strategy<B>` in `algorithms/{cma_es,cmsa_es}.rs`; `ProbabilityModel<B>` is available but deliberately unused (CSA + evolution paths don't fit fit→sample). Refines ADR 0017 §9; hand-rolled host Jacobi (no new dep); `SeedPurpose::CmaSampling = 11`. Purely additive. |
| [0022](0022-tensorgenome-gat-population-storage.md) | New `TensorGenome: GenomeKind` trait with a GAT `type Tensor<B>` ties `Population<B, K>` storage to the genome kind: one typed field replaces two `Option`s, accessors become total (no `.expect()`), and non-rectangular kinds (`Tree`) are excluded at the type level. Refactor; also removes the unused `GENOME_LEN` const and a dead rank assertion. |
| [0023](0023-objective-sense-and-maximize-convention.md) | Adopt a single **maximise-native** internal convention across the whole library and add a `rlevo_core::objective::ObjectiveSense { Minimize, Maximize }` primitive. Flips the entire `rlevo-evolution` engine to canonical maximise (sense-unaware); cost objectives are reconciled at one chokepoint (the `EvolutionaryHarness` + `FromLandscape`/`FromFitnessEvaluable` adapters); `BatchFitnessFn::sense()` required (only `Landscape` defaults to `Minimize`). Deletes the NEAT exception and three hand-negations; bumps record schema 6→7. K=1 atom of the deferred MOO sense vector. |
| [0024](0024-rlevo-test-support-dev-crate.md) | Extract a **dev-only** (`publish = false`) `rlevo-test-support` crate — consumed via `[dev-dependencies]`, its deps tracking the fixtures it holds (today `rlevo-core` + `rlevo-environments`) — as the workspace's general home for shared cross-crate test scaffolding. **RL is the first consumer** (evolution/hybrid to follow additively): the `LinearEnv`/`cartpole_seeded` fixtures, the `Flex` determinism preamble, a **measured** random-policy baseline (`baseline::random_return`, replacing hard-coded magic numbers), standard assertions, and the `rl_learning_test!` / `rl_reproducibility_test!` suite macros. Fixes the `<algo>_<env>_<category>` test-naming convention; per-algorithm networks and behavioural tests stay test-local. Consume-don't-constrain posture mirrors the benchmark/viz isolation rule; no production-code or schema change. |
| [0025](0025-stateful-policy-rollout-contract.md) | New `StatefulPolicy<B,E>` trait (with a `ReactivePolicy` `Hidden = ()` convenience + blanket impl) in `rlevo-hybrid` makes **recurrent / memory policies first-class** on the RL-coupled fitness seam, so `RolloutFitness`/`PolicyNeuroevolution` can host the POMDP policies the library's POMDP envs require (Santa Fe ant). `RolloutFitness` is refactored to delegate scoring to an inner `ModuleEvalFn` (deleting the duplicated unflatten-score loop, #92); the rollout threads `&mut Hidden` via `reset`/`act`. **Not purely additive** — removes the stateless policy closure and changes `RolloutFitness::new` (drops the closure, gains a captured `device`); reactive call sites migrate to `ReactivePolicy`. Scorer is `Box<dyn Fn>` (not the spec's `Arc`, which does not impl `Fn`). Preserves ADR 0023 maximise-native sense; contained to `rlevo-hybrid`. Implements #91 + #92. |
| [0026](0026-shared-config-validation-convention.md) | Introduce a standalone `Validate` trait (`fn validate(&self) -> Result<(), ConfigError>`) + structured allocation-free `ConfigError`/`ConstraintKind` in a new `rlevo_core::config` module — one shared config-validation convention for the workspace's 87 `*Config` types. Resolves the standing `rules.md §4` panic-vs-`Result` contradiction: `Deserialize`-able configs are user data, so an **assembled** config validates to `Result` while a single documented **builder setter** (`with_capacity`/`with_alpha`) may still panic. `Default`s must pass their own `validate()`. Purely additive to `rlevo-core`; adoption of the ~85 remaining configs is deferred to the per-crate issues (#102/#103, #137–139, #164–166) this unblocks. Follows the ADR 0011/0023 pattern of a small typed primitive in its own core module. |
| [0027](0027-bounds-newtype-for-closed-ranges.md) | Add a `Bounds` inclusive-range newtype (`rlevo_core::bounds`) that is **valid by construction** — the whole invariant is `lo <= hi`, rejecting `lo > hi` and `NaN` while allowing a single-point `lo == hi` and a one-sided infinite `[0.7, ∞)` healthy interval — making the `f32::clamp` panic and the `x.max(lo).min(hi)` silent-collapse hazard unrepresentable wherever a `Bounds` is held, not just at the config boundary. Complements ADR 0026: a `Bounds` field is self-validating, so it **removes** the paired `config::ordered("bounds", …)` line (~22 sites) rather than replacing `Validate`. Validated `Deserialize` via `#[serde(try_from)]`; dedicated `Copy` `BoundsError` (constructor has no field name). Migrates every `(f32,f32)` range field in `rlevo-evolution` (all `bounds`) and `rlevo-environments` (`action_clip`, `HealthyCheck` ranges, `clip_contact_cost`) and folds mountain-car's `min/max` scalar pairs into `Bounds` (breaking serde shape, alpha). Closes #99/#140/#196 and fixes three previously-unguarded sites (four local-search params, `HealthyCheck::state_range`, `clip_contact_cost`). |
| [0028](0028-tensor-batch-conversion-seam.md) | Make the host-only flat-`f32` **row-writer the required primitive** of `rlevo_core::TensorConvertible` (`row_shape()` + `write_host_row()`); `to_tensor` becomes a **derived** default (do not override) so the single-item and batch layouts share one definition; new free fn `stack_to_tensor<R, BR>` stages a whole batch as one host buffer and issues a **single** `Tensor::from_data` upload (the sole library-side `BR = R + 1` assert — stable Rust cannot express `R + 1` in types). Burn 0.21 audit: no built-in single-upload batcher exists (Burn's own `Batcher`/MNIST idiom is N transfers + `cat`), so the helper is additive, not redundant. First consumer: `sample_batch` in RL `memory.rs` — obs/actions/next-obs via `stack_to_tensor` (one upload each, was N); rewards stay rank-1 `[batch]` via the row-writer + one `from_floats` (never the helper, which would yield `[batch, 1]`). **Breaking** trait change migrating all ~27 impls in one PR (alpha); `from_tensor` untouched; inverse batch decode and `Kind` (`Int`/`Bool`) generalisation explicitly deferred. Implements #195 P2; P1 (the `unwrap_or_default` footgun) lands as a `docs/rules.md` convention + CI grep guard, not a sweep. |
| [0029](0029-host-rng-seeding-convention.md) | **(§3's dead-`_rng` grid carve-out, and the matching "Neutral" consequence, partially superseded by 0062.)** Codify the **host-RNG seeding convention** (already followed in `rlevo-evolution`) as a `docs/rules.md` §8 rule and bring the two lagging crates into line. Environments **own a persistent RNG seeded once at construction**; `reset()` advances the stream (successive episodes differ) instead of re-seeding from `config.seed` each call — fixing #104 across all genuinely-stochastic envs (locomotion, classic-control, `pixel_grid`, `dynamic_obstacles`). Deterministic replay moves to an inherent `reset_with_seed(seed)`; run-level reproducibility is still guaranteed by the construction seed. The **bandit family** is brought into compliance too: the problem instance (arm-means / phases / context) is sampled **once at construction** and preserved across resets (`NonStationaryBandit` restores its drift baseline), so reward noise varies per episode; `AdversarialBandit` is genuinely deterministic (pure-function reward) and drops its now-unused RNG field. The deterministic `_rng` grids are no-ops and untouched. Host-sampling stays `seed_stream`/`SeedStream`-only (`B::seed` + `Tensor::random` banned — process-wide RNG mutex races parallel tests). Unifies PPO/PPG RNG trait bounds onto `R: Rng + ?Sized` (the `&mut dyn Rng` was never object-safety-load-bearing). **Behavioural / semver-relevant** env `reset()` change (alpha); otherwise additive. Extends ADR 0016/0017. Implements #197; fixes #104. |
| [0030](0030-permutation-tensorgenome-and-population-nonempty-invariant.md) | Close the three #158 core type-safety gaps in `rlevo-evolution`. **`Permutation` implements `TensorGenome`** (`Tensor<B, 2, Int>`) now — a permutation is genuinely rectangular, unlike `Tree` — so `Population<B, Permutation>` type-checks and gains a `new_permutation` constructor (its per-row bijection invariant stays **unchecked**, mirroring `Binary`/`Integer` unchecked values; the ACO operators remain a `todo!()` stub). This advances the opt-in ADR 0022 anticipated, landing the *storage seam* ahead of the *operators*. **`Population` constructors** (`new_real`/`new_binary`/`new_integer`/`new_permutation`) change `-> Self` → `-> Result<Self, ConfigError>`, rejecting a zero-row/zero-column tensor via `config::nonzero("Population", …)` so the failure names `Population` instead of surfacing as an opaque downstream operator panic (ADR 0026 convention; breaking but no external consumers yet, alpha). The **`TensorGenome::Tensor` GAT gains `+ Send + Sync`** (was `Clone + Debug` only) so `Population<B, K>: Send + Sync` is provable under the `Strategy: Send + Sync` contract — a straight bug fix under ADR 0022. Extends ADR 0022; reuses ADR 0026. Fixes #158. |
| [0031](0031-probability-rate-newtypes.md) | Add two validated `f32` newtypes in `rlevo-core` — `probability::Probability` (invariant `0.0 <= p <= 1.0`) and `rate::NonNegativeRate` (invariant `is_finite() && r >= 0.0`) — mirroring the `Bounds` shape (ADR 0027): `new` (panicking literal) / `try_new` (`Result`), dedicated `Copy` error, validated `#[serde(try_from)]`. Threads them into the `rlevo-evolution` operator signatures (`blx_alpha` α, `uniform_crossover`/`bit_flip_mutation`/`uniform_reset` p, `gaussian_mutation` σ) and the `GaConfig`/`BinaryGaConfig`/`CgpConfig`/`GaCrossover`/`WritebackPolicy::Partial` fields, so a `NaN`/`Inf`/out-of-range rate is **unrepresentable** at the operator boundary rather than silently degenerating the Bernoulli mask (all-false → no-op / one-parent clone), inverting the CGP host comparison (mutate-everything), or poisoning the BLX-α offspring tensor. Self-validating fields **remove** their paired `config::in_range` line (ADR 0027 §3) and delete the memetic hot-path `debug_assert!`. Resolves #144; the 🔴 non-finite *parent-gene* propagation guard (crossover §1.3) and the out-of-scope `arch_mutation_rate`/`gep` rates are deferred to follow-ups. |
| [0032](0032-neat-opaque-id-newtypes.md) | Convert the three NEAT id aliases (`type NodeId = u64` / `InnovationId` / `SpeciesId`) into **opaque newtypes** over `u64`, defined in place (`topology.rs` / `species.rs`), so id-confusion — passing a `NodeId` where an `InnovationId` is expected, the 🔴 gap all three file reviews flagged — becomes a compile error. Reuses the opaque half of the ADR 0027/0031 shape (private field, `const new`/`get`, `pub(crate) succ` for the registry/speciation counters) **minus validation**: an id has no invariant (infallible `new`, no `try_new`/error) and no NEAT type is serialized (no serde, no schema bump). Derives `Eq + Hash` (HashMap/HashSet keys) and `Ord` (the innovation-sorted `connections` invariant, `BTreeMap<NodeId,_>`, ascending-id `sort_*`). Contained: production `neat.rs` works via the derives (only `next_species_id` init wrapped); no downstream-crate, example, or NAS/weight-only impact. Resolves #142. |
| [0033](0033-share-splitmix64-mixer-across-core-and-evolution.md) | Dedupe the byte-identical `splitmix64` mixer — promote it to a single `pub const fn rlevo_core::util::seed::splitmix64` (with a **frozen-reference stability contract** and a golden-value pin test), delete the copy in `rlevo-evolution/src/rng.rs`, and import the core one. **Partially supersedes ADR 0004 §6** (which chose to keep the local copy): for a frozen algorithm whose output must never change, one source of truth guarded by a pin test is strictly safer than two copies that can silently drift. Pure refactor — byte-identical body means **no runtime-output change**; the two seed-derivation *schemes* (`SeedStream`'s `(env, trial)` fan-out vs `seed_stream`'s `(base, generation, purpose)`) stay independent, sharing only the mixer. Same PR lands §7.1 (exhaustive all-pairs `SeedPurpose` distinctness + constant-distinctness tests) and §8.1 (documents that `SeedPurpose::Other` is a shared catch-all whose cross-strategy isolation relies on a fresh `base`/`generation`, and that its constant coincides with φ64). Resolves #161. |
| [0034](0034-fitness-hygiene-chokepoint-convention.md) | One fitness-hygiene primitive in `rlevo-evolution` (`sanitize_fitness` scalar + `sanitize_fitness_tensor` device sibling) applied at the four *driver chokepoints* — `EvolutionaryHarness::step` (on the **canonical** post-`sense` tensor, so one insert covers every `Strategy<B>`), `NeatStrategy::tell`, `ArchNasStrategy::tell`, and the coevolution coupled-fitness path — backed by the per-site "sanitize-then-`total_cmp`" `rules.md` §3 convention as the correctness floor. Rule: `NaN → −∞` (worst under maximise, preserved from ADR 0023; Rust's positive `NaN` else `total_cmp`-ranks as an immortal champion), `+∞ → f32::MAX` (ranks top but **finite**, so it can't blow a mean/variance/reward), `−∞`/finite pass through. `StrategyMetrics` means over finite members and reports `broken_count`. Closes only the **fitness-hygiene** class of #131–#134 (the class-2 ordering-direction and class-3 parameter-numerics/config bugs split to follow-ups); generalizes the per-model #129 (EDA)/#130 (GEP) precedents. Extends ADR 0023 (not superseded); class-3 guards belong at the ADR 0026 `Validate` boundary. `CanonicalFitness<B>` newtype named as the fast-follow if a bypass regression recurs. |
| [0035](0035-coupled-fitness-sense-and-coevolution-canonicalization.md) | Extend **ADR 0023 to the co-evolution coupled path**: add a **required, no-default `CoupledFitness::sense()`** (mirroring `BatchFitnessFn::sense`) so `evaluate_coupled` returns **natural** fitness and the co-evolutionary algorithm `step` becomes the sole **canonicalise-then-sanitize** chokepoint (negate iff `Minimize`, then `sanitize_fitness_tensor` — ordering load-bearing, per ADR 0034). `CoEAMetrics` display fields (`best/mean_fitness_{a,b}`) now report in the objective's **natural** sense (parity with `StrategyMetrics`), and a new **`binding_fitness`** field carries the canonical `min(best_a,best_b)` harness reward. `HallOfFameFitness` canonicalises champion selection **unconditionally** (even at blend `0`) before the sense-blind `HallOfFame::update` argmax, and its mutex is narrowed to an archive snapshot + final update. `CooperativeState` caches its `dims_b` complement (state-caching, not a `DimSplit` newtype — the `Validate` chokepoint already enforces the split). Enforces a **uniform-common-direction** constraint on `sense()`; asymmetric zero-sum objectives are out of scope for v1 (pre-negate + `Maximize`). Extends ADR 0023, reuses ADR 0034/0026. Resolves #160. |
| [0036](0036-adopt-proptest-for-property-tests.md) | Adopt **`proptest`** as a workspace-declared, **`rlevo-evolution`-only dev-dependency** for property/invariant tests, complementing (not replacing) the seeded-`StdRng`/`seed_stream` example convention. Declared `{ version = "1", default-features = false, features = ["std", "bit-set"] }` — defaults stripped to drop `fork`/`timeout` (`rusty-fork`/`tempfile`/`wait-timeout`), since per-case forking would re-init the Burn backend in a subprocess. **HARD RULE (ADR 0029):** proptest generates **host config only** (`λ`, `D`, structural sizes, `seed: u64`); the test body drives all algorithm RNG through `seed_stream(seed, generation, SeedPurpose::_)` — `B::seed` + `Tensor::random` stays banned, proptest's PRNG never touches Burn, and `fork`-off keeps cases sequential (no new race against the process-wide RNG mutex). proptest for **input-space** invariants (roundtrip, shape/length, monotonicity, no-panic/no-NaN across the valid domain, `Validate` boundaries); seeded examples for **specific-scenario/known-answer** cases (don't rewrite passing tests). Strategies bounded to the valid domain (`Bounds`/`Probability`/`NonNegativeRate`, `Validate` ranges); assertions thread-count-invariant (GEP rayon) or pin the pool. CI-cost policy: 16 cases + capped `max_shrink_iters` for backend-heavy props, 64–128 for cheap structural ones (no config file — per-`proptest!` `ProptestConfig` / `PROPTEST_*`). **Commit `proptest-regressions/`** as a durable replay guard (reversible). Adversarial findings are filed issues, not silenced properties. Builds on ADR 0029; reuses ADR 0026/0027/0031/0034. Resolves #239. |
| [0037](0037-external-force-lifetime-and-substep-actuation.md) | Fix the #98 force-accumulation defect: rapier3d 0.32 `add_force`/`add_torque` are additive and the pipeline never clears the accumulator (the "auto-cleared each step" claim is false). Enforce a **one-integration-step force lifetime** inside each backend's step primitive — `Rapier3DWorld::step_once` now calls a private `reset_external_forces` (`reset_forces`/`reset_torques`, `wake_up = false`) **after** `pipeline.step`; the box2d half (`RapierWorld::step`) does the same under the shared contract (parallel engineer). Add `Rapier3DWorld::step_actuated(apply)` that re-invokes a control closure before **each** of the `frame_skip` substeps (MuJoCo `ctrl`-held-across-substeps): the four rapier3d locomotion envs (InvertedPendulum, InvertedDoublePendulum, Reacher, Swimmer) precompute control, capture `Copy` handles + control into a world-only closure (no `self`/`RefCell`), and re-apply it every substep; Swimmer's world now uses `config.frame_skip` (was a hand-rolled loop) and its `apply_drag` becomes a free `apply_drag_to` recomputing drag per substep from live velocity. Motor-driven joints (bipedal walker) and snapshot/restore are unaffected. Swimmer `gear [5,5]`/drag frozen (canonical `[150,150]` re-tune deferred). Regression tests assert stationary per-step Δv under a constant action. Resolves the force-accumulation scope of #98; RNG-reseed / finiteness / terminal-guard / config-validation scopes split to follow-ups. |
| [0038](0038-continuous-action-components-const.md) | Fix #100 (`ContinuousAction::random()` panics for multi-component rank-1 actions) by adding a **required, no-default** `const COMPONENTS: usize` to `ContinuousAction` — defined as the length of `as_slice()`/`from_slice()`, explicitly not `Action::RANK` (tensor rank) and not `shape().iter().product()` (fails the trait's own Convention-B test type, `ContinuousActionTest`, whose `shape() == [1,1,1]` products to `1` for 3 real components). The default `random()` now loops `0..Self::COMPONENTS`, correcting `BipedalWalkerAction`/`LunarLanderContinuousAction`'s symmetric `[-1,1)` sampling for free; `CarRacingAction` keeps an asymmetric-bound override (Gymnasium gas/brake `∈ [0,1]`). A `COMPONENTS == as_slice().len()` contract test, added across all 11 impls, caught and fixed a pre-existing `CarRacingAction::as_slice()` truncation bug. Breaking change to all 11 impls (alpha, atomic). Explicitly defers `BoundedAction<R>::low()/high()` (`[f32; R]`, same rank-vs-component gap, blocked on unstable `generic_const_exprs`) to a follow-up ADR, naming the concrete DDPG/TD3/SAC `act`/`act_with` call sites and SAC's `-(A::RANK as f32)` target-entropy default that already rest on the coincidental `RANK == COMPONENTS == 1` of every existing `BoundedAction` impl. |
| [0039](0039-box2d-states-own-markov-dofs.md) | Resolve #117 (box2d `State`s alias external Rapier handles instead of owning Markov state). **Step 1 ships now:** encapsulate the three box2d states (`pub(crate)` fields + `#[must_use]` accessors — deliberately **no `try_new`**, since construction is in-module incremental with `::invalid()` handle placeholders and there is no public/serde path, so `pub(crate)` alone closes the rules §3 threat model), give each a **real `is_valid()`** (handle-validity + finiteness — including lunar's silently-skipped `prev_shaping`, + CarRacing's `tiles_visited <= total_tiles`), **wire it via `debug_assert!`** at the end of each `reset`/`step` (it had zero callers — cross-arch-safe NaN/handle check, not a magnitude bound), turn CarRacing `current_tile: usize` → `Option<usize>` (kills the `0` dual-sentinel), and delete the redundant `numel()` overrides. **Target (deferred):** box2d states own their Markov DOFs as values with handles moved onto the env core; **CarRacing re-models as `Environment<3,1,1>` = compact `State<1>` + `Observable<3>` pixel projection** (ADR 0019/0020) — the load-bearing constraint being that `observe`/`project` are `&self`-only, so BipedalWalker lidar and CarRacing pixels stay env-computed **cached sensor snapshots**, not pure functions of the owned DOFs. Refactor deferred to #255 (CarRacing) / #256 (LunarLander + BipedalWalker). Grounded in Gymnasium canonical state defs + Sutton & Barto §3.5. |
| [0040](0040-environment-config-error-and-terrain-output-contract.md) | Resolve #120 (BipedalWalker terrain generates invalid geometry). Add a **generic** `EnvironmentError::Config(#[from] ConfigError)` variant — kept environment-agnostic because `reset()` re-runs procedural-world construction, so a config-domain invariant can re-surface at reset, and `#[from]` gives `?` ergonomics without a stringly re-wrap. Define and enforce the **`TerrainGenerator` output contract** (≥2 points, x non-decreasing, first x < 0, world-space) — the trait stays infallible + object-safe (`Box<dyn TerrainGenerator>`), and `BipedalWalker::rebuild_world` is the **single chokepoint** that re-validates the polyline (rejecting the backwards/zero-length collider with `ConstraintKind::Custom`) before building; the silent `pts.len() < 2` early-return in `build_ground` is removed and `build`/`reset` propagate with `?`. Fix the three #120 High findings: (1) `HardcoreTerrain::generate` rewritten in **world space** with named constants, appending the terminal `[190, 0]` cap **only if** the last obstacle didn't overshoot (a wide boundary pit previously pushed x past 190, so the unconditional cap landed left of it → backwards cuboid); (2) both Rough/Hardcore hold a flat `SPAWN_PAD = 20` pad **spanning the spawn point** (walker at the pad middle, Gymnasium `TERRAIN_STARTPAD`) so obstacles begin ahead of the hull; (3) **Model B** — `RoughTerrain`/`HardcoreTerrain` fields go private with validating `new(...) -> Result<Self, ConfigError>` + `impl Validate` (roughness non-negative **and finite** — `0` legal, not strictly positive; step positive+finite; `pit + stump <= 10`), killing the `random_range(-roughness..=roughness)` empty-range panic on deserialized/struct-literal config. Reuses ADR 0026 (`Validate`/`ConfigError`); follows ADR 0039's make-invalid-states-unrepresentable posture. Regression: 512-seed monotonicity sweep. |
| [0041](0041-rapier3d-joint-actuation-and-contact-wrench-semantics.md) | Resolve #123 (rapier3d backend: unimplemented joint torque, under-specified contact force). **Implement `apply_joint_torque`** — signature changes to `-> Result<(), BackendError>` (new `thiserror` enum: `InvalidJointHandle`, `UnsupportedJoint(&'static str)`; zero prior callers) — **dispatched by joint kind**: `Impulse` (maximal coords) applies **equal-and-opposite** `±τ·â` to both bodies (solver enforces the hinge, zero net external torque); `Multibody` (reduced coords) torques the **child only** (`+τ·â`, solver supplies the parent reaction — the swimmer double-torquing convention), under the parent→child insertion invariant. `τ` is the generalized force on the single free **revolute** axis (MuJoCo `motor` analogue; gear stays the caller's job), one-substep lifetime (ADR 0037, re-applied via `step_actuated`); sign: `+τ` drives `body2` positively about `+â`. **Domain is revolute-only** (`Result`-rejects prismatic/spherical/fixed via `locked_axes`); prismatic/linear cart-force actuation deferred (envs keep using `bodies_mut`). Two rapier-0.32-under-glam discoveries: `GenericJoint::local_axis1()` is **anchor-contaminated** (`Pose3 * Vec3 = transform_point`) so `â` is extracted from `local_frame1.rotation · X` (unit) instead; and `contact_force` reported an **inverted vertical sign** (a resting ball yielded negative `wrench[2]`) — **discovered by the new pinning tests and fixed under this same issue** (#123): the returned wrench is the external contact force-torque acting **ON the queried body**, so a resting ball now reports `wrench[2] ≈ +m·g`. **`contact_force` math unchanged**, docs rewritten + tests added: instantaneous **last-substep** `impulse/substep_dt` (frame-skip-invariant, Gymnasium read-after-last-substep parity), **force-first** `[f(3), τ(3)]` layout (deliberate deviation from `cfrc_ext`'s `[τ(3), f(3)]` — false "matches cfrc_ext" claim removed), **body-CoM** torque reference (vs subtree-CoM — Ant/Humanoid `contact_cost` caveat), contact-manifold-only content, same-parent pairs cleared (`narrow_phase.rs:841`). Skeleton builders set `contacts_enabled(false)` on jointed neighbours (MuJoCo parent–child filter parity; applied to the four env builders by a concurrent change). Extends ADR 0037. |
| [0042](0042-snapshotbase-carries-optional-metadata.md) | Resolve #128 (`LunarLanderSnapshot` duplicates `LocomotionSnapshot` and mis-documents its metadata). `SnapshotBase<R, ObservationType, RewardType>` gains a `pub metadata: Option<SnapshotMetadata>` field and a fluent `#[must_use] with_metadata(self, SnapshotMetadata) -> Self` builder, and overrides `Snapshot::metadata()` to return it — closing the gap where core declared `SnapshotMetadata` + a `metadata()` seam but its own default `Snapshot` carrier could not express either. Both bespoke `impl Snapshot<1>` types collapse to local type aliases (`pub type LocomotionSnapshot<O> = SnapshotBase<1, O, ScalarReward>;`, `pub type LunarLanderSnapshot = SnapshotBase<1, LunarLanderObservation, ScalarReward>;`), dropping in-tree direct `Snapshot` impls from 2 to **0**. **The decider is `TimeLimit`**: `wrappers/time_limit.rs` binds its inner env's `SnapshotType = SnapshotBase<D, Obs, Rew>` and mutates `.status` in place, so a bespoke `SnapshotType` forfeits `TimeLimit` composition — a wart already admitted in `lunar_lander/snapshot.rs`'s doc comment. This unblocks `TimeLimit` for all six previously-locked-out environments (4 locomotion + 2 lunar lander) and every future `SnapshotBase`-bound wrapper. Rejects the naive shared alias `LunarLanderSnapshot = LocomotionSnapshot<LunarLanderObservation>` on verified evidence: `box2d` and `locomotion` are orthogonal Cargo features, so it fails E0433 under `--no-default-features --features box2d` — a class of bug no existing CI job could catch, so a two-step `cargo check` feature-orthogonality guard is added to `crate-tests.yml` (a full `cargo hack --feature-powerset` job is deliberately deferred to its own issue). Also rejects promoting a separate `MetadataSnapshot<R,O,Rw>` into core (institutionalizes the exception, does not fix `TimeLimit`) and an ungated shared type inside `rlevo-environments` (leaves core's contract unfulfilled, does not fix `TimeLimit`, inverts the dependency story). **Breaking at alpha** (5 struct-literal sites gain `metadata: None`; ~20 constructor call sites move metadata to a `.with_metadata()` tail; no `#[deprecated]` shim — the type *names* survive as aliases, but a constructor cannot be deprecated-and-retained on a now-foreign type, E0116); precedented by ADRs 0028/0030/0038. `SnapshotMetadata` still has no production consumer (`BenchStep` drops it, `EpisodeRecord` has no metadata field) and no serde derives are added, so there is no record `FORMAT_VERSION` bump. Narrows ADR 0019's "`SnapshotBase` byte-unchanged" statement, which scoped only that ADR's own change. Amends `docs/rules.md §10`. |
| [0043](0043-grid-observation-contract.md) | **(§3 superseded by 0063.)** Resolve #109 (`MemoryEnv` is reactively solvable; `GoToDoorEnv`'s mission never reaches the policy). Records **Invariant M** — the egocentric view must never contain the cue cell, for any decision-region cell/facing — and makes `MemoryEnv` size-configurable on the canonical layout with `Validate` rejecting `size < 11`; the bound is *derived* (agent view reach 6, cue at `x=1`, fork column `size-2` ⇒ `size >= 11`) from rlevo having **no occlusion** anywhere in the grid family (`egocentric_view` reads every cell unmasked), so rlevo ships a strict subset of canonical Minigrid configs (S11/S13 only) and adding occlusion later would relax the bound to 7 — a documented two-way door. `GoToDoorEnv` gets a bespoke `GoToDoorObservation` (`[7,7,4]`, channel 3 broadcasts the mission color as the **same ordinal encoding** `Entity::color_u8` already uses in channel 1, not a one-hot, so a network can learn mission/door equality); rank stays 3 so `Environment<3,3,1>` and the `SnapshotBase`-alias convention (ADR 0042) are unchanged. Rejects widening the shared `GridObservation` (breaks all 12 envs' shape), routing the mission through `SnapshotMetadata` (ADR 0042 — never reaches a policy), and an out-of-band `env.mission()` accessor (no rollout loop threads it). Records, but explicitly defers, the crate-wide occlusion gap as a stated non-decision (Invariant M is phrased to survive it). Both envs were also violating ADR 0029 (re-seeding `self._rng` from `config.seed` inside `reset()`, previously harmless only because the RNG was never read); both lines are deleted and both envs gain `reset_with_seed`, and `MemoryConfig::swap_fork` / `GoToDoorConfig::target_color` are removed as config fields that pinned a quantity the environment is supposed to sample. |
| [0044](0044-post-terminal-step-is-an-error.md) | Resolve #105 (post-terminal `step()` unguarded across `toy_text` — a `Left` from cliff-walking's goal lands on cliff and emits **−100 on a `Running` snapshot**; blackjack's post-terminal `Hit` overflows a **`u8`** pip accumulator at ~26 ten-valued cards: panic in debug, silent wraparound *back into the valid range* in release). **`step()` after a done snapshot returns `Err`, it does not absorb.** New **generic, structured** `EnvironmentError::StepAfterEpisodeEnd { status: EpisodeStatus }` (following ADR 0040's precedent, and rejecting the stringly-typed `InvalidAction("...")` — the action is legal, the *call sequence* is not); the payload carries the status so a caller can tell an MDP `Terminated` from a wrapper-imposed `Truncated`. `EnvironmentError` becomes `#[non_exhaustive]` (safe: it constrains only exhaustive `match` outside core, not variant construction). Grounded **honestly**: Sutton & Barto's absorbing state (§3.4) is a *definitional device for the return* $G_t$, not an operational `step()` spec, and Gymnasium does **not** reject post-terminal steps (`OrderEnforcing` guards only reset-before-step; CartPole warns once then keeps integrating) — so this is an **engineering judgment**, not a literature mandate. The decider is asymmetry: **absorbing is recoverable from reject via a wrapper; reject is not recoverable from absorb** — absorb is a one-way door into silent replay-buffer bias. New crate-level `EpisodeGuard` (`rlevo-environments/src/episode.rs`; **not** core — §1 contract crate; **not** `toy_text/mod.rs` — `TimeLimit` consumes it too) stores an `EpisodeStatus` behind a **private** field (never `done: bool`, §10; privacy keeps the step-before-reset fix a two-way door via `Option<EpisodeStatus>`, **not** a public `EpisodeStatus::NotStarted` variant). `check()?` is the **first statement** of `step()` — before any state mutation *or RNG draw* (ADR 0029), with a single exit recording the emitted snapshot's own status so guard and snapshot cannot drift. **The generalizing rule: a wrapper that manufactures a terminal status owns the guard for it** — `TimeLimit` delegates *then* upgrades `Running → Truncated`, so the inner env never learns it was truncated and a guard below the wrapper **provably cannot** reject a post-truncation step. Blackjack's `hand_value` accumulates in `u16` and **saturates** to `u8::MAX` (`255 > 21` ⇒ still a bust), keeping the public `player_sum: u8` fields untouched — a naive `u16` return type would have been a public API change. Rejects the template-method `step`/`step_inner` split (unbreakable, but a one-way door across 48 impls and incompatible with the wrapper pre/post pattern). Contract is **normative in the `step` rustdoc with a disclosed alpha migration note** (44 envs non-conformant on day one; note deleted when #289 closes) — "recommended" was rejected as a contract nobody can rely on. Guard is **opt-in**: the `assert_rejects_post_terminal_step` conformance helper is the mitigation, not enforcement; fixture/stub envs are **exempt by decision** (`TimeLimit`'s `StubEnv` stays unguarded *so the tests exercise the wrapper's* guard). A future `VecEnv` **must own auto-reset itself**. The guard immediately caught a latent defect: `frozen_lake`'s `slippery_mean_direction_differs_from_action` was silently stepping past termination in ~⅓ of its 10,000 iterations. Follow-ups: #289 umbrella (+#290–#294 per family), #295 bandits' dead `done` field + the `KArmedBandit::is_done()` §10 second-source-of-truth violation, #296 step-before-reset. |
| [0045](0045-landscape-bounds-is-a-search-box.md) | Resolve #113 (`bounds()` wrong for asymmetric-domain landscapes). `Branin::bounds()` returned `(-5, 10)` — the `x₁` range of the asymmetric domain `x₁ ∈ [-5,10]`, `x₂ ∈ [0,15]` — and since the single `(lo,hi)` pair is applied to **every** coordinate, the box **excluded the certified global optimum `(−π, 12.275)`**: unreachable by any search, at any budget. `Trefethen` had the mirror defect (returned the `x₂` range `(-4.5,4.5)`, clipping `x₁ ∈ [-6.5,6.5]`). `Bukin6` had independently got it right (square hull `(-15,3)` + guard tests) but its convention was never elevated to a rule, so two siblings diverged from it and no test could catch them (value tests assert `evaluate(optimum) ≈ f*` — evaluating *at* a point says nothing about *reaching* it). **Decision: `bounds() -> (f64, f64)` returns the recommended per-coordinate *search box*, not the mathematical domain.** It is the search box because that is the only thing its consumers can hold: every EA config in `rlevo-evolution` (~20 of them, 52 fields) carries a **single** scalar `rlevo_core::bounds::Bounds` applied to every coordinate via `clamp_vec` (**ADR 0027**), so a per-axis search *rectangle* is **not representable** — and making one representable is out of scope. A conforming box satisfies **two obligations**, each pinned by an in-source test: **O1 reachability** (contains every certified optimum on *every* coordinate) and **O2 no spurious optimum** (contains no point with `f < f*` — discharged **analytically** in a code comment, regression-tested by a deterministic dense grid sweep). Two **equally legitimate** conforming shapes, neither an exception to the other: the **square hull** of an asymmetric domain (Branin `(-5,15)`, Trefethen `(-6.5,6.5)`, Bukin6 `(-15,3)` — a hull *does* admit points outside the published rectangle, which O2 certifies as harmless), and a **cited reduced range** of an impractically large canonical domain (RosenbrockFlat `(-30,30)`, reduced after Chen 1997 from the canonical `[-2000,2000]ⁿ` of Monismith 2010; Six-Hump Camel `(-2,2)`, reduced from Al-Roomi's canonical `[-5,5]²`). The true per-axis domain stays **documentation only** (module `# Domain` section + citation): **no `domain()` accessor and no per-axis renderer ship here** — after the O1 fix the square box *contains* every landscape's full true domain, so the renderer no longer clips anything (a per-axis one would only trim excess area, cosmetic), and shipping one would introduce a **second rectangle convention colliding with the planned candidate overlay** (`render/payload.rs` documents `Landscape2DSnapshot`'s `bounds_x`/`bounds_y` as the **search domain**, and candidate markers live in search-box space, so a true-domain ASCII frame and a search-box report frame would disagree about the same landscape). **Costs:** results on Branin/Trefethen/Bukin6/Six-Hump Camel are over the hull / reduced range, **not the literature rectangle**, and are **not directly comparable to published results** that search the rectangle; widening a box is a **research hazard in general** (it could admit a point better than `f*`, silently invalidating every result on that landscape — O2 is the guard, and both widenings here are *provably* safe: Branin's `f* = 10/(8π)` is the infimum over all ℝ², and any point beating Trefethen's `f*` must lie within radius ≈0.817 of the origin); showcase `mutation_sigma` constants are **absolute, not box-relative**, so changing a box detunes that landscape's demo. **Open:** `bounds()` is deliberately **not on the `Landscape` trait**, so **nothing enforces that a search runs inside the landscape's box** — the examples wire it by hand, 23 times, which is exactly why a wrong constant was invisible; that missing chokepoint is the natural thing for a future constrained-objective design to close. If per-axis *search* boxes ever become representable this decision is revisited — and that is the moment to design a per-axis type, which must not be pre-empted by a render-only one. |
| [0046](0046-slot-newtype-replaces-option-take-around-learn-step.md) | Resolve #167 (`Option::take()` on target/policy nets bricks the agent on any mid-learn-step panic) across all eight gradient-based agents (`dqn`, `c51`, `qrdqn`, `ddpg`, `td3`, `sac`, `ppo`, `ppg`). Replaces the 17-site `Option<M>` + whole-learn-step `take()`/`expect()` idiom with `pub(crate) struct Slot<M>(Option<M>)` in `algorithms/shared.rs`: `forward`/loss/`backward`/`GradientsParams::from_grads` now run against a `Slot::get()` borrow, so the module is only ever moved out for the single `Optimizer::step` call inside `Slot::step_with`. Records **why an RAII drop-guard and `catch_unwind` cannot close this window** — Burn's `Optimizer::step(&mut self, lr, module: M, grads) -> M` consumes the module by value (verified `burn-optim-0.21.0/src/optim/base.rs:77`), so a panic inside `step` drops the module in the callee's frame before any caller-side guard or unwind handler could recover it; the only real fix (cloning weights every step) is rejected as a hot-path cost against an already-fatal failure. The residual in-`step` panic window is accepted by design and now poisons the `Slot` with an honest, actionable message instead of bricking the agent silently. Also fixes a latent multi-field bug TD3/SAC shared (stepping both critics from one wide window meant a `critic_1` panic destroyed `critic_2` too; the two `step_with` calls are now disjoint) and rejects `std::mem::take` on a `Default` module (`#[derive(Module)]` generates no `Default`; even if it did, it would convert a loud panic into silent training corruption). Verified behaviour-preserving via seeded A/B runs bit-identical to pre-fix `HEAD` across all eight agents. |
| [0047](0047-sensor-relocates-emission-model-to-environment.md) | **(§5 / decision 5 superseded by 0063.)** **Supersedes 0019; annotates 0039.** Resolve #329: relocate the POMDP emission model `O` off `State` to a new env-side `Sensor<OR, AR, SR>` trait with the canonical signature `O(a, s')` (plus an `observe_reset(&s)` companion for the actionless initial observation). Removes `State::observe` + `State::Observation` — a `State` is now just a point in state space (`shape`/`numel`/`RANK`/`is_valid`). `&self = env` gives sensors direct world access, retiring ADR 0039's cached-sensor hack (BipedalWalker lidar / CarRacing pixels are read from the world, not stashed on the state) and the rank inflation that forced CarRacing to `Environment<3,3,1>`. `Observable<OR>` is **demoted, not deleted** — an optional pure-projection helper a `Sensor` may delegate to (`pixel_grid` is the reference). Env-implements-`Sensor` (YAGNI; env-holds is a recorded two-way door). The 11-env grid family is the one documented exception: `GridState: Observable<3>` + the shared `build_snapshot` chokepoint instead of 11 identical impls (`GoToDoorEnv` impls `Sensor` for its env-owned mission). `BeliefState` repointed to its own `type Observation: Observation<OR>` (mirrors `HiddenState`/`LatentState`; no impls exist). **Breaking** (alpha), migrated atomically. Deferred: State's residual tensor machinery, CarRacing's honest `<3,1,1>` re-model (#255). |
| [0048](0048-partial-episode-bootstrapping-in-gae.md) | Adopt **partial-episode bootstrapping** (Pardo et al. 2018 Eq. 6) in on-policy GAE, reversing the CleanRL-matching tradeoff documented in `ppo/rollout.rs` — a deliberate fidelity divergence, not a silent bugfix. `rlevo`'s `TimeLimit` is an opt-in *wrapper*, so every env is Pardo's *time-unlimited* case (ii) where a timeout must bootstrap, not terminate. At a truncated step the **delta bootstraps** `V(s_continuation)` while the **λ-recursion is cut** — two distinct masks a single `next_nonterminal` factor cannot express. The missing datum is a **value, not a mask**: `ppo/train.rs` discards `next_snapshot` before its value is ever computed (the value net runs exactly `num_steps + 1` times per rollout), so #170's "infrastructure pre-built" premise is refuted. Chooses the **two-mask** form over SB3's algebraically-identical reward folding, because folding makes the stored `rewards` array mean something other than the environment's reward (safe *today* only by the accident that no `rewards()` accessor exists) and relocates the rule from a purely unit-testable pure function into the untestable training loop; both forms need the same new `V(s_cont)` plumbing, so the choice is only about *where* the correction lands. `truncated: Vec<bool>` → **`truncation_value: Vec<Option<f32>>`** makes a truncated-step-without-a-bootstrap unrepresentable (ADR 0026/0027/0039/0046 posture; a zero-filled `&[f32]` would reproduce the exact bug). Fixes the **entangled off-by-one** in the same PR as its own first commit — `compute_gae` read `terminated[t+1]` while the `t == n-1` branch used `last_done` on the `[t]` convention, mis-timing the cut for **terminated** episodes too (wider blast radius than the truncation bias); PEB forces the `[t]` convention, so the two are separable as *defects* but not *in the code*. `last_done` is **deleted**, not fixed (redundant once every step's own status is read at `[t]`). `record_step` gains the continuation observation and the **agent** computes `V(s_cont)` iff `Truncated` (one extra forward per truncation, zero otherwise). **Breaking, no shim** (alpha) at `compute_gae`, `RolloutBuffer::{push_step, finish}`, `{Ppo,Ppg}Agent::record_step` — a `#[deprecated]` shim would signal scheduled removal, not numerical incorrectness. PPG inherits the fix via the shared buffer and `returns()`. Every seeded PPO/PPG baseline moves and is no longer CleanRL-comparable. Records the **residual GAE weighting bias** (Doering et al. 2026 — geometric-weight mass collapse at episode boundaries) as an explicit, deliberate **non-decision**: live research, not an agreed fix. |
| [0049](0049-ppo-gaussian-log-std-is-bounded.md) | Clamp PPO's continuous Gaussian head's state-independent `log_std` `Param` to a configurable `[log_std_min, log_std_max]` (default `[−20, 2]`, matching SAC's ceiling), fields living on `TanhGaussianPolicyHeadConfig` and `validate()`-checked for both ordering and a span `< 40` (the derived overflow threshold: past a span of `≈44`, `((z−μ)/σ)²` between the σ that sampled a buffered `z` and the σ evaluating it overflows f32). The reachable failure is **absolute underflow** — the surrogate objective's gradient on `log_std` is `≈ −1` per high-advantage update, driving `σ → 0` in f32 (`~290k` updates at the Pendulum bench's `lr=3e-4`) and NaN-poisoning the weights; the entropy bonus is `≈300×` too weak to oppose it at the default `entropy_coef=0.01`, and is `0` in the crate's one reference-faithful continuous-control benchmark (SB3 rl-zoo Pendulum-v1 / Schulman et al. 2017 Table 3 both omit it). States plainly that this is a **deviation from reference PPO** (TRPO/PPO papers, CleanRL, SB3, Spinning Up, Huang et al. 2022 all leave `log_std` unclamped; SB3 clamps only SAC), licensed by Andrychowicz et al. 2021's finding that unbounded exponentiation "occasionally produced NaN values" and that a floor "matters little, if it is not set too large" — numerical totality, not a claimed training-stability win. Because `log_std` is a **shared `Param`, not a per-step network output**, the SAC clamp analogy actively misleads: crossing the bound zeroes its gradient *permanently* (no recovery path, unlike SAC's `Linear` head), so a loud `NaN` becomes a silently-pinned `σ≈2e-9` policy — clamp ships with telemetry (min-`log_std` stat + one-shot `tracing::warn!`) in the same change, not as a follow-up. Bounds live on the **policy-head** config, following the same convention issue #185 names for SAC's dead `SacTrainingConfig` duplicate fields (the live clamp is on `SacPolicyConfig`). Breaking (two new required fields, no `Default`); PPG is discrete-only in v1 and unaffected, but a future continuous PPG head inherits this decision. Rejects a softplus/`min_std` soft floor (perturbs the *whole* operating region vs. a non-binding clamp perturbing nothing in-distribution), `Option`-able bounds (`None` = opt into NaN), and warn-only (weights already poisoned by the time it's observable). |
| [0050](0050-replay-strategy-seam.md) | **Corrects ADR 0003 §Context row 1.** Resolve #188 by introducing a `ReplayStrategy<T>` seam in a new `crate::replay` module and **rebuilding** PER rather than finishing it. Records that 0003's claim that `memory.rs` is "consumed by `rlevo-reinforcement-learning` only (8 algorithms…)" is **false**: the six off-policy agents import only `ReplayBufferError`, `TrainingBatch` has **zero** consumers, and the sole live caller of the builder is `crates/rlevo/tests/integration_test.rs:304` (0003's *decision* is unaffected — the true consumer count is further from the ADR 0002 bar, not closer). The six hand-rolled `VecDeque<Transition<O>>` buffers collapse onto one `Transition<O, P>` generic over the **erased** action payload (`DiscreteTransition<O> = Transition<O, usize>`, `ContinuousTransition<O> = Transition<O, Vec<f32>>`), explicitly **preserving** the erasure `sac_agent.rs:112-115` documents (storing a typed `A: Action<AD>` reimposes the `Clone + 'static` bound all six deliberately avoided) — so reconciling onto `ExperienceTuple` is rejected, and `experience.rs` is left **untouched** under 0003's conservative dead-code policy (deletion is #190's call). The existing `PrioritizedExperienceReplay` is **removed, not evolved**, on four independent defects: it has no `update_priorities` (so it is not PER), it samples from an internal `rand::rng()` (unseedable, contra every agent's `rng: &mut R`), its `TrainingBatch` float one-hot action tensor cannot express DQN's `Int` `gather` index, and it draws *without* replacement where all six agents draw *with* — no `#[deprecated]` shim despite shipping in v0.1.0–v0.3.1, per the ADR 0028/0030/0038/0042/0048 alpha precedent and 0048's "a shim signals scheduled removal, not incorrectness" (stronger here: the type does not do what its name says). `TrainingBatch` is **retired, not extended** — IS weights ride on the sample handle because the weight is consumed at the *loss* site, not the staging site, and each agent stages differently; no persisted-data migration exists (nothing in the replay path derives serde). **Uniform and prioritized are two impls behind one trait, not an `α = 0` degenerate case**: Schaul Eq. 1's unification is mathematical, and a single impl pays O(n) distribution construction per learn step at capacities to 10⁶ against O(k) draws — but the *sufficient* reason is that uniform draws i.i.d. **with** replacement while Schaul's scheme is **stratified without** it (Appendix B.2.1), so one impl cannot honour both. `UniformReplay::sample`'s draw order (exactly `batch_size` in-order `rng.random_range(0..len)` calls) is a **pinned contract**, not an implementation detail — it is what makes the migration a bit-identical no-op and what stops a later "optimization" silently moving every seeded baseline. The index handle is an owned, generation-stamped `TransitionId(u64)` (absolute id = `pushes - len + i`; an evicted id resolves to `None` instead of aliasing a live transition — one add per insert, bought against the roadmap's distributed-replay item) and is **deliberately not** an ADR 0046 `Slot`: `Slot` bounds a Burn module's ownership window across a by-value `Optimizer::step`, `SampledBatch` carries sample *identity*; merging gives `Slot` a second reason to change. **β-annealing and max-normalization are one knob, not two** (Appendix B.2.1) — the buffer computes and max-normalizes `w_i` in the same expression (no unnormalized weights are obtainable) while the *schedule* lives on the agent config, because a buffer-owned step counter duplicates the agent's (rules §10 second-source-of-truth) and breaks the moment two learners share a buffer. Names avoid the `alpha` collision with SAC's entropy temperature: `priority_exponent` / `importance_exponent` / `priority_epsilon`. A validated `Priority` newtype (`finite && > 0`, ADR 0027/0031 shape) makes the reachable `NaN`-TD-error → `NaN`-probability defect unrepresentable. PER wires into the **value-based three only** on the literature (Rainbow: 2-of-7 most crucial, 53/57 games; vs. Panahi RLJ 2024 "none of the prioritized variants consistently outperform uniform replay" in control and Saglam JAIR 2022's actor-gradient mechanism); DDPG/TD3/SAC get the seam, keep uniform, and their loss sites are **not** restructured. Two fidelity findings recorded: **C51's priority must be KL, not the existing cross-entropy** — they differ by `H(target)`, which is θ-constant but **varies per sample**, so `c51/loss.rs`'s CE is not Rainbow's priority; and **QR-DQN's priority is uncited**, an extrapolation from Rainbow's principle to a case Dabney et al. explicitly decline ("we compare the pure versions… without these additions"), shipped opt-in and labelled as analogy. Verified against `burn-nn-0.21.0/src/loss/huber.rs:86-98` that `forward(.., Reduction::Mean)` is *literally* `forward_no_reduction(..).mean()`, so the whole per-sample loss restructure is **bit-identical at `w ≡ 1`** — which is why the seam migration and the loss restructure can land as separate independently-verified no-ops. Also rejects an `enum ActionPayload` (runtime branch on the staging hot path + an unrepresentable-state hole), and rejects designing a HER `relabel` hook now: Andrychowicz et al. Algorithm 1 **stores additional relabeled transitions** rather than overwriting stored ones, so HER's binding demand is ordinary `push` plus a goal-conditioned `O` (a data-model change no seam shape prevents) — `get_mut` is kept on PER's `update_priorities`, a *present* instance of post-insertion mutation, not on HER's speculative one. |
| [0051](0051-replay-kind-dispatch-and-validated-importance-exponent.md) | **Corrects ADR 0050 §3, §11, and §13** mid-implementation, between its steps 3 and 4; 0050's decision stands. §3's trait sketch is **superseded**: `update_priorities` is **not** a defaulted `ReplayStrategy` method but **inherent on `PrioritizedReplay`**, dispatched through a new `enum ReplayKind<T> { Uniform, Prioritized }` that itself implements `ReplayStrategy<T>` — because a defaulted no-op lets a third strategy silently swallow a priority writeback, which is *the same* write-once defect (0050 §Context defect 1) that removed the old `PrioritizedExperienceReplay` one PR earlier, whereas an exhaustive `match` makes it a **compile error**: when HER lands, `ReplayKind` fails to build until someone decides what a hindsight buffer does with a writeback. Rejects a generic strategy parameter (makes PER a **type-level** choice, contradicting 0050's config-level opt-in — a type parameter cannot be flipped by a deserialized field) and a swapped concrete field (lands as either an `Option` pair, reintroducing the exact hole ADR 0046 removed, or two `learn_step` bodies that drift). Accepted cost: `ReplayKind` is a **closed set**, so no out-of-crate strategy plugs into an agent — no such consumer exists, `Slot`'s `pub(crate)` is precedent, and it is a two-way door (`ReplayStrategy<T>` stays public; only the *agent's* dispatch closes). §3/§11's `beta: f32` is **superseded** by `beta: ImportanceExponent` (`finite && [0,1]`, `new`/`try_new` + `Copy` error + `const get` + a named `ONE` for the annealed endpoint, mirroring `Priority` and through it ADR 0027/0031) — deliberately **not** a `Result` return, since `UniformReplay` ignores β and a fallible signature forces it to carry an unconstructible variant, the same "meaningless for half the callers" shape that retired `TrainingBatch` in 0050 §7. §11's accepted cost ("a caller can pass a nonsense β; `Validate` on the config is the mitigation") is **WITHDRAWN AS UNSOUND**, not merely premature: a config holds schedule *endpoints* while what reaches `powf` is the *evaluated* interpolation, and `beta_anneal_steps == 0` yields `0/0` = `NaN` **though every endpoint validates**. Implementation sharpened the argument — whether that `NaN` survives turns on an incidental IEEE-754 detail (`f32::min` returns the non-`NaN` operand and silently **launders** it to `1.0`; `clamp` and `if x > 1.0` both propagate), so the zero-length schedule is *masked*, not absent, and a refactor between three interchangeable-looking spellings decides whether the buffer eats a `NaN`. The old guard was a `debug_assert!` — absent in exactly the release builds where `~10⁵`-step runs happen — and the consequence is #188's own NaN shape: `NaN` weights → `NaN` loss → poisoned gradients, silently. Three guards ship together, none sufficient alone: `beta_anneal_steps` gains a `nonzero` check, the evaluator **clamps** before construction, and the newtype turns any residual bug into a **loud panic at a named construction site** rather than gradient poisoning twenty steps downstream. Also records that ADR 0050:394's `categorical_cross_entropy` is a **stale symbol** (renamed `categorical_cross_entropy_per_sample`, `c51/loss.rs:37`, by 0050's own step 2) — the CE-is-not-KL *finding* is unaffected. The β retype is a strict behavioural no-op: no RNG draw moves, no arithmetic changes, 0050 §5's pinned draw order untouched. |
| [0052](0052-hostrow-supertrait-splits-layout-from-backend.md) | **Extends ADR 0028; does not supersede it** — every 0028 decision (required row-writer, derived `to_tensor`, hand-written `from_tensor`, rewards bypassing `stack_to_tensor`, the `BR = R + 1` chokepoint, the Burn reuse-or-reject verdict) survives verbatim; only *which trait holds the row methods* changes. Splits `TensorConvertible<R, B>` into a backend-independent supertrait `HostRow<R>` (`row_shape` + `write_host_row`) and a slimmed `TensorConvertible<R, B>: HostRow<R> + Sized` (`to_tensor` + `from_tensor`); every impl split in the same PR — 35 impl blocks plus 8 rustdoc examples, across 6 crates. **The justification is type-safety, not ergonomics.** 0028 gave `write_host_row` an explicitly backend-independent contract ("push plain `f32`, do not pre-convert to `B::FloatElem`") that the type system did **not** enforce — the method lived on a `B`-parameterised trait, so a backend-specialised body was permitted by the signature and forbidden only by prose. Under the off-policy agents' dual bound `O: TensorConvertible<DO, B> + TensorConvertible<DO, B::InnerBackend>` an unqualified call is **E0284** (verified, rustc 1.97.0), so six staging sites carried `<O as TensorConvertible<DO, B::InnerBackend>>::write_host_row(..)` — naming a backend *provably irrelevant to the result*, obliging every reader to re-derive that irrelevance from docs; two engineers independently picked **different** irrelevant backends. Had a backend-specialised row-writer ever been written, the two spellings would push **different bytes** into the staging buffer **silently** — 0028's `debug_assert` checks the row's *length*, and the defect is in its *contents*. The split makes it unrepresentable: `HostRow` has no `B`. Same invariants-in-types move as ADR 0027 (`Bounds`), 0031 (rate newtypes), 0046 (`Slot`), applied to a trait boundary rather than a value. Explicitly **rejects the weak "`stack_to_tensor` had zero call sites due to friction" framing as post-hoc storytelling** — its only nominal consumer, `memory.rs::sample_batch`, was dead code (#188) deliberately retired by ADR 0050. Resolution mechanism is a **shared supertrait obligation**: both bounds elaborate to the *same* `HostRow<DO>` obligation with `B` projected away, so it does **not** relocate the ambiguity to `R` (`R` is pinned by the enclosing signature at every site); all six turbofish deleted. Residual hazard recorded explicitly: one type implementing `HostRow` at **two ranks** reintroduces ambiguity (**E0283**, plus E0284 on the const) at concrete call sites — zero such types today, mitigated by a documented `docs/rules.md` invariant ("a domain type has exactly one row rank"), and accepted because the failure is a **loud compile error, never silent**, fixable with the *same* turbofish that existed before, so the downside case is no worse than the status quo. `HostRow` deliberately carries **no** `Sized` (neither method needs it) and is deliberately not `dyn`-compatible (`row_shape` has no receiver — as `TensorConvertible` was not before); adding `where Self: Sized` to `row_shape` would buy dyn-compatibility later, purely additively. **Normative #201 note:** the split makes the deferred inverse seam *cleaner* (`from_host_row` belongs on `HostRow`, `from_tensor_batch` beside `to_tensor`), **but** `from_host_row -> Self` requires `Sized` — whoever implements #201 must put `where Self: Sized` on **that one method**, not restore it to the trait, or they will silently undo this ADR's §4 reasoning. `stack_to_tensor`'s bound relaxes to `T: HostRow<R>` (staging never decodes; `B` names only the upload target). `Observation<R>: HostRow<R>` is now **expressible** for the first time (`Observation<R>` has no `B`, so it could never name `TensorConvertible`) but is **deferred** to a follow-up: 12 `Observation` types have no `TensorConvertible` impl and choosing a discrete/tabular row layout (one-hot vs. ordinal-as-float, bearing on 0028 §4's 2²⁴ exactness limit) is a design question with learning consequences, not a mechanical migration — and the supertrait alone would still **not** force `shape() == row_shape()`. Migration trap recorded: `impl<B: Backend> HostRow<1> for Foo` is **E0207** (`B` unconstrained), so each `HostRow` half must drop `B`. Rejects do-nothing (leaves the contract as unenforced prose and the silent-divergence hazard live), an associated const `RANK` (**dead on stable** — `[usize; Self::RANK]` needs `generic_const_exprs`; verified), a free fn `write_host_row_of::<R, T>` (treats the call-site symptom, not the signature cause), and giving `write_host_row` a `B`-mentioning signature (**actively harmful** — contradicts the method's reason for existing and reintroduces the per-item device coupling 0028 removed). **No performance change of any kind** — not one byte moves differently; type-safety and ergonomics only. |
| [0053](0053-bounded-action-per-component-bounds.md) | **`BoundedAction::low()`/`high()` return `&'static [f32]`, not `[f32; R]`** — discharges the follow-up ADR 0038 named in its "Explicitly out of scope" section (issue #253). Bounds were keyed on tensor **rank**, so a rank-1 multi-component action could express only **one** bound for all `C` components, contradicting the methods' own "per-component" doc. Latent only because all 5 current impls have `R == COMPONENTS`; fires on the first of the 5 blocked multi-component actions (`BipedalWalker` C=4, `CarRacing` C=3, `LunarLanderContinuous`/`Reacher`/`Swimmer` C=2). `[f32; Self::COMPONENTS]` re-verified **dead on stable** (`rustc 1.97.0`: "generic parameters may not be used in const operations"), so a real choice was required. Rejects the **second const generic** `BoundedAction<R, C>` on the decisive ground that its apparent compile-time win is **illusory**: `C == Self::COMPONENTS` is not expressible on stable, so the invariant that actually matters falls back to the *same* contract test as the slice option — while costing a trait-arity change, all 12 `A: BoundedAction<DA>` plumbing sites, and a breaking arity change on both public export paths, with an identically silent failure mode. Chooses `&'static [f32]` over `Vec`/`Box<[f32]>` (no hot-path allocation, `Copy`, agents store the slice directly) and over `&'static [Bounds]` (ADR 0027) because every consumer wants two **parallel** slices — `exploration.rs::apply(mean, low, high)` already takes `&[f32]` twice and is already per-component correct, making the slice-shaped trait the *smaller* change. Trait arity is **unchanged** (`R` retained solely to name the supertrait), so all 12 bound sites compile verbatim. Deletes the trait's **false** doc claim that bounds may derive from "a runtime env config" — `low()`/`high()` are static, take no `self`, and cannot reach any instance. Also fixes a **second defect neither #253 nor 0038 named**: `ddpg_agent.rs:473-474` / `td3_agent.rs:521-522` collapse bounds to `low[0]/high[0]` for the target-action clip (citing CleanRL), which for `CarRacing`'s `Box([-1,0,0],[1,1,1])` would admit **negative gas and brake** — exactly the "impossible actions" TD3 Eq. 14 clips against the `Box` *vectors* to prevent. Fixed **in scope, not deferred**, because the deferral would have to *gate* which impls may land; Burn 0.21 `clamp` is scalar-only, so per-component clipping uses `max_pair`/`min_pair` against `[1, C]` bound tensors cached at agent construction (TD3's `noise_clip` stays scalar — it bounds noise, not the action space). All `A::RANK`-keyed action loops migrate to `A::COMPONENTS`. The 5 blocked impls land as the **final** step: `CarRacingAction` is the only action whose components disagree on bounds and is therefore the sole regression witness for the target-clip half. Answers 0038's deferred **Convention A/B** question: B stays (retiring it buys no correctness now that nothing derives component count from `shape()`); instead `Action::shape()` docs gain the prohibition. Compile-time `low`/`high` length agreement is traded for a contract test plus a construction-time `assert_eq!` in each agent. SAC target entropy is **already** `-(A::COMPONENTS as f32)` (commit `e0ce1cd`) and is not re-litigated. |
| [0054](0054-policy-head-construction-is-fallible.md) | **Policy-head construction is fallible.** Every policy-head config in `rlevo-reinforcement-learning` (`SquashedGaussianPolicyHeadConfig`, `TanhGaussianPolicyHeadConfig`, and both discrete `CategoricalPolicyHeadConfig`s) exposes exactly one public constructor, `try_init<B: Backend>(&self, device) -> Result<_, ConfigError>`, whose first statement is `self.validate()?`; the infallible `init()` is **removed**, not kept alongside — resolves #386, where `Validate` was implemented on all four head configs but never called on any production path (only training configs were validated; every head-config `.validate()` call was `#[cfg(test)]`-only). The failure was **backend-divergent**: an inverted `log_std_min > log_std_max` silently pins `σ` to a constant under `Autodiff<Flex>`'s default `float_clamp`, but panics under raw `Flex`'s `core::f32::clamp` override — same op, same bad config, opposite outcomes. Applies uniformly to the discrete heads too, so "does this head validate?" is never a per-head question. The two Gaussian configs additionally fold `log_std_min`/`log_std_max` into a single `log_std: Bounds` field (ADR 0027), making the inverted range unrepresentable, not merely rejected — though `Bounds` does **not** subsume `validate()`: ADR 0049's floor/span checks stay, since `log_std_min = -120, log_std_max = -100` is a well-ordered `Bounds` that still reaches `NaN`. Named `try_init`, deliberately not `init() -> Result<..>` — Burn's own `*Config::init` idiom (~30 `burn-nn` instances) is infallible everywhere, and the ecosystem owns the meaning of `init`, not of `try_init`. 31 call sites migrated (10 outside the RL crate, 21 inside it, all `#[cfg(test)]`); zero non-test call sites existed. Rejects a panicking `init()` that `.expect()`s `validate()` — ADR 0026 already rejected that shape by name, and while none of the four configs derives `Deserialize` *today* (narrowing 0026's stated trigger), `Bounds` itself is `Deserialize`, so the gap is one derive away from a silent §4 violation. Rejects agent constructors building the head internally — they are generic over **open** policy traits, so welding a `BuildableFrom<Cfg>` bound onto the behavioural trait would repeat exactly what ADR 0011 removed from `Environment`. Defers, does not reject, a `PolicyHeadConfig` trait carrying `try_init` — no generic consumer exists yet; revisit if a fifth head appears. Extends ADR 0026/0027; constrained by ADR 0011; preserves ADR 0049. Records the honest cost plainly: rlevo's policy heads become the one place Burn's `Config → init(device) → Module` chain does not compose, a permanent tax against a failure mode with zero occurrences in the repo. Related: #185/#385 removed the same dead bounds from the *training*-config duplicates, making this gap visible without creating it. |
| [0055](0055-config-invariant-enforcement-allocation.md) | **Resolves #326 by refuting its premise, not by adopting its remedies** — and makes an already-implicit allocation explicit. `*Config` types **keep `pub` fields**; validation is the **consumer's** obligation at construction (ADR 0026); `*State`/`*Params`/`*Genome` encapsulate (`rules.md §2`); any invariant that must survive struct-literal construction is encoded in a **validated newtype** (ADR 0027/0031), not enforced by hiding the field; `#[non_exhaustive]` is reserved for **enums**. #326's observation is accurate — **71** `pub struct *Config` (environments 34, evolution 20, RL 13, benchmarks 4; ADR 0026's `87` has drifted, and 0026 is immutable) with 100% `pub` fields — but its *inference* that this is a bypass is refuted by ADR 0026 §3, which already names "fields set via struct-literal / `Default`-update / `Deserialize` where there is no guarded setter call site" as **in scope and routed to `validate()`**: the `pub` fields are 0026's premise, not a violation of it. The chokepoints 0026 deferred are **built** — environments **34/34** `with_config → validate()?`, RL agents **8/8** (`dqn:178`, `c51:165`, `qrdqn:165`, `ddpg:227`, `td3:254`, `sac:264`, `ppo:255`, `ppg:254`), policy heads 4/4 inside `try_init` (ADR 0054), and evolution covered **wholesale** by the generic bound 0026 promised (`strategy.rs:505-517`, `pub fn new(..) -> Result<Self, ConfigError> where S::Params: Validate { params.validate()?; .. }`), with 8 strategies carrying an identically-shaped `debug_assert!(params.validate().is_ok(), ..)` behind it as *defence in depth*, not oversight. **The key insight #326 missed: a `pub` field of a validated newtype is safe under struct-literal construction** — `GaConfig { bounds: Bounds, mutation_sigma: NonNegativeRate }` (`ga.rs:73-89`) cannot be given an out-of-range value *at all*, which is why its `Validate` impl (`ga.rs:113-117`) documents that no scalar checks are needed; privacy on the **container** is the wrong lever, privacy on the **value** is the right one, and it is **transitive** (holds after `Deserialize`, after `Clone`-then-mutate, in a literal) where field privacy guards one boundary. So the real audit question is "which `pub` field carries an unguarded invariant?", not "how many `pub` fields?". Rejects **private fields + accessors** (churns 71 configs and destroys the `Config { lr: 3e-4, ..Default::default() }` idiom a research library exists to serve, to guard invariants the newtypes already make unrepresentable); rejects **`#[non_exhaustive]` on structs** on four independent grounds — breaks **24 cross-crate struct-literal sites across 19 files** incl. the published user-book example `ch01_sphere_ga.rs:37`/`:87`; **does not preserve the escape hatch people assume**, since it forbids cross-crate functional-update syntax so `..Default::default()` also stops compiling; buys **zero** validation guarantee (the builder's `build()` must still call `validate()` — identical work to 0026 adoption *plus* a breaking change); and `GaConfig` + the four policy-head configs **have no builder to migrate to**. Records that **41** `#[non_exhaustive]` sites exist workspace-wide and **zero** are on a struct. Refutes #326's "validating at point of use scatters" empirically: a config is consumed **by value at construction**, not read field-by-field at runtime, so there is ~**one chokepoint per config type** and ~95% are already written; the one genuine read-without-construction case is answered by **totality**, per the reframing already applied in-tree citing #326 by number (`c51_config.rs:120-130` — `delta_z()` returns `f32::NAN` for degenerate `num_atoms < 2` "so this stays total rather than panicking"). Honestly records three residual costs: **TOCTOU is real and unfixed** (`pub` + `Clone` permits validate → mutate → use; consumption-validation cannot close it, only newtypes can — accepted because configs move by value at construction so the window needs deliberate effort); **read-before-validate is a footgun template** (`bipedal_walker/env.rs:106-110` reads `config.terrain` before `Self::build` validates — harmless today, `terrain` being an invariant-free enum, but the convention is now **validate first, destructure second**); and **the `Deserialize` obligation is dormant, not satisfied** — ~22 configs derive `Deserialize` plainly and `bincode::serde::decode_from_slice::<GoToDoorConfig>` provably accepts `size: 1` below `MIN_SIZE = 5`, violating nothing today **only because no config loader exists anywhere in the workspace**, so the obligation is live and unowned by whoever adds the first one. Zero code change; reconciles `docs/rules.md §2` (the `*Config` exemption was previously inferable only by noticing `*Config`'s **absence from a list** — the weakest form a rule can take, and the reason a competent reviewer filed #326 in the first place). |
| [0056](0056-non-finite-loss-skip-and-warn-guard.md) | **Non-finite loss ⇒ skip the step + warn once, keyed on the already-host-resident loss scalar.** Resolves #318 (NaN propagates silently through training instead of surfacing) by **generalizing the SAC-α guard (#184)** from one hand-rolled optimizer to every agent's learn step. Every agent already reads its loss scalar host-side (`into_scalar().elem::<f32>()`) *before* `backward()` for metrics, so a `FiniteLossGuard` (`algorithms/shared.rs`) checking `f32::is_finite()` on that value adds **no** device→host sync — the issue's own "per-step sync may be too costly" caveat is a false premise at these sites. On non-finite the caller **skips `backward()` and the optimizer step**; the skip re-fires **every** occurrence (latching it would silently corrupt weights on the second NaN — the one correctness point the literature review flagged) while only the `tracing::warn!` is one-shot, **per loss site** (one failure mode must not silence another's diagnostic, per #184's three-latch precedent). Runs **unconditionally in release**. Skipped values are excluded from epoch-mean accumulators (`denom==0` guarded) so a single NaN cannot re-poison an otherwise-finite reported mean; the `warn!`, not a NaN metric, is the surfacing mechanism (matches #184, which added no stats field). Sits strictly **before** ADR 0046's `Slot` take/restore window, which it therefore never even enters on a skip. **17 latches across 8 agents** — PPO (policy, value), PPG (policy, value, aux-value, aux-total; aux-total input host-*derived* from its two already-read summands), DQN, C51, QR-DQN, SAC (critic_1, critic_2, actor — α keeps its own #184 guard), DDPG (critic, actor), TD3 (critic_1, critic_2, actor). **Skip-and-continue is the canonical response shape** — PyTorch AMP `GradScaler` skips `optimizer.step()` on non-finite grads and continues; origin Micikevicius et al., *Mixed Precision Training*, ICLR 2018. Two honestly-recorded deviations from that precedent: the trigger is a pre-backward **loss** scalar, not a post-backward **gradient** check, making this a **proxy** that fully prevents *loss-origin* NaN (the dominant PPO/SAC/DQN mode) but is blind to *finite-loss → NaN-gradient* (`torch.where`/masked `0·∞` backward; PyTorch #16317/#52248/#156212), which it catches one step late and can only surface-and-stop — that gradient-origin case is **#328**'s territory (grad-norm clipping being itself *not* a NaN guard, since a NaN element makes the norm NaN). **Out of scope, deliberately:** reward finiteness at `remember` (#352), grad-finiteness/grad-norm (#328), a PPO `log_ratio` clamp before `exp` (non-canonical — no reference PPO impl clamps it, and `min(unclamped, clamped)` masks `Inf` but not `NaN`). Provenance and full citations: `docs/.private/research/2026-07-21-issue-318-nonfinite-loss-guard.md`. |
| [0057](0057-target-soft-update-path-is-fallible.md) | **The target soft-update path is fallible — `PolyakError`, `soft_update`, and off-policy `learn_step` return `Result`.** Resolves #341 (`polyak_update` panicked on a `ParamId` mismatch, double-consumed tied weights, and silently dropped a strict-subset leftover — a real tied-weights module hits all three) and **partially** #317. Introduces `PolyakError` (`utils.rs`, `Copy + PartialEq`, `thiserror`) with `MissingActive(ParamId)` (a target param absent from active) and `MissingTarget(ParamId)` (an active param no target field consumed, reported as the **smallest** leftover id via `ParamId: Ord` for determinism). `polyak_update → Result<M, PolyakError>` — the infallible `ModuleMapper` records the first miss and returns the param untouched, and the fn surfaces it after the walk. `soft_update → Result<_, PolyakError>` on all five model-trait decls (`DqnModel`/`C51Model`/`QrDqnModel` + DDPG actor/critic; SAC/TD3 reuse the DDPG traits); `learn_step → Result<Option<LearnOutcome>, XAgentError>` on the six off-policy agents (DQN, C51, QR-DQN, DDPG, TD3, SAC), each error enum gaining one `#[error(transparent)] Polyak(#[from] PolyakError)`; call sites use `?`. `Ok(None)` still means "step skipped" (ADR 0056). The retained per-site `.clone()` means an `Err` `?`-returns *before* the target field is reassigned, so the field keeps its prior weights — no silent hard-sync, the same invariant a panic gave, now via early return. **Why `Result` over panic:** a `ParamId`-topology mismatch is a **recoverable config error** (rebuild target by cloning active), consistent with ADR 0056's skip-don't-crash posture. **References, does not supersede, ADR 0046**, which rejected `Result` on `learn_step` *as a panic-safety mechanism for #167* and explicitly deferred this change to its own issue (0046 lines ~251-257) — this is that issue; the two are orthogonal. Breaking API change (callers add `?`/double-unwrap; no persisted data affected); the three #341 topology tests move from `#[should_panic]` to exact `assert_eq!` on variant + id. **Out of scope (residual #317):** `act()` and on-policy PPO/PPG stay panic-based; the device→host→device round-trip (#322) untouched. |
| [0058](0058-target-update-type-unifies-cadence-and-tau.md) | **τ and cadence are one target-update mechanism — `TargetUpdate` is its type.** Resolves #334, but **corrects the issue's own premise** to a three-way divergence, not the two-way one it states: DQN/C51/QR-DQN run Polyak ungated every learn step (`tau > 0`) with `target_update_frequency` **inert** at the shipped default (the #182 fix's self-gating `sync_target()`, `dqn_agent.rs:385-400`); SAC gates the **soft** update itself on `target_update_frequency` (`sac_agent.rs:757-771`, SB3/CleanRL's unified convention); DDPG/TD3 have **no** frequency field but are not "sidestepping" it as the issue claims — Polyak is gated on `policy_frequency` (`ddpg_agent.rs:566-605`, `td3_agent.rs:670-714`), an **undeclared alias for target cadence** tied to the actor-delay knob, which is the worse case because a reviewer checking the issue's claim against `ddpg_config.rs` sees no field and misses the aliasing (correction recorded verbatim from the #182 vault note's 2026-07-24 addendum). Introduces `TargetUpdate { tau: PolyakTau, every: NonZeroUsize }` (`target.rs`) — `hard`/`polyak`/`try_polyak` constructors, `fires_at(updates) -> Option<f64>` predicate, `τ = 1.0` a hard copy **by degeneracy, not by variant** — adopted by all six off-policy configs; DDPG/TD3 keep `policy_frequency` as the actor-delay knob only, gaining `target_update` as a newly independent field, so the aliasing is removed. Deletes, by ADR 0027 §3/0055 §4 construction, `config::in_range("tau", 0, 1)` from all six configs, `config::at_least("target_update_frequency", 1)` from SAC, and the three `tau<=0 && target_update_frequency==0` cross-field blocks from DQN/C51/QR-DQN — the combination becomes **unrepresentable**; the three `rejects_frozen_target_network`-shaped tests are replaced, not deleted. Closes **#455** (`tau=0.0` passing `config::in_range`'s closed interval) by construction; `config::in_range` itself is deliberately unchanged (every other caller has legitimate closed endpoints). **Records two corrections to the issue's premises:** no config in the workspace derives `Serialize`/`Deserialize` (all seven `*TrainingConfig`s are `#[derive(Clone, Debug)]`; `Hyperparameters` is a free-form `BTreeMap`, not a serialized config), so there is **no `FORMAT_VERSION` bump** — the real stale-config vector is struct-literal/builder construction, which the field removal turns into a **compile error**; and the unified operator is not merely an SB3/CleanRL convention but appears in the primary literature years earlier — Haarnoja et al. 2018a (SAC, arXiv:1801.01290) Appendix D Table 1 runs a "SAC (hard target update)" ablation at τ=1 beside standard SAC purely by setting τ=1 on the soft rule, and Fujimoto et al. 2018 (TD3, arXiv:1802.09477) §3/§5.2 states and uses the same generality four years before SB3 — what is rejected is SB3/CleanRL's **two-flat-`pub`-field shape**, not their semantics; the theory literature (Lee 2026a/b — **preprints**, Kobayashi & Ilboudo 2020/21, Zhang et al. 2021) treats the two mechanisms as distinct with distinct proofs and never claims equivalence, so the ADR does not overclaim one. Rejects the issue's own two-variant `Hard{every}|Polyak{tau,every}` enum (representable-but-equivalent at `tau=1`, breaks `PartialEq`-as-identity), a `tau ∈ (0,1)` open-interval enum (asserts τ=1 illegal, which is false; the shipped invariant is the half-open `(0,1]` — zero excluded because it *is* the frozen-target state), reusing `rlevo_core::Probability` (no dedicated name for the τ=0 frozen case), placing the type in `rlevo-core` (one consumer, not two), and a generic applier unifying the five `soft_update` traits (an ADR-0052-scale refactor; the per-site `.clone()`+`?` idiom is **load-bearing** per ADR 0057 and a macro would hide it). Defers the default-*value* question to **#337**. |
| [0059](0059-target-update-cadence-counts-gradient-updates.md) | **The target-update cadence counts gradient updates, not environment steps.** Companion to ADR 0058, recorded separately because it alone is reversible. In-tree, DQN/C51/QR-DQN currently gate on `self.step` (env steps, `on_env_step`); SAC/DDPG/TD3 already gate on `self.critic_updates` (gradient updates). Every canonical paper counts gradient/parameter units — Nature DQN's `C` is explicitly "measured in the number of parameter updates" (Extended Data Table 1); SAC's interval sits inside "for each gradient step do" (Haarnoja 2018a); TD3's `d` is "updates to the critic" (Fujimoto 2018 §5.2) — and the **only** primary-source dissenter is SB3's `dqn.py` docstring, an artifact of vecenv `_n_calls` plumbing, not an algorithmic claim; CleanRL's env-step counter is correct only at its 1:1 update-to-data default. Rainbow's own unit (32K raw frames, Table 1) is a **third**, unrelated one, evidence that "environment steps" is already ambiguous in the Atari literature. **Decision:** `TargetUpdate::every` (ADR 0058) counts gradient/optimizer updates uniformly on all six agents, non-configurable — `Cadence::EnvSteps|GradientSteps` is rejected as a representable-but-meaningless state at SAC/DDPG/TD3's own update site, which holds no env-step counter. `learning_starts`/`train_frequency` correctly stay in env steps (no gradient step exists before the first one), so a config now carries two units, named in the accessor rustdoc. **API consequence:** `sync_target()` is deleted from `DqnAgent`/`C51Agent`/`QrDqnAgent` along with its three unconditional train-loop calls; the target update moves inside `learn_step`, removing the "train loop forgot to call it" failure mode. **Subtle counter-advance rule, stated explicitly:** SAC/DDPG/TD3 already advance `critic_updates` unconditionally through an ADR-0056 non-finite-loss skip; DQN/C51/QR-DQN currently early-return *before* any counter could advance (`dqn_agent.rs:546-548`) — their new gradient counter **must** advance unconditionally too, or the cadence silently drifts on a diverging run, the same defect class in a new place. Defaults are chosen to be **bit-identical to today**: `polyak(0.005, 1)` for DQN/C51/QR-DQN/SAC, `polyak(0.005, 2)` for DDPG/TD3 (matching the `policy_frequency` it decouples from); the existing `target_update_frequency = 10_000` is **deliberately not carried across** — it was inert under `tau=0.005`, and adopting it literally under the unified contract would collapse Polyak's cadence 10,000×, an incoherent pairing SB3 avoids only by also setting `tau=1.0`. Bundling that value change into this ADR would make the diff behaviourally unbisectable; the default-*value* question (Atari-scaled `10_000` against classic-control `steps_per_episode: 1000`) is deferred whole to **#337**. This dissolves, rather than merely documents, the #182 vault note's "unit trap" (the workspace's `10_000` env steps was 4× more frequent than Nature's `C=10,000` parameter updates, purely from counting the wrong thing). |
| [0060](0060-config-values-must-be-finite.md) | **Config *values* must be finite; config *bounds* may be `±∞`.** Resolves #353 (`config::positive` accepts `inf`) by naming the distinction the predicate signatures never expressed: `lo`/`hi` are the **schema**, supplied as source literals, and `f64::INFINITY` is the intended spelling of "unbounded above"; `got` is the **data**, and a config field is never legitimately `NaN` or `±∞`. `positive` and `in_range` now reject a non-finite `got`; `ordered`/`distinct` require both arguments finite; `lo`/`hi` are deliberately unchecked. New `ConstraintKind::NotFinite { got: f64 }` checked **first**, so it wins over `NotPositive`/`OutOfRange`/`NotOrdered`/`DegenerateInterval` — a non-finite value is a wrong-kind-of-number problem, and `value inf must be strictly positive` is a message that argues against itself and offers no repair. `ConstraintKind` becomes `#[non_exhaustive]` (the ADR 0055 §5 enums-only use). **Extends ADR 0026, supersedes nothing** — `Validate`, the `ConfigError` shape, and the construction-chokepoint rule are untouched; what 0026 left unstated is the *domain* the float predicates check over. **The consequence was not theoretical:** `f64::INFINITY as f32` is `f32::INFINITY`, not `NaN`, so an infinite `alpha_lr` (`sac_config.rs:98`, guarded only by `config::positive`) reaches `LogAlpha::adam_step`, where at `grad == 0` the step is `inf · 0 = NaN` and the `[−88, 88]` clamp **propagates** rather than rescues (`sac_alpha.rs:164-168,246-247`) — #184 patched that one site defensively; the other 104 had no guard. **The workspace already had the cure twice, at the wrong layers** — `NonNegativeRate`'s `is_finite() && r >= 0.0` (ADR 0031, `rate.rs:82,95`) at the *newtype* layer and `qrdqn_config.rs:262`'s hand-rolled `!(0.5·κ²).is_finite()` at the *call-site* layer — two independent local cures being evidence of a missing shared-layer rule, not of two unrelated fixes. **The bound half is equally load-bearing:** `Bounds` (ADR 0027) legitimately holds `Bounds::new(0.7, f32::INFINITY)` (`locomotion/common.rs:312`, with both one-sided infinite ranges tested at `bounds.rs:202-203`), so "reject infinity everywhere" would have been wrong. **One predicate edit closes 142 call sites with zero migration** — 105 `config::positive` (learning rates, `cartpole.rs:225-229` `masscart`/`force_mag`/`tau`, `dt` in pendulum/acrobot) plus 37 `in_range(.., f64::INFINITY, ..)` — and makes every *future* unbounded-above check correct for free, which matters because the issue's own count drifted **103 → 105** while it sat open: any remedy scaling with the call-site count is stale when it lands. Rejects **per-site `finite_*` helpers** (two spellings of one check, correctness contingent on 37 migrations all landing, hole stays open in whichever config is forgotten — the issue's failure mode with extra steps); rejects **reusing `NotPositive`/`OutOfRange` for infinity** (free, but leaves a misleading message on the exact input class the rule exists to reject); rejects **deferring `#[non_exhaustive]`** (free now — all five in-workspace `match` sites already carry a wildcard `other =>` arm — breaking later; deferring converts a two-way door into a scheduled one-way door). Deliberately adds **no** `finite`/`finite_nonneg`/`finite_min` helper: `in_range(C, f, 0.0, f64::INFINITY, x)` stays the single blessed spelling, because a second one means 37 sites each *choosing*. `ordered` consequently can no longer express a half-open interval — intended, since a half-open **range** is a `Bounds` (ADR 0027). Records **one deliberate deviation** rather than leaving it to be rediscovered: TD3 `noise_clip` (`clip(x, −∞, ∞) ≡ x`, i.e. unclipped target smoothing) and CMSA-ES `tau_c` (`1/τ_c → 0` freezes the covariance update, `cmsa_es.rs:475`) are the two parameters admitting `+∞` as a *numerically* well-defined sentinel, and both are rejected anyway — neither is canonical (Fujimoto et al. 2018 fix `c = 0.5`; Beyer & Sendhoff 2008 *derive* the always-finite `τ_c = 1 + N(N+1)/(2μ)`, which is what `cmsa_es.rs:136` computes), both fields are `f32` so `f32::MAX` says "effectively unclipped/frozen" exactly, and a predicate whose strictness depends on which field it guards is the looseness this ADR removes. Costs recorded honestly: behaviour-breaking with **no source change** (a downstream `Validate` impl inherits stricter behaviour by recompiling), and kind assertions move for inputs that were *already* rejected (`NaN`/`−∞` shift from `NotPositive`/`OutOfRange` to `NotFinite` — the accept/reject outcome is unchanged, only the diagnosis moves). Narrows ADR 0055 §Consequences cost 3: the first config loader inherits finiteness rejection instead of owning it, `inf` being representable in JSON/TOML/bincode alike. |
| [0061](0061-optional-facing-and-tensorconvertible-no-fabrication.md) | **`GridObservation`/`GoToDoorObservation::agent_direction` becomes `Option<Direction>`, and `TensorConvertible`'s contract gains a second clause: any field the row-writer omits must decode to an explicit absence, never a fabricated value.** Resolves #286 (`from_tensor` hard-coded `Direction::North.to_u8()` on every decode — a plausible-looking value that was never a measurement — while `write_host_row` never wrote a facing byte at all) and #844 (`agent_direction` becomes a typed `Direction`, making an illegal byte unrepresentable) in one pass, because `GoToDoorObservation` carries the identical, unfiled defect and is the ADR-0043 sibling type the mission-channel precedent exists for. **Keeping the facing out of the tensor is canonical, not a shortcut**: the view is already rotated into the agent's frame (`egocentric_view` + `rotate_view_offset`, `grids/core/grid.rs`), matching canonical Minigrid's own `gen_obs_grid` + `rotate_left`; canonical ships `direction` as a *separate* dict entry, never inside the image, and every inspected baseline drops it before the network — `ImgObsWrapper` ("use the image as the only observation output"), `rl-starter-files`' `ACModel`, and RIDE/NovelD's shared `MinigridPolicyNet` all reference the image only; the BabyAI paper's own model (Chevalier-Boisvert et al., ICLR 2019, arXiv:1810.08272v4, §4.1) takes image + instruction with no third term, and its Baby Language BNF has no absolute compass term at all. **The honest counterexample is recorded, not glossed over**: `FullyObsWrapper` stamps `agent_dir` into the agent's own cell precisely because there is no rotation left to carry it implicitly — direction is redundant *because* the view is rotated, so this decision is scoped to the rotated branch, and a future unrotated/fully-observable grid variant reopens it. Rejects encoding as a 4th channel (deviates from canonical *and* spends 33% more features — `[7,7,3]`→`[7,7,4]`, 147→196 — on a signal every baseline discards); rejects removing the field (canonical does ship it, `GridState::project()` threads it, and `grids/memory.rs:1311`'s cue-invariance assertion reads it); rejects resetting to `Direction::default()` on the sharpest ground in the ADR — `default()` is `East`, also the *most common real facing*, so a round-trip check would pass by coincidence whenever the agent faced East, which is strictly worse than the conspicuously-wrong `North` it would replace; rejects an associated `ROUND_TRIP_IS_TOTAL: bool` const (nothing branches on it — no production code decodes a grid observation at all — so it is unenforceable write-only metadata that doesn't say *which* field is partial); rejects splitting into a tensor-only payload type (`Environment::ObservationType`'s bound requires `TensorConvertible` on the observation itself; `grid_empty_rl.rs`/`grid_memory_rl.rs` train DQN directly on `GridObservation`, so a split breaks every off-policy agent or keeps an identically-partial delegating impl); rejects `from_tensor` returning `Err` (throws away the useful view-decode half and makes #860's proptest round-trip unstatable). Honestly downgrades severity: nothing in production round-trips an observation through a tensor (`ExperienceTuple` stores `observation: O` by value; every grid `from_tensor` call site is a test, doc example, or bench encode-only path) — fixed anyway because the honest option cost about the same as the dishonest one, and because "no current call site decodes" is a snapshot of today's code, not a property of the design, that an offline-RL loader or tensor-backed replay would walk straight into. Closes the loop with ADR 0043 into one rule: anything a policy must read goes in the tensor (0043's mission channel — `GoToDoorObservation`'s `MISSION_CHANNEL` round-trips totally, vindicating 0043 rather than amending it), anything else must not be fabricated on decode (this ADR). Records clause 2 as **not machine-checkable** — review-enforced, the same class of convention as `rules.md`'s `total_cmp`-over-`partial_cmp` rule, which is why it lives in `rules.md`'s invariant table, not only here. `Direction::to_u8` stays public despite being callerless in-tree (it is the canonical Minigrid byte order); no `from_u8` is added. Extends ADR 0052 (the trait this amends); closes the loop with ADR 0043; supersedes nothing. |
| [0062](0062-grid-layout-fidelity-and-no-dead-rng.md) | **Grid layouts reproduce the draws their upstream Minigrid `_gen_grid` makes, and a dead RNG field is not a resting state.** Resolves #282 (nine grid envs hold an `_rng` that `rg 'self\._rng\.'` proves is never read, re-seeded inside `reset()` on every call) and #108 (the same family's `seed` doc implies a layout randomization that does not exist). **Partially supersedes ADR [0029](0029-host-rng-seeding-convention.md)** — its §3 dead-`_rng` grid carve-out (lines 117-119) and Neutral consequence (line 161) only; every other 0029 decision stands and this ADR depends on them. The carve-out contradicted 0029's *own* rule six lines earlier ("Deterministic envs are allowed, but must still not re-seed on reset") because it classified nine envs as deterministic on the evidence that they do not sample, without asking whether they should — a read that rests on conflating arXiv:2306.13831 §2.2's **transition** determinism with layout determinism, when §2.4/Fig. 4 present `_gen_grid` (which samples) as the world-generation seam. Reconciled per-env against Farama Minigrid `master`: **`Empty` and `DistShift` are verified faithful** (upstream makes zero `_gen_grid` draws for their shipped ids; DistShift's determinism *is* the distributional-shift experiment) and deliberately stay deterministic; **seven deviate**, five tractably (`LavaGap`, `Crossing`, `DoorKey`, `FourRooms`, `UnlockPickup`). **Every env is now exactly one of two things**: it samples (persistent `rng`, `build(cfg, &mut R)` with `R: Rng + ?Sized`, `reset_with_seed`, **no** re-seed in `reset`) or it is honestly deterministic (**no RNG field at all**, plus a `seed` doc saying why — "reserved for future variants" is not an acceptable doc). No third state, which is what makes the guard exception-free: the hazard is that adding sampling *without* deleting the re-seed line leaves every episode identical while a "we sample now" test passes. Enforced by a crate-wide source-text guard modelled on `tests/landscape_dim_guards.rs` that resolves each `seed_from_u64` to its enclosing `fn` and requires an allowlist of constructors/replay hatches (`reset_with_seed` legitimately contains the line a naive grep would flag); its limits — brittle to reformatting, defeatable by aliasing, catches the accident not the adversary — are recorded, and that *is* the threat model. Also fixes the shared-sampler gap `CHANGELOG.md:2004` admitted in prose: a new `grids/core/placement.rs` of free functions (not a `Grid` extension trait, not a borrowing `Placer`) whose exhaustion **returns `Result` propagated with `?`, never `.expect()`** — the chokepoint validates config, but exhaustion depends on config *and* prior draws, and `EnvironmentError::Config`'s rustdoc already anticipates the reset-time procedural rebuild — and which **materializes the candidate set and draws a uniform index** (as `dynamic_obstacles.rs:389-402` already does) rather than porting upstream's unbounded rejection loop, a deliberate deviation whose failure mode would otherwise be a non-terminating `reset()`. **Behavioural / semver-relevant** in the same class as 0029's own `reset()` change (alpha): five envs now vary per episode, and their fixed-script oracles must become planner-driven seed loops. `MultiRoom` (#1021, procedural generation) and `Unlock` (#1020, a confirmed door-in-the-perimeter-wall defect at `unlock.rs:293-305`) ship **knowingly non-conformant**, tracked by issue and documented in-file; #282 therefore stays open on a 7/9 checklist rather than being closed against work not done. #108 closes. |
| [0063](0063-grid-visibility-occlusion.md) | **All 12 grid envs implement env-side `Sensor` with a per-env `const VISIBILITY`, `process_vis` ports Minigrid's shadow cast as a post-pass, and an unseen cell is `Option<Entity>::None` — never an `Entity` variant.** Resolves #281 (`egocentric_view` masked nothing, so `see_through_walls` was effectively `true` crate-wide). **Supersedes ADR 0043 §3** (the stated occlusion non-decision) **and amends ADR 0047 Decision 5** (the grid family's `Sensor` exemption): that exemption's premise — one shared, state-pure projection covers all eleven envs — is false on canonical's own numbers (eight occluded, four see-through), so it lapses on its own terms. `impl Observable<3> for GridState` is removed; each env's `Sensor::observe`/`observe_reset` forwards to the shared `observe_grid`/`mask_view`, so the per-env difference is one constant, not eleven duplicated shadow casts (`grid::process_vis` stays the single port of `Grid.process_vis`, flood fill not ray cast — a lone wall casts no shadow). `egocentric_view` is demoted to `pub(crate)` and dropped from the `grids::core` re-export; a raw unoccluded view is wrong for eight of twelve envs and must not be public. An `Entity::Unseen` variant is rejected: it would propagate into the versioned `GridTile` wire type the WASM report client mirrors, so absence is `Option<Entity>::None` instead (mirrors `agent_direction`, ADR 0061, and `docs/rules.md` §3's no-fabrication clause). `Entity::type_u8` is renumbered to canonical `OBJECT_TO_IDX` (`Empty` 0→1 … `Lava`→9, `0` reserved for unseen) — **measured as a live correctness defect, not parity polish**: under the old numbering `UNSEEN_TYPE == Entity::Empty.type_u8() == 0`, so masked-empty cells were wire-identical to seen-empty ones, and at `MemoryEnv`'s fork decision cell facing West the occluded and unoccluded observations were byte-for-byte identical — occlusion existed but was invisible to any policy until this renumbering. **ADR 0043's predicted `MemoryEnv` `MIN_SIZE` 11→7 relaxation was executed and refuted**: a sweep at sizes 7/9/11/13 found the occluded and see-through violation sets identical at every size (5 violating poses at 7 and 9, 0 at 11/13, including the two decision cells adjacent to the fork) — the flood fill routes around the corridor walls through the open start room, so occlusion buys `MemoryEnv` nothing, `MIN_SIZE` stays 11, and canonical S7/S9 remain unreproducible. `FourRooms`/`MultiRoom`/`DoorKey`/`UnlockPickup` get harder under occlusion, but measured more weakly than the floor plan suggests — `FourRooms`'s cross openings let the flood fill spread sideways into neighbouring rooms, so the goal was maskable from any pose in only 2 of 12 seeds. Rejects a `see_through_walls` field on `GridState` or `Grid` (re-commits the category error 0047 diagnosed); rejects a config-driven visibility toggle (a `Deserialize`-able config is user-supplied runtime data, `rules.md` §4, and could silently re-break Invariant M); rejects appending unseen at index 9 instead of renumbering (an all-zero tensor must decode as "all unseen," not "all confirmed empty," for the zero-padded recurrent policies these envs exist to train). `GoToDoorObservation`'s mission channel is a deliberate, documented exception to masking (it carries the agent's own goal, not something perceived through the grid) — currently inert since `GoToDoorEnv` stays `SeeThrough`. Known, separately-tracked gaps: canonical's carried-object-in-own-cell behavior (rlevo shows the world cell instead) is not ported here, and `build_snapshot`'s survival exposes but does not fix a pre-existing `done → EpisodeStatus` defect (every env reports a step-limit cutoff as `Terminated`, never `Truncated`). |
| [0064](0064-observation-carries-no-serde-supertrait.md) | **`Observation<R>`'s supertraits become `Debug + Clone + Send + Sync`; a serde requirement is declared at the consuming seam, never as a supertrait on a domain trait.** Resolves #405. The removed `Serialize + for<'de> Deserialize<'de>` was justified by a doc comment citing "storage in a replay buffer", and that claim is **false in every direction**: no function, struct, or trait in the workspace generically requires `O: Serialize`; `ExperienceTuple`/`History` derive only `Clone, Debug`; and ADR 0050 records that the replay seam derives no serde and **erases the action payload precisely to avoid imposing bounds**. The workspace's one real persistence consumer of a domain type, `RecordingTap`, declares `where E::ActionType: Serialize + Clone` explicitly (`env_tap.rs:322`) and requires **nothing** of the observation — `capture_frame` persists action bytes, reward, ascii, styled, and family payload, so no observation has ever been in a persisted payload and **no record-schema `FORMAT_VERSION` bump is needed**. The bound bought no invariant (it constrained neither wire format nor round-trip fidelity — contrast ADR 0061's checkable `TensorConvertible` clauses) while costing two hand-written impls no consumer exercises: `ContextualBanditObservation`'s ~30-line validating `Deserialize` (upholding rules §4, deserialized data must `Err` not panic) and `CarRacingObservation`'s `Visitor` over 27,648 bytes (serde derives no array impl above length 32). It also **left the real bound set invisible where callers read for it**: `experience.rs`'s bounds doc on `ExperienceTuple`/`History` cites C-STRUCT-BOUNDS to refuse `Send`/`Sync` bounds as restriction-without-guarantee — a narrow claim that was and remains correct, unamended by this ADR — while their `O: Observation<D>` header silently imported a `Serialize + Deserialize` obligation that doc never mentions, so the defect is **incompleteness, not contradiction**; the guideline is cited for its **rationale** and for internal consistency only, since it literally governs struct definitions rather than supertraits, and the ADR declines to overstate it. **Applies ADR 0052 §8's ruling in the removal direction**: 0052 found `Observation<R>: HostRow<R>` newly *expressible* and deliberately deferred it for want of an invariant and a consumer; the same two grounds retire this bound. Rejects **extending the bound to `Action`/`Reward` for symmetry** on measured cost — **260+ compile errors, a floor** (cargo aborted before reaching `rlevo`, `rlevo-examples`, `rlevo-test-support`), ~31 new derives across ≥7 crates, 25 unique types, with **zero non-derivable types**, so it is recorded as a *cost* rejection and not a feasibility one; rejects **fixing only the doc comment** (keeps every cost and stops merely *explaining* it — an honest "required by nothing" supertrait invites the next reader to invent a justification); rejects a **feature-gated supertrait** (makes `Observation` two traits, so a downstream generic compiles or fails on unrelated feature unification); rejects a **`SerializableObservation` marker trait** (zero implementors, zero consumers — the 0052 §8 bar is a consumer, not an anticipation). States the counterargument rather than hiding it: ADR 0061's warning that "no current call site decodes" is a snapshot of today's code, not a property of the design, **applies here too** — the distinguishing point is that 0061 fixed a design producing *wrong output* (a fabricated value, so deferring meant shipping a lie) whereas this one produces only a *cost*, and its named anticipated consumers (roadmap "Distributed replay architectures", offline-RL loaders, checkpoint restore) are each served by an **additive, reversible** `where T: Serialize + for<'de> Deserialize<'de>` at their own seam — while re-removing a supertrait later is not reversible. That asymmetry is why this is a two-way door and the status quo was a one-way one. **All existing serde derives and both manual impls are retained** — the change removes a trait *obligation*, not the types' *capability*, so `serde_json::to_string(&concrete_obs)` is unaffected. Strict relaxation for implementors (every `impl Observation<R>` still compiles) but **breaking for downstream generic code** relying on the implied bound; in-tree affected sites: 0. Records the live failure mode: the removed doc comment is exactly the argument a future contributor will reconstruct when distributed replay lands, mitigated only by this ADR, `base.rs`'s replacement rustdoc, and the sharpened `rules.md` §3/§8 rows. Supersedes nothing; `Action` and `Reward` deliberately unchanged. |
| [0065](0065-non-finite-reward-is-dropped-at-replay-ingestion.md) | **A non-finite reward is dropped at replay ingestion, counted, and warned on a decade schedule.** Resolves #352. **Counterpart to ADR 0056**, which deferred this issue by number: 0056's `FiniteLossGuard` already breaks the NaN-reward → NaN-loss → NaN-weights chain before `backward()`, so this ADR's residual harm is narrower than the issue's own stale title ("permanently poisons the target network") claims — a poisoned transition instead lingers in the FIFO buffer until eviction, and every minibatch resampling it yields a loss 0056 silently skips (0056's `warn!` is a one-shot latch, so only the first such skip is ever logged). A `FiniteRewardGuard` in `algorithms/shared.rs`, one per agent, runs `f32::is_finite()` at the top of each of **six** (not the issue's four — `dqn_agent.rs:386`, `ddpg_agent.rs:417`, `td3_agent.rs:466`, `sac_agent.rs:476`, `c51_agent.rs:405`, `qrdqn_agent.rs:366`; C51/QR-DQN were added later by copying an unguarded `remember`, which is why the guard is a shared struct with a per-agent test, not six independent inline checks) off-policy agents' `remember`. A non-finite reward's transition is **not pushed**; the drop re-fires on **every** occurrence (never latched — a run emitting NaN every step must be protected every step), but the `warn!` escalates on a decade schedule (`dropped == 1, 10, 100, 1000, …`, carrying the running total) rather than either firing every time or latching once — the one deliberate deviation from 0056's pure one-shot latch, justified because a dropped transition is *unbounded data loss* where a skipped update is self-limiting. Traces C51's NaN propagation through `projection.rs:97-168` to confirm it does **not** escape the loss guard, and records as an **open, untested question** (not a claim) that the `NaN.floor().int()` saturation-to-`0` the trace relies on is a host `as`-cast guarantee not contractually pinned on a GPU backend, where an out-of-range scatter index would be a `rules.md:232` panic-on-runtime-data reachable only through this hole. Lead precedent is **`Priority::try_new`/`from_td_error`** (`replay/priority.rs:115,142`), not ADR 0060 — both reject NaN/±Inf on a *runtime-derived* float at a buffer boundary, where 0060 is about *config* checked once at construction. Rejects a validating `Transition::try_new` (every field is `pub` and a public doctest struct-literal-constructs one, so validation-beside-public-fields is unenforceable decoration); rejects a validating `ScalarReward::new` and **closes the thread outright**, not by deferral — the tuple field is `pub` by documented intent, `new` is `const fn`, and decisively `Reward` is a **trait** with `Environment::step` constraining `RewardType` not at all, so a core-level check could not close the hole even where it could run; `remember(reward: f32)` is the universal chokepoint precisely because every reward type has already been erased to `f32` by the time it is called. Rejects `debug_assert!` (the C51 review's own §1.3 recommendation, closed by name, same reasoning as 0056 §4: a release no-op protects nothing in the long diverging runs that need it); rejects sanitize-to-`0.0` (`f32::clamp` propagates NaN, documented in-tree at `c51/projection.rs:97`; a deliberate NaN→0 substitution manufactures a reward the environment never emitted, directly against ADR 0061's no-fabrication rule); rejects `panic!`/`assert!` (`rules.md:231-232`, reward is user-supplied runtime data); rejects `remember -> Result` as out of scope (**#317**'s job, though `admit(reward) -> bool` is designed to become `?`-able with no semantic change when #317 lands). States bookkeeping consequences explicitly so a future reader does not "fix" them: the env-step counter, ε-decay, and ADR 0059's gradient-keyed target cadence all advance unaffected by a drop; `can_learn` is correctly delayed (an all-NaN environment yields a buffer that never fills, the *correct* degenerate outcome, made diagnosable rather than a silent hang by the escalating warn); and **episode return is deliberately left poisoned** (`episode_reward += reward_f32` still adds the NaN) in a stated tension with 0056 §3's loss-mean exclusion — a loss is an internal diagnostic, an episode return is the primary scientific measurement, and silently excluding a step from it would report a return the agent never earned (`best_score` is unaffected, `metrics.rs:71`'s `f32::max` ignores NaN). **Literature is honest about being ahead of practice, not behind it**: Stable-Baselines3, RLlib, Tianshou, OpenAI Baselines, and Acme were each inspected at their `add`/adder path and **none** validate reward finiteness (Dopamine unreachable, recorded unverified, not negative) — 5/6 confirmed absent, so this ADR is ahead of ecosystem convention, not a port of it. Precisely scopes reward clipping's non-subsumption: `Inf.clamp` correctly returns a bound (±∞ orders normally) but `NaN.clamp` returns `NaN` (every IEEE-754 ordering comparison against NaN is false, the reason `minNum`/`maxNum` exist), so clipping neutralizes ∞ but not NaN, quoting Mnih et al. 2013 verbatim on rationale; rlevo does not clip by default regardless. Uses the AMP/`GradScaler` precedent (Micikevicius et al. 2018 §3.2, quoted verbatim) as **shape analogy only**, explicitly not inherited uncritically the way 0056 used it: AMP discards a *computation* whose corrupted example returns on a later pass; this ADR discards a *datum* that never comes back. Grounds decade escalation in an **independent statistical argument**, not just an operational one: PER's importance-sampling correction (Schaul et al. 2016) does not apply (nothing to re-weight on a hard 0/1 admission), but whether drops are missing-completely-at-random (policy-independent bug, statistically inert) or missing-not-at-random (correlated with a genuinely diverging policy's visited states, a systematic hole) is answerable only by telemetry from the run itself — the load-bearing reason the count must be observable and the warn must convey magnitude. Out of scope, deliberately, each with a stated reason: **PPO/PPG on-policy rollout** (larger blast radius per occurrence — GAE's reverse recursion spreads one NaN reward to every earlier timestep, then batch-mean normalization spreads it further — but this ADR's "don't push the tuple" semantic does not transfer to a contiguous positional trajectory, so filed as **#1042** rather than folded in; mitigated today by the rollout buffer clearing each iteration and 0056 already containing the resulting NaN loss); **non-finite observations at the same seam** (structurally identical hole, a per-element tensor scan instead of one register compare, different cost/benefit, filed as **#1043**); **`ScalarReward`/`rlevo-core`** (closed above, not deferred). |
| [0066](0066-clamp-nan-behavior-is-backend-specific-pin-with-is-nan.md) | **`Tensor::clamp`'s NaN behavior is backend-unspecified; where correctness depends on what happens to a NaN, pin it with an explicit `is_nan` mask, never an op's implicit handling.** Resolves #1044, the open GPU question ADR 0065 §Context filed by number. Measured (Apple M2 Pro, Metal 4; `burn` 0.21.0, `cubecl` 0.10.0, `wgpu` 29.0.x): `burn-flex`'s `clamp` lowers to `f32::clamp` and propagates NaN; `burn-cubecl`/wgpu's lowers through the WGSL `clamp` builtin to Metal's `fmin(fmax(...))` and **rescues** NaN to the lower bound — the opposite of Flex, and `clamp_min`/`clamp_max` are inverted again relative to `clamp` on each backend. In C51's `project_distribution`, a NaN reward yields `[NaN, 0, …]` (loud, ADR 0056's `FiniteLossGuard` fires) on Flex versus `[1.0, 0, …]` summing to **exactly 1.0** — a well-formed probability vector asserting certainty of the worst return, with no NaN anywhere — on the wgpu/Metal backend measured; a row-sum postcondition would not have caught this. `Tensor::is_nan` is safe to build a guard on: `cubecl-wgpu` lowers it as a bitcast/mask/compare integer polyfill, not `x != x`, so it survives fast-math (caveat: the unenabled `msl`/`spirv` compiler paths would emit Metal's native `isnan`, which *is* marked optimizable). Separately documents `burn-cubecl`'s scatter kernel as `launch_unchecked` with `cubecl-wgpu`'s `bounds_checks: false`, so an out-of-range index does not panic but writes into an unrelated tensor's buffer at an offset that tracks the allocator's stride and varies with allocation order — unbounded, not fixed-offset; Flex panics on the same input. **Corrects, without editing,** a Flex-specific "`f32::clamp` propagates NaN" premise stated as universal fact in three places: `c51/projection.rs:96-101`'s own doc, ADR 0065 §Context (`:63-73`), and ADR 0056 §Out-of-scope (`:112-115`) — both ADRs' own conclusions stand unamended; only the backend-specific reasoning in their traces is superseded. Describes (without implementing — landed concurrently in `algorithms/shared.rs` and `c51/projection.rs`) a `clamp_preserving_nan(t, lo, hi)` helper (`is_nan` mask, `clamp`, `mask_fill` the NaN back in) applied at both of `project_distribution`'s coordinate clamps, paired with a clamp of the derived `Int` indices to `[0, num_atoms-1]` — **the two are only correct together**: preserving the NaN through the coordinate clamps is what *creates* the out-of-range index (on Metal, `NaN.floor().int()` is `i32::MIN`, not the host `as`-cast's saturating `0`), and the index clamp is what makes that safe. Uses `is_nan` rather than `is_finite` because `±inf` is handled identically and correctly by the plain clamp on both backends — that saturation *is* the algorithm's Bellman-shift semantics; only NaN needs the explicit mask. Two blessed primitives result: `clamp_preserving_nan` (preserve, new) alongside `sanitize_fitness_tensor` (replace, ADR 0034) — a bare `Tensor::clamp` on possibly-NaN data with neither is a review-flagged defect, the same enforcement class as `rules.md`'s `total_cmp` convention. Rejects sanitizing the row to zero (indistinguishable from a converged sample — harder to notice than the bug); a row-sum postcondition (the corrupted row sums to exactly 1.0); a host-side finiteness check on the already-round-tripped projected target (free, but the corrupted target is finite so there is nothing to catch); switching to Dopamine's tent-kernel projection (does not fix it — its own `clip` calls sit on the NaN path too, yielding an all-zero row that is harder to notice than the current corruption — and would couple a performance rewrite to a correctness fix). States plainly that **CI does not cover this class**: every workflow is `ubuntu-latest`, and the workspace's one cross-backend test (`crates/rlevo-evolution/tests/backend_parity.rs:128`) is `#[ignore]`d for want of a GPU runner, so regression protection here is a manual GPU run, not an automated gate. Records known-affected sites deliberately left unfixed, each to be filed separately: `ppo/policies/gaussian.rs:460` and `sac/sac_policy.rs:176` clamp a **persistent, gradient-updated** `log_std` parameter, where a rescued NaN becomes a finite, plausible `σ = exp(-20)` consumed consistently by sampling and log-prob alike, and the ADR 0049/#347 host-side warn scan's `<`/`>` comparisons fail on NaN and never fire, on *either* backend; `ppo/losses.rs`'s `min_elem` (measured: NaN `log_ratio` gives PPO policy loss `-0.50172365` on Metal versus `NaN` on Flex — the loss guard never fires); `c51/loss.rs:92`'s `clamp_min` on the KL priority signal. Names the idiom that already gets this right: `rlevo-evolution/src/fitness.rs:324-329` masks NaN to `-inf` *before* `clamp_max`, correct on both backends by construction — only its explanatory comment repeats the corrected premise. `sac_alpha.rs`'s clamps are host `f32`, unaffected. |
| [0067](0067-non-finite-observations-are-dropped-at-replay-ingestion.md) | **A non-finite observation is dropped at replay ingestion, counted, and reported — but never substituted — at `act`.** Resolves #1043. **Counterpart to ADR 0065**, which deferred this issue by number; supersedes nothing, and 0065's §Decision is unreversed. The justifying finding is one issue #1043 does not contain and that inverts its framing: #1043 argues `act` is the urgent half because a NaN action has no downstream guard, but measured on `flex` (Apple M2 Pro, rustc 1.94.1, burn 0.21.0), `relu` **rescues NaN to `0.0`** (`relu([NaN,inf,-inf,-1,2]) = [0.0, inf, 0.0, 0.0, 2.0]`; wgpu propagates), so a ReLU-fronted Q network fed a **fully** non-finite observation emits a finite, in-domain, `is_valid() == true` action — with the one-NaN and all-NaN Q rows *bit-identical* (`[0.023038015, -0.09688881]`, the bias-only output after the first ReLU zeroed everything). The observation is **erased, not corrupted**, so ADR 0056's `FiniteLossGuard` (loss is finite), a Q-value check (row is finite), and an action check (`is_valid` is true) are each structurally incapable of seeing it — **only a check on the observation before `to_tensor` can**, and it is the case CI reaches, CI having no GPU. Records the backends disagreeing in *both* directions: `flex`'s `argmax` is a `>` scan seeded at element 0, so an all-NaN row gives `0` at every width 1..=80 while a partly-NaN row returns **the index of the first NaN** (`[1,NaN,3,2] → 1`, `[5,NaN,0.5,-1] → 1`) — silently wrong on CPU and silently *correct* on wgpu, whose sentinel is `-1` or `width` and in range at **no** width in 1..=80; the unclamped path from that sentinel into `gather` is a distinct defect filed as **#1050**, not fixed here. Benchmark `benches/obs_guard_bench.rs` (`a5a927d`) settles the cost question #1043 said must not be settled by assertion, and **refutes #1043's own hypothesis** that the check could ride the existing `write_host_row` traversal: `remember` stores the typed `O` and never flattens, so there is no traversal there — the ingestion check costs 0.36 % of an env-step cycle at 8 floats but **84–385 %** at a synthetic 27 648-float f32 row, against ≤ 0.52 % of a `learn_step` at the staging seam. Establishes that cost is dominated by the predicate's **spelling**, not memory traffic: `iter().all(|v| v.is_finite())` runs at ~9 GB/s against a ~62 GB/s memcpy because `Iterator::all` must short-circuit and cannot lower to a horizontal reduction, while a branchless `u32::max` over the IEEE-754 exponent field restores ~1× fusion (0.057 ns/elem against a 0.065 ns/elem write) **and is data-independent** — the short-circuiting spelling also confounds any poisoned-vs-clean benchmark control. Enumerates every `row_shape` in `rlevo-environments` to show the workspace splits cleanly: all four image-shaped observations are **integer-backed and structurally unpoisonable** (`CarRacingObservation` `Arc<[u8; 27648]>`, `PixelObservation` `u8 → f32::from(b)/255.0`, `GridObservation` 7×7×3, `GoToDoorObservation` 7×7×4), while every poisonable row is a small f32 vector (24, 10, 9, 8, 6, 4, 3, 2, 1) — i.e. the entire rapier/box2d physics family, exactly where integrator blow-up produces the NaN state 0065's own warning text tells operators to look for, so **the guard is at its cheapest and its most necessary on the same set**. Distinguishes the expensive *cost* case (real and shipped — the default body would materialize 27 648 f32 from `u8` per env step) from the expensive *benefit* case (no instance), which is why guarding unconditionally with the simple spelling is the one option that is definitely wrong: full cost on precisely the types whose benefit is provably zero. Adds `HostRow::row_is_finite(&self, scratch: &mut Vec<f32>) -> bool` as a **provided** method (all 42 existing impls compile unchanged), taking `scratch` **now** because the default body otherwise allocates ~110 KB per env step for the first f32-backed image observation and the signature is a one-way door across 42 impls. Requires each override to carry a **compile-time witness** (`let _: &[u8] = &self.pixels;`) rather than a bare `true`, so an f32-backing refactor fails to compile at the site whose reasoning it invalidates — following `rules.md` §4's `const _: () = assert!(..)` precedent — and warns the witness must be a **concrete type ascription**, never `Into<f32>`, since `impl<T> From<T> for T` means `f32: Into<f32>` and that spelling survives the refactor guaranteeing nothing; states the override is **both** a structural assertion and a performance decision, because framing it as pure assertion is how it stops being maintained. Places the guard at `remember` only — `FiniteObsGuard` in `algorithms/shared.rs`, one per agent, drop-and-count on 0065's unlatched decade schedule with a public `dropped_observations()` — rejecting the **staging seam** (no good action there: dropping breaks the batch shape, and skip-the-step is what 0056 already does one step later for free, so its entire marginal value over 0056 would be attribution) and rejecting **both** (two counters for one event, legitimately disagreeing since staging counts *samples* and ingestion counts *transitions*). Rejects `Action::neutral()` (a breaking core-trait change bought for semantics no site can consume; `A::from_index(0)` and `BoundedAction::low()/high()`'s midpoint already exist). At `act`, decides **detect and report but do not substitute**, on the ADR's own central finding: a plausible in-domain fallback action is the same class of failure the flex-`relu` case demonstrates — a legal-looking action making a broken run appear healthy — so the `act` guard's value is *attribution*, worth having independently of response; flagged in the ADR as its clause most open to challenge. **Corrects, without editing,** two ADR 0065 §Out-of-scope sentences: that observation cost is uniform (it is bimodal, and the split is what the design turns on), and the ride-along assumption (measured false at `remember`). Records that under `PrioritizedReplay` (ADR 0050 §10) a poisoned transition enters at running-max priority, yields a NaN TD error that `Priority::try_new` rejects so the writeback is dropped, and therefore stays **pinned at max forever** — resampled more often than average, never decaying — an independent argument against staging-only; and that a transition with both a non-finite reward and observation increments only `dropped_transitions()`, 0065's guard running first and returning early, so both rustdocs must say the counters disagree. Notes the degenerate case (buffer never fills, training silently never starts) is inherited from 0065 and still correct since the alternative fabricates, but is **weaker here** — an early NaN observation in a diverging locomotion episode is likelier a transient integrator excursion than a permanent defect — so the decade `warn!` carries more weight than in 0065. Warns that a proptest oracle pinning overrides against the default body is a **tripwire, not the guarantee**, because `any::<f32>()` excludes NaN/±Inf by default and the obvious spelling passes **vacuously**; the strategy must request `f32::INFINITE \| f32::QUIET_NAN`. Out of scope, deliberately: **#1050**'s unclamped `argmax → gather`; **PPO/PPG rollout observations** (`record_step` ingests an unguarded `obs` and on `Truncated` calls `value_of(next_obs)`, an immediate forward pass seeding GAE's backward recursion — 0065's #1042 reasoning transfers unchanged); evaluation-only `act` rollouts (detected, never dropped); `Observation<R>: HostRow<R>` (ADR 0052 §8, 12 affected types); and `ContinuousAction::from_slice` being unchecked by contract, bypassing the action types' own validation (`PendulumAction::new` rejects NaN; no agent calls it). Notes `row_is_finite` lands on ~12 action impls and `BootstrapMask` where the question is well-formed but pointless — accepted cost of 0052 §8 being deferred, with the rustdoc worded about *rows*, not observations. |
| [0068](0068-bounds-strictness-enforcement-is-crate-asymmetric.md) | **`Bounds` strictness gets a named helper everywhere and a mechanical guard in exactly one crate.** Resolves #387, which describes a real migration trap and **no live defect**: `config::ordered` requires strict `low < high` while `Bounds::try_new` (`bounds.rs`) accepts `lo <= hi` and *deliberately* permits `lo == hi` (ADR 0027 §2 — `clamp` is well-defined on a point, and every sampler's `lo + (hi - lo) * r` degenerates to the constant `lo`), so folding an `ordered`-checked scalar pair into one `Bounds` field discharges the **ordering** half of the old check and silently drops the **strictness** half — a diff that reads as a pure refactor while being a semantic loosening. The one genuine instance, the two Gaussian heads' `log_std`, was already closed by ADR 0054 §3 (`ppo/policies/gaussian.rs:284`, `sac/sac_policy.rs:123`); the audit #387 notes had not been run **was** run for this ADR and found every other migration to be ADR 0027 §2/§6's documented choice, with §5's promised mitigation actually landed (`debug_assert!(span > 0.0)` at `simulated_annealing.rs:79`, `random_restart.rs:83`). **The justifying fact is that the ratio is perfectly crate-partitioned**, not merely lopsided: of 32 `Bounds` config fields (re-enumerated, `pub` and private alike), all 10 zero-width-legitimate ones in `rlevo-environments` (4 `action_clip`, 3 `HealthyCheck` ranges, 3 mountain-car) and all 20 in `rlevo-evolution` (18 `pub bounds` + 2 private on `HillClimbingParams`/`SimulatedAnnealingParams`) tolerate a pinned range, while **both** strictness-needing fields are `log_std` in `rlevo-reinforcement-learning`; `rlevo-core`, `rlevo-hybrid`, `rlevo-benchmarks` have zero. The minority is one crate, which is what licenses a one-crate guard — a workspace-wide scan would need ~32 allowlist rows, 30 of them restating ADR 0027 §2, and a guard whose rows are 94 % "this one is fine" trains its readers to add a row without thinking. **Three unequal parts:** (1) `config::nondegenerate_bounds(C, field, b: Bounds)` in `rlevo-core::config` delegating to `config::distinct` — **not** to `b.span() > 0.0`, the subtlest clause, because `Bounds::new(-20.0, f32::INFINITY)` has span `inf` which *is* `> 0.0` and the SAC head has no downstream span check (ADR 0049's floor/span guard the PPO path only) — inheriting ADR 0060's finiteness rule, so the helper is *strictly stronger* than "nonzero span" and its name correspondingly over-promises (a legitimately one-sided range like `HealthyCheck::z_range`'s `[0.7, ∞)` simply does not call it); error semantics preserved byte-for-byte (`DegenerateInterval`, same `field`), making the two RL edits a rename, not a behaviour change. (2) A source-text guard test in `rlevo-reinforcement-learning` only, in the shape of `tests/rng_seeding_guards.rs` — bidirectional allowlist (a stale row fails too), `#[cfg(test)]`-region skipping, fail-loud with the repair named — shipping with **2 rows and an empty exemption list**, so "every `Bounds` field in this crate needs a strictness check" is a crate-level invariant rather than a per-field essay; the scan must not be `pub`-only (two evolution fields are private) and resolves matches to struct fields, since `config.rs` matches the same regex on the new helper's own `b: Bounds` **parameter**. (3) Prose only (`rules.md` §4 + `Bounds` rustdoc) for the other 30 fields, where zero width is a *permission* rather than a drift risk, and where the single place it bites sits in infallible `default_for` constructors that a `Validate` seam cannot reach. **Records the strongest argument against itself:** this invokes a mechanical remedy at **recurrence-count zero**, where the `rng_seeding_guards.rs` precedent was written after the same bug recurred twice (#104, then #282) — the countervailing input being the roadmap's near horizon (model-based RL / MPC for robotics, continued CleanRL build-out), which adds continuous-action Gaussian heads into precisely the crate the guard scans, by copy-paste from one of the two existing heads, so recurrence is a forecast rather than a hypothesis; if the roadmap changes, deleting the guard costs nothing. States the crate partition as **a fact today, not a law**, with a named reopen trigger — a second real instance anywhere, the strict population exceeding ~5, or it crossing a crate boundary — whose concrete plausible shape is already visible, since `simulated_annealing.rs:79`/`random_restart.rs:83` **already want** `span() > 0` and settle for `debug_assert!` in a constructor, so a future step-size-derived evolution operator would need the helper and fall outside the guard's scan. Rejects a **`NonDegenerateBounds` newtype** — the most attractive alternative, and rejected on cost, **not** on the false ground that it retires `validate()`: ADR 0054's Consequences are explicit that ADR 0049's floor (`>= -35`) and span (`< 40`) checks stay regardless (`log_std_min = -120, log_std_max = -100` is a well-ordered `Bounds` that still reaches `NaN`), so the newtype removes one line while adding a second public range type that all 30 permissive adopters must answer "which `Bounds`?" about — the inverse of ADR 0054 §2's own "one answer, not four" uniformity reasoning; at 2-vs-30 it does not earn its keep, at 5-vs-30 the arithmetic changes, hence the trigger. Rejects **`Bounds::try_new_nondegenerate`** as worse on the newtype's own strongest ground (it returns a plain `Bounds`, so the invariant does not travel with the value — a fourth spelling with none of the compensating guarantee) and on error shape (distinguishing degenerate from inverted requires enum-ifying the `pub struct BoundsError { lo, hi }`, a breaking public-type change for a check that is not `Bounds`'s business). Rejects **docs-only enforcement for the RL crate** on this workspace's own history — ADR 0054 already rejected that shape for these exact configs, citing "exactly the drift that produced 87 un-validated configs," and #386 *was* that drift arriving. Rejects a **workspace-wide guard** (the 94 %-noise failure mode) and **inverting the default** onto all 32 fields. **Changing `Bounds`'s inclusive invariant is out of scope and explicitly not re-litigated** — ADR 0027 §2 decided it with reasons and 30 of 32 fields depend on the answer. Additive; no migration; refines ADR 0027 §2 and ADR 0054 §3 into a named mechanism and **supersedes nothing**. Records the residual costs plainly: a fourth spelling of "these two numbers differ" now exists in `config` (mitigated by delegation — one predicate, three entry points), the helper's name over-promises, enforcement is now **asymmetric by design** in a way a reader must be told rather than infer (or they file the ADR-0027-relitigating issue this exists to prevent), and the guard is source-text — brittle to reformatting, defeatable by aliasing, catching the accident and not the adversary. |
